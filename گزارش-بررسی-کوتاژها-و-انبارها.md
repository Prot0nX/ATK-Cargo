# گزارش بررسی جامع صفحه «جزئیات کشتی — کوتاژها و انبارها»

**تاریخ گزارش:** ۱۴۰۵/۰۵/۱۹ (2026-08-10)
**دامنه بررسی:** امنیت، صحت عملکرد (باگ)، کدنویسی تمیز و اصولی، بهینه‌سازی عملکرد — هر دو سمت اندروید و سرور
**وضعیت:** فقط بررسی و گزارش (هیچ کدی تغییر داده نشده است)

**فایل اصلی موضوع بررسی:**
- `app/src/main/java/com/atk/atk_cargo/feature/reports/presentation/details/ShipDetailsScreen.kt`

**فایل‌های مسیر داده که در بررسی لحاظ شدند:**

سمت اندروید:
- `feature/reports/presentation/quota_details/QuotasListScreen.kt`
- `feature/reports/presentation/warehouse_details/WarehouseDetailsScreen.kt` (بخش `WarehousesSection`)
- `feature/reports/presentation/dialogs/QuotaWarningDialog.kt`
- `feature/reports/presentation/ships/ShipsListScreen.kt` (بخش `SearchField`)
- `feature/reports/domain/ReportsDomain.kt`
- `ui/viewmodel/ReportsViewModel.kt`
- `ui/screens/ManageReportsScreen.kt`
- `data/repository/ReportsRepository.kt`، `data/model/ReportModels.kt`
- `api/ApiService.kt`، `api/RetrofitClient.kt`، `api/DataModel.kt`

سمت سرور:
- `PHP/protected_proxy.php`، `PHP/app_api.php`
- `PHP/src/Controllers/AppApiController.php` (توابع `getShipDetails`، `getQuotasList`، `getFilteredQuotas`، `editQuota`، `deleteQuota`، `toggleQuotaStatus`، `updateQuotaPercentage`، `updateQuotaPercentageRestriction`، `updateTemporaryTonnage`)
- `PHP/src/Core/Request.php`، `MicroCache.php`
- `PHP/PermissionManager.php`

---

## ۱. خلاصه اجرایی

این صفحه سه نقص ساختاری جدی دارد که هر سه سمت سرور هستند و از طریق همین صفحه قابل فعال شدن‌اند:

۱. **حذف کوتاژ، رکوردهای حواله‌ی نوع‌کالاهای دیگر را هم پاک می‌کند** (شرط `cargoType` در `DELETE` جدول `CargoInfo` جا افتاده) — از دست رفتن غیرقابل بازگشت داده.
۲. **هیچ کنترل مجوزی روی عملیات نوشتن/حذف وجود ندارد** — فقط «نشست معتبر» چک می‌شود. کلاس `PermissionManager.php` در پروژه هست ولی هیچ‌جا فراخوانی نمی‌شود. هر کاربر لاگین‌کرده (حتی اپراتور باسکول) می‌تواند کوتاژ و کل تاریخچه‌ی حواله‌های آن را حذف کند.
۳. **عملیات تغییر وضعیت و درصد فقط با `loadingQuotaNumber` محدود می‌شوند** — یعنی روی *همه‌ی* ردیف‌هایی که آن شماره کوتاژ را دارند (انبار/شرکت/نوع‌کالای دیگر و حتی کشتی دیگر) اعمال می‌شوند.

در سمت اندروید یک کرش قطعی (`!!` روی فیلد nullable)، یک دکمه‌ی گروهی خطرناک بدون تأیید، و یک اسپینر بارگذاری که هرگز نمایش داده نمی‌شود وجود دارد.

از نظر عملکرد، هیچ‌کدام از دو endpoint سنگین این صفحه (`getShipDetails` و `getQuotasList`) کش ندارند و در هر ورود و هر بازگشت از صفحه‌ی انبار دوباره از صفر اجرا می‌شوند.

### جدول اولویت‌بندی

| # | اولویت | مورد | سمت |
|---|--------|------|------|
| ۱ | 🔴 P0 | حذف کوتاژ، حواله‌های نوع‌کالای دیگر را هم پاک می‌کند | سرور |
| ۲ | 🔴 P0 | نبود کامل کنترل مجوز روی عملیات نوشتن/حذف | سرور |
| ۳ | 🔴 P0 | محدود نبودن `toggleQuotaStatus`/`updateQuotaPercentage` به ردیف یکتا | سرور |
| ۴ | 🔴 P0 | کرش `NullPointerException` در `calculateWarningStatus` | اندروید |
| ۵ | 🔴 P0 | انجام تمام عملیات نوشتن/حذف با متد `GET` | هر دو |
| ۶ | 🟠 P1 | `htmlspecialchars` روی مقادیر شرط `WHERE` (لیست کوتاژ خالی / حذف بی‌صدا ناموفق) | سرور |
| ۷ | 🟠 P1 | دکمه‌ی «غیرفعال کردن» گروهی: بدون تأیید، Toggle به‌جای Deactivate، عبور از Rate Limit | اندروید |
| ۸ | 🟠 P1 | اسپینر بارگذاری اولیه هرگز نمایش داده نمی‌شود (`_isLoadingShipDetails` مرده) | اندروید |
| ۹ | 🟠 P1 | `Warehouse.quotas` و `availableExitDates` در پاسخ سرور نیستند اما در کاتلین non-null‌اند | هر دو |
| ۱۰ | 🟠 P1 | `isActive` کشتی از آخرین ردیف انبار گرفته می‌شود نه از همه | سرور |
| ۱۱ | 🟠 P1 | کیبورد عددی روی جستجویی که شرکت/صاحب کالا را هم فیلتر می‌کند | اندروید |
| ۱۲ | 🟠 P1 | باز شدن دوباره‌ی دیالوگ هشدار بعد از هر تغییر داده | اندروید |
| ۱۳ | 🟠 P1 | `Log.d` با شماره کوتاژها در هر Recomposition (نشت داده در لاگ Release) | اندروید |
| ۱۴ | 🟡 P2 | نبود کش (`ETag`/`Cache-Control`/`MicroCache`) روی دو endpoint سنگین صفحه | سرور |
| ۱۵ | 🟡 P2 | Derived Table تکراری `all_vouchers` (کاملاً برابر با `exit_data`) | سرور |
| ۱۶ | 🟡 P2 | زیرکوئری همبسته‌ی `totalVoucherCount` به ازای هر انبار | سرور |
| ۱۷ | 🟡 P2 | ماشین‌آلات مرده‌ی Pager: صفحه‌بند تک‌صفحه‌ای + `AnimatedContent` + دو `LaunchedEffect` | اندروید |
| ۱۸ | 🟡 P2 | ارسال ۱۱ فیلد بی‌استفاده در پاسخ (دو تای آن‌ها همیشه `0`) | سرور |
| ۱۹ | 🟡 P2 | `remember` به‌جای `rememberSaveable` برای جستجو، تب و گروه بازشده | اندروید |
| ۲۰ | 🟡 P2 | محاسبه‌ی دوباره‌ی `loadedTonnage` در کلاینت با وجود فیلد آماده‌ی سرور | اندروید |
| ۲۱ | 🟡 P2 | `Float` برای کیلوگرم + `.toInt()` → خطای دقت در تناژهای واقعی | اندروید |
| ۲۲ | 🟡 P2 | آستانه‌های جادویی `5000` و `9000` داخل کد UI | اندروید |
| ۲۳ | 🟡 P2 | استفاده از قدرمطلق در آستانه‌ی هشدار → کوتاژهای عبورکرده از حد هشدار نمی‌گیرند | اندروید |
| ۲۴ | 🟡 P2 | گروه‌بندی با کلید nullable → احتمال حذف بی‌صدای گروه و تداخل کلید `LazyColumn` | اندروید |
| ۲۵ | 🟡 P2 | مرتب‌سازی الفبایی گروه‌ها بدون `Collator` فارسی | اندروید |
| ۲۶ | 🟡 P2 | فیلتر انبارها بدون `remember` (اجرا در هر Recomposition) | اندروید |
| ۲۷ | 🟡 P2 | «کشتی یافت نشد» با کد ۵۰۰ و تشخیص آن با تطبیق رشته‌ی فارسی | هر دو |
| ۲۸ | 🟡 P2 | نبود `.htaccess` روی پوشه‌ی `log/` (لاگ دسترسی شامل IP و User-Agent) | سرور |
| ۲۹ | 🟢 P3 | تکرار گسترده‌ی کد در `WarehouseQuotasTabs` و بلوک‌های خطا | اندروید |
| ۳۰ | 🟢 P3 | توابع Wrapper بی‌فایده در `QuotaWarningDialog` | اندروید |
| ۳۱ | 🟢 P3 | ضرب بی‌اثر `getWeightInKg` و lookup اضافی داخل Comparator | اندروید |
| ۳۲ | 🟢 P3 | نبود `contentDescription` و `Role` مناسب روی تب‌ها | اندروید |

---

## ۲. موارد بحرانی (P0)

### ۲.۱ 🔴 حذف کوتاژ، حواله‌های نوع‌کالای دیگر را هم پاک می‌کند

**محل:** `PHP/src/Controllers/AppApiController.php:1334-1342`
**مسیر رسیدن:** تب «کوتاژها» ← منوی کوتاژ ← دکمه «حذف»

```php
// حذف از CargoInfo — چهار شرط
$cargoQuery = "DELETE FROM CargoInfo WHERE loadingQuotaNumber = ? AND shipName = ?
               AND loadingWarehouse = ? AND shippingCompany = ?";
$cargoStmt->bind_param("ssss", $quotaNumber, $shipName, $warehouse, $shippingCompany);

// حذف از InitialInfo — پنج شرط (cargoType هم هست)
$initialQuery = "DELETE FROM InitialInfo WHERE loadingQuotaNumber = ? AND shipName = ?
                 AND loadingWarehouse = ? AND shippingCompany = ? AND cargoType = ?";
```

شرط `cargoType` در `DELETE` جدول `CargoInfo` وجود ندارد، در حالی که کل معماری این ماژول بر پایه‌ی کلید مرکب پنج‌تایی (`loadingQuotaNumber` + `shipName` + `loadingWarehouse` + `shippingCompany` + `cargoType`) بنا شده است — همان کلیدی که در `getQuotasList` برای `GROUP BY` و در `quotaKey` استفاده می‌شود.

**سناریوی خرابی:** کوتاژ `12345` برای کشتی «آرش»، انبار «۳»، شرکت «دریا» با دو نوع کالا ثبت شده: «گندم» و «جو». کاربر ردیف «گندم» را حذف می‌کند. نتیجه: ردیف «گندم» از `InitialInfo` حذف می‌شود (درست)، اما **تمام حواله‌های هر دو نوع کالا از `CargoInfo` پاک می‌شوند** (غلط). حواله‌های «جو» بدون هیچ پیام خطایی از بین می‌روند و کوتاژ «جو» ناگهان تناژ بارگیری‌شده‌ی صفر نشان می‌دهد.

این حذف داخل تراکنش است اما تراکنش موفق commit می‌شود، پس بازگشتی در کار نیست.

**اصلاح پیشنهادی:** افزودن `AND cargoType = ?` به کوئری `CargoInfo` و تغییر `bind_param` به `"sssss"`.

---

### ۲.۲ 🔴 نبود کامل کنترل مجوز روی عملیات نوشتن و حذف

**محل:** `PHP/src/Controllers/AppApiController.php:35-46` و `:188-256`

تنها گیت امنیتی این کنترلر `requireAuthenticatedSession()` است که فقط اعتبار نشست را چک می‌کند:

```php
if (!$this->sessionService->isValidToken($username, $deviceId, $token)) { /* 401 */ }
```

بعد از این نقطه، تمام actionها — از جمله `editQuota`، `deleteQuota`، `toggleQuotaStatus`، `updateQuotaPercentage`، `updateQuotaPercentageRestriction` و `updateTemporaryTonnage` — بدون هیچ بررسی نقش یا سطح دسترسی اجرا می‌شوند.

پروژه کلاس `PHP/PermissionManager.php` را دارد، ولی جست‌وجو در کل `PHP/src/` و `PHP/*.php` نشان می‌دهد **هیچ فایلی به آن ارجاع نمی‌دهد**. سمت اندروید هم هیچ گیت مجوزی روی دکمه‌های ویرایش/حذف/تغییر وضعیت این صفحه وجود ندارد.

**پیامد:** هر کاربری که بتواند لاگین کند، صرف‌نظر از نقش سازمانی‌اش، می‌تواند کوتاژ و کل تاریخچه‌ی حواله‌های آن را حذف کند. با توجه به مورد ۲.۱، دامنه‌ی این حذف حتی از آنچه کاربر انتخاب کرده هم بیشتر است.

**اصلاح پیشنهادی:** تعریف یک متد `requirePermission(string $capability)` در کنترلر، فراخوانی آن در ابتدای هر `case` نوشتنی، و مخفی/غیرفعال کردن دکمه‌های متناظر در `QuotasListScreen` بر اساس مجوز کاربر جاری.

---

### ۲.۳ 🔴 عملیات تغییر وضعیت و درصد به ردیف یکتا محدود نمی‌شوند

**محل:** `PHP/src/Controllers/AppApiController.php:1280-1310`

```php
public function updateQuotaPercentage(string $quotaNumber, float $percentage): bool {
    $query = "UPDATE InitialInfo SET percentage = ?, is_enabled = ? WHERE loadingQuotaNumber = ?";
    // ...
}

public function updateQuotaPercentageRestriction(string $quotaNumber, int $isEnabled): bool {
    $query = "UPDATE InitialInfo SET is_enabled = ? WHERE loadingQuotaNumber = ?";
    // ...
}

public function toggleQuotaStatus(string $quotaNumber, int $id = 0): bool {
    if ($id > 0) {
        $query = "UPDATE InitialInfo SET isActive = NOT isActive WHERE id = ?";
    } else {
        $query = "UPDATE InitialInfo SET isActive = NOT isActive WHERE loadingQuotaNumber = ?"; // ← همه ردیف‌ها
    }
    // ...
}
```

`loadingQuotaNumber` در `InitialInfo` کلید یکتا نیست — دقیقاً به همین دلیل `getQuotasList` بر اساس کلید مرکب پنج‌تایی `GROUP BY` می‌کند. بنابراین:

- **`updateQuotaPercentage` و `updateQuotaPercentageRestriction`** (دیالوگ «درصد» و دکمه‌ی «محدود کردن درصد» در دیالوگ هشدار) هیچ مسیر مبتنی بر `id` ندارند و **همیشه** روی همه‌ی ردیف‌های هم‌شماره اعمال می‌شوند — حتی ردیف‌های متعلق به کشتی دیگر.
- **`toggleQuotaStatus`** اگر `id` معتبر بگیرد درست کار می‌کند، اما هر دو فراخوان‌کننده‌ی سمت کلاینت از الگوی `quota.id ?: 0` استفاده می‌کنند (`QuotasListScreen.kt:932` و `QuotaWarningDialog.kt:226, 651`). اگر `id` به هر دلیلی خالی باشد، درخواست با `id=0` می‌رود و به شاخه‌ی ناامن می‌افتد.

**اصلاح پیشنهادی:** هر سه تابع باید کلید مرکب کامل (یا `id`) را الزامی کنند و در نبود آن خطا برگردانند، نه اینکه به `WHERE loadingQuotaNumber` تنزل پیدا کنند.

---

### ۲.۴ 🔴 کرش `NullPointerException` در محاسبه‌ی هشدار

**محل:** `ShipDetailsScreen.kt:696`

```kotlin
if (!quota.isPercentageRestricted!! || quota.percentage == null) {
    return null
}
```

فیلد در مدل به‌صورت nullable تعریف شده (`ReportModels.kt:98`):

```kotlin
val isPercentageRestricted: Boolean? = false
```

`!!` روی این فیلد یعنی هر پاسخی که این کلید را نداشته باشد یا `null` بفرستد، اپ را در همان لحظه‌ی باز شدن صفحه کرش می‌دهد. `calculateWarningStatus` داخل `remember(selectedShipQuotas)` اجرا می‌شود، پس کرش در فاز Composition رخ می‌دهد و کل صفحه از بین می‌رود.

امروز `getQuotasList` این فیلد را همیشه می‌فرستد، اما این یک تضمین شکننده است: مقدار پیش‌فرض `false` در مدل صراحتاً می‌گوید «ممکن است نیاید». همچنین اگر در آینده `_selectedShipQuotas` از مسیر دیگری (مثلاً `getAllQuotasList` یا کش) پر شود، کرش قطعی است.

**اصلاح پیشنهادی:** جایگزینی با `if (quota.isPercentageRestricted != true || quota.percentage == null)`.

**نکته‌ی مرتبط:** در همان تابع، خط ۶۸۱ شرط `quota.percentage != null && quota.percentage == 0.0` را دارد که بخش اول آن زائد است (`== 0.0` روی `null` خودش `false` است).

---

### ۲.۵ 🔴 انجام تمام عملیات نوشتن و حذف با متد `GET`

**محل:** `api/ApiService.kt:160-206` و `AppApiController.php:50-52`

```kotlin
@GET("protected_proxy.php")
suspend fun deleteQuota(
    @Query("action") action: String = "deleteQuota",
    @Query("quotaNumber") quotaNumber: String,
    // ...
): Response<SuccessResponse>
```

و سمت سرور، `handle()` صراحتاً هر متدی به‌جز GET را رد می‌کند:

```php
if (!$this->request->isGet()) {
    throw new Exception('روش درخواست نامعتبر است');
}
```

مشکلات این طراحی:

- **قابلیت بازپخش (Replay):** یک URL حذف کوتاژ، کامل و خودبسنده است؛ هر کسی که آن را در تاریخچه، لاگ یا اشتراک‌گذاری ببیند می‌تواند دوباره اجرایش کند.
- **کش‌شدن ناخواسته:** OkHttp با `Cache` فعال (`RetrofitClient.kt:121`) و هر پروکسی میانی مجازند پاسخ‌های `GET` را کش کنند. نبود `Cache-Control: no-store` روی این پاسخ‌ها یعنی یک عملیات حذف ممکن است پاسخ کش‌شده بگیرد و کلاینت «موفق» گزارش کند بدون اینکه چیزی حذف شده باشد.
- **نشت پارامترها در لاگ:** شماره کوتاژ، نام کشتی و شرکت در Query String قرار می‌گیرند و در access log وب‌سرور، لاگ پروکسی معکوس و هر واسط دیگری ثبت می‌شوند. (لاگ خود `protected_proxy.php` خوشبختانه Query String را ثبت نمی‌کند، اما لاگ Apache/Nginx می‌کند.)
- **نقض قرارداد HTTP:** ابزارهای خودکار (Crawler، Prefetch مرورگر، اسکنر امنیتی) مجازند `GET` را بدون عارضه صدا بزنند.

**اصلاح پیشنهادی:** تبدیل هر شش action نوشتنی به `POST` با بدنه‌ی JSON، و افزودن `header('Cache-Control: no-store')` روی پاسخ آن‌ها.

---

## ۳. موارد با اولویت بالا (P1)

### ۳.۱ 🟠 `htmlspecialchars` روی مقادیر شرط `WHERE`

**محل:** `AppApiController.php:1096` (`getQuotasList`)، `:950-952` (`getFilteredQuotas`)، `:635-636` (`getWarehouseDetails`)، `:1314-1318` (`deleteQuota`)، `:1372` (`updateTemporaryTonnage`)

```php
private function sanitizeInput(string $input): string {
    return htmlspecialchars(strip_tags(trim($input)), ENT_QUOTES, 'UTF-8');
}
```

در گزارش قبلی همین مسئله برای `getShipDetails` شناسایی و رفع شد و متد `validateIdentifier` جایگزین آن شد، با این توضیح صریح در خود کد (خط ۳۱۳-۳۱۸):

> برای مقادیری که در یک شرط تساوی (`WHERE = ?`) با prepared statement مقایسه می‌شوند نباید `htmlspecialchars` اعمال شود.

اما این اصلاح فقط روی `getShipDetails` اعمال شده و پنج تابع دیگری که همین صفحه به آن‌ها وابسته است هنوز از `sanitizeInput` استفاده می‌کنند.

**سناریوی خرابی:** کشتی‌ای با نام `M&V TRADER`:
- `getShipDetails` (با `validateIdentifier`) → موفق، هدر کشتی با تناژ و تعداد انبار درست نمایش داده می‌شود.
- `getQuotasList` (با `sanitizeInput`) → نام به `M&amp;V TRADER` تبدیل می‌شود، `WHERE i.shipName = ?` هیچ ردیفی برنمی‌گرداند.

نتیجه: صفحه با موفقیت باز می‌شود، هدر می‌گوید «۳ انبار · ۴۷ کوتاژ»، اما تب کوتاژها **کاملاً خالی** است و هیچ پیام خطایی هم نمایش داده نمی‌شود. تشخیص این باگ برای کاربر عملاً غیرممکن است.

در `deleteQuota` اثر بدتر است: چون `sanitizeInput` روی هر پنج مقدار اعمال می‌شود، شرط `COUNT(*)` صفر برمی‌گردد و تابع `false` می‌دهد — کاربر پیام «خطا در حذف کوتاژ» می‌بیند بدون هیچ سرنخی از علت.

**اصلاح پیشنهادی:** جایگزینی `sanitizeInput` با `validateIdentifier` در هر پنج تابع بالا. (SQL Injection توسط prepared statement مهار شده است، نه توسط escape کردن.)

---

### ۳.۲ 🟠 دکمه‌ی «غیرفعال کردن» گروهی در دیالوگ هشدار

**محل:** `QuotaWarningDialog.kt:222-245`

```kotlin
Button(
    onClick = {
        coroutineScope.launch {
            warnings.forEach { warning ->
                viewModel.toggleQuotaStatus(warning.quotaId ?: 0, warning.quotaNumber)
            }
            onDismiss()
        }
    },
    // ...
) { /* ... Text("غیرفعال کردن") */ }
```

سه اشکال جدا در یک دکمه:

**الف) بدون دیالوگ تأیید.** حذف تک‌کوتاژ و تغییر وضعیت تک‌کوتاژ هر دو دیالوگ تأیید دارند (`QuotasListScreen.kt:907, 926`)، ولی این دکمه که *همه‌ی* کوتاژهای هشداردار را یکجا تغییر می‌دهد هیچ تأییدی نمی‌خواهد. ضمن اینکه دیالوگ هشدار **خودکار و بدون درخواست کاربر** باز می‌شود؛ یعنی کاربر ممکن است بدون اینکه بداند چه دیالوگی جلویش است دکمه را بزند.

**ب) Toggle به‌جای Deactivate.** برچسب دکمه «غیرفعال کردن» است، اما `toggleQuotaStatus` وضعیت را معکوس می‌کند. `calculateWarningStatus` در ابتدای خودش کوتاژهای غیرفعال را حذف می‌کند (خط ۶۷۵)، پس *در حالت عادی* همه‌ی موارد فعال‌اند. ولی اگر لیست کوتاژها بین لحظه‌ی محاسبه‌ی هشدار و لحظه‌ی کلیک تازه‌سازی شده باشد (که با توجه به مورد ۳.۵ اتفاق می‌افتد)، این دکمه یک کوتاژ غیرفعال را **فعال** می‌کند.

**ج) انفجار درخواست و برخورد با Rate Limit.** هر فراخوانی `toggleQuotaStatus` در ViewModel زنجیره‌ی زیر را راه می‌اندازد (`ReportsViewModel.kt:567-586`):

```kotlin
val success = repository.toggleQuotaStatus(id, quotaNumber)   // ۱ درخواست
if (success) {
    _currentShipName.value?.let { refreshShipDataSilently(it) }  // ۲ درخواست (details + quotas)
    loadShips()                                                   // ۱ درخواست
}
```

یعنی **۴ درخواست HTTP به ازای هر هشدار**. سقف Rate Limit پروکسی ۶۰ درخواست در ۶۰ ثانیه است (`protected_proxy.php:20-21`). با ۱۵ کوتاژ هشداردار — که برای کشتی بزرگ کاملاً محتمل است — دقیقاً به سقف می‌رسیم و بقیه‌ی درخواست‌ها `429` می‌گیرند. نتیجه: بخشی از کوتاژها غیرفعال می‌شوند، بخشی نه، و کاربر هیچ گزارشی از اینکه کدام‌ها ناموفق بوده‌اند نمی‌گیرد (چون `toggleQuotaStatus` فقط Snackbar می‌زند و آخرین Snackbar بقیه را می‌پوشاند).

علاوه بر این، هیچ اتمیسیتی وجود ندارد: عملیات نیمه‌تمام رها می‌شود.

**اصلاح پیشنهادی:** افزودن دیالوگ تأیید با ذکر تعداد؛ ساخت یک endpoint دسته‌ای (`deactivateQuotas` با آرایه‌ی id) که در یک تراکنش اجرا شود و یک بار refresh کند؛ یا در نبود آن، حداقل جمع کردن refresh‌ها به یک بار در انتهای حلقه.

---

### ۳.۳ 🟠 اسپینر بارگذاری اولیه هرگز نمایش داده نمی‌شود

**محل:** `ShipDetailsScreen.kt:176-197` و `ReportsViewModel.kt:349-353, 418-449`

صفحه یک شاخه‌ی کامل برای نمایش وضعیت بارگذاری دارد:

```kotlin
isLoadingShipDetails || isLoadingShipQuotas -> {
    // CircularProgressIndicator + "در حال بارگذاری اطلاعات کشتی..."
}
```

اما:
- `_isLoadingShipDetails` در **کل ViewModel هیچ‌جا مقداردهی نمی‌شود** — فقط در خط ۳۴۹ تعریف و در ۳۵۰ expose شده. مقدارش همیشه `false` است.
- `_isLoadingShipQuotas` فقط در `loadShipQuotas()` ست می‌شود، ولی مسیر ورود به این صفحه `loadShipDataAsync()` است که آن را ست نمی‌کند.

در نتیجه هنگام ورود به صفحه، `when` از هر چهار شرط اول رد می‌شود و به شاخه‌ی `else` می‌رسد:

```kotlin
Text(text = "اطلاعات کشتی در حال بارگذاری می باشند")
Text(text = "لطفاً صبر کنید...")
```

یعنی کاربر یک کادر متنی ساکن بدون هیچ نشانگر پیشرفتی می‌بیند. علاوه بر تجربه‌ی کاربری ضعیف، این یعنی سه شاخه از پنج شاخه‌ی `when` (شاخه‌ی اسپینر، و متن‌های شرطی داخلش) کد مرده هستند و هر سه متن «در حال بارگذاری جزئیات کشتی...» / «در حال بارگذاری سهمیه‌های کشتی...» غیرقابل دسترس‌اند.

**اصلاح پیشنهادی:** ست کردن هر دو فلگ در `loadShipDataAsync` (در `try`/`finally`)، یا حذف کامل این دو فلگ و اتکا به `uiState is UiState.Loading` که واقعاً مقداردهی می‌شود.

---

### ۳.۴ 🟠 ناسازگاری مدل `Warehouse` با پاسخ `getShipDetails`

**محل:** `data/model/ReportModels.kt:120-128` در برابر `AppApiController.php:593-603`

مدل کاتلین:

```kotlin
data class Warehouse(
    val name: String,
    val quotaCount: Int,
    val totalTonnage: Float,
    val loadedTonnage: Float,
    val remainingTonnage: Float,
    val quotas: List<Quota>,              // non-null، بدون مقدار پیش‌فرض
    val availableExitDates: List<String>  // non-null، بدون مقدار پیش‌فرض
)
```

پاسخ سرور در `getShipDetails` هیچ‌کدام از دو فیلد آخر را نمی‌فرستد:

```php
$warehouses[] = [
    'name' => ..., 'quotaCount' => ..., 'uniqueQuotaCombinations' => ...,
    'shippingCompanyCount' => ..., 'cargoTypeCount' => ...,
    'totalTonnage' => ..., 'remainingTonnage' => ..., 'loadedTonnage' => ...,
    'percentageLoaded' => ...
];
```

Gson با reflection شیء را می‌سازد و قید non-null کاتلین را دور می‌زند، پس `warehouse.quotas` و `warehouse.availableExitDates` در واقعیت `null` هستند در حالی که تایپ‌سیستم می‌گوید نیستند.

امروز `WarehousesSection` هیچ‌کدام را نمی‌خواند، پس کرش نداریم. اما این دقیقاً همان الگویی است که در گزارش قبلی برای `Ship.warehouses` شناسایی شد و با افزودن مقدار پیش‌فرض رفع شد (کامنت توضیحی‌اش در `ReportModels.kt:75-77` هنوز موجود است). هر کد آینده‌ای که `warehouse.quotas.size` بنویسد، بدون هیچ هشدار کامپایلری کرش می‌کند.

**اصلاح پیشنهادی:** افزودن `= emptyList()` به هر دو فیلد، دقیقاً مطابق الگوی به‌کاررفته در `Ship`.

---

### ۳.۵ 🟠 محاسبه‌ی نادرست `isActive` کشتی

**محل:** `AppApiController.php:587-610`

کوئری به‌درستی `MAX(i.isActive) as isActive` را با `GROUP BY i.shipName, i.loadingWarehouse` می‌گیرد، اما حلقه‌ی PHP مقدار را در هر تکرار **بازنویسی** می‌کند:

```php
while ($row = $result->fetch_assoc()) {
    // ...
    $totalVoucherCount = intval($row['totalVoucherCount']);
    $isActive = (bool)$row['isActive'];   // ← فقط آخرین انبار برنده می‌شود
}
```

`MAX()` فقط داخل هر گروه (هر انبار) اعمال می‌شود، نه بین گروه‌ها. بنابراین اگر کشتی سه انبار داشته باشد و انبار آخر (به ترتیب خروجی کوئری، که `ORDER BY` هم ندارد پس ترتیبش تضمین‌شده نیست) همه‌ی کوتاژهایش غیرفعال باشند، کل کشتی `isActive: false` گزارش می‌شود در حالی که دو انبار دیگر فعال‌اند.

**اصلاح پیشنهادی:** `$isActive = $isActive || (bool)$row['isActive'];`

**نکته‌ی مرتبط در همان حلقه:** `$totalVoucherCount` هم بازنویسی می‌شود، اما چون از یک زیرکوئری همبسته‌ی سطح‌کشتی می‌آید مقدارش در همه‌ی ردیف‌ها یکسان است — نتیجه درست ولی محاسبه تکراری (مورد ۴.۳).

---

### ۳.۶ 🟠 کیبورد عددی روی جستجویی که متن هم فیلتر می‌کند

**محل:** `ShipDetailsScreen.kt:355-360` در برابر `QuotasListScreen.kt:331-336`

```kotlin
SearchField(
    searchQuery = searchQuery,
    onSearchQueryChange = { searchQuery = it },
    keyboardType = if (selectedSection == 0) KeyboardType.Number else KeyboardType.Text
)
```

اما فیلتر واقعی در تب کوتاژها روی سه فیلد کار می‌کند که دوتایشان متنی‌اند:

```kotlin
.filter { quota ->
    quota.number.contains(searchQuery, ignoreCase = true) ||
    quota.shippingCompany.contains(searchQuery, ignoreCase = true) ||
    (quota.cargoOwner?.contains(searchQuery, ignoreCase = true) == true)
}
```

کاربر نمی‌تواند نام شرکت حمل یا صاحب کالا را تایپ کند چون کیبورد عددی باز می‌شود. علاوه بر این، متن `placeholder` مقدار پیش‌فرض `SearchField` را می‌گیرد: **«جستجو بر اساس نام کشتی یا نوع محموله...»** — که هیچ ربطی به رفتار واقعی این صفحه ندارد (نه نام کشتی فیلتر می‌شود نه نوع محموله).

همین مورد برای تب انبارها هم صدق می‌کند: `KeyboardType.Text` درست است ولی placeholder باز هم گمراه‌کننده است (فیلتر روی `warehouse.name` است).

**اصلاح پیشنهادی:** `KeyboardType.Text` برای هر دو تب، و ارسال `placeholder` صریح و متناسب با هر تب.

---

### ۳.۷ 🟠 باز شدن دوباره‌ی دیالوگ هشدار بعد از هر تغییر

**محل:** `ShipDetailsScreen.kt:99, 106-114, 320-326`

```kotlin
var showWarningDialog by remember { mutableStateOf(false) }
val warnings = remember(selectedShipQuotas) {
    selectedShipQuotas.mapNotNull { quota -> calculateWarningStatus(quota) }
}

LaunchedEffect(warnings) {
    if (warnings.isNotEmpty()) {
        showWarningDialog = true
    }
}
```

`LaunchedEffect` روی برابری محتوایی `warnings` کلید خورده است. هر عملیاتی که لیست کوتاژها را تغییر دهد — غیرفعال کردن یک کوتاژ، تغییر درصد، ویرایش، حذف — باعث `refreshShipDataSilently` می‌شود، محتوای `warnings` عوض می‌شود، و دیالوگ **دوباره خودکار باز می‌شود**.

سناریوی عملی: کاربر دیالوگ را می‌بندد ← از تب کوتاژها یک کوتاژ را غیرفعال می‌کند ← داده تازه می‌شود ← دیالوگ هشدار دوباره جلوی صورتش باز می‌شود با بقیه‌ی هشدارها. برای غیرفعال کردن پنج کوتاژ باید پنج بار دیالوگ را ببندد.

مشکل دوم: `showWarningDialog` با `remember` نگه داشته می‌شود نه `rememberSaveable`، پس با چرخش صفحه یا بازگشت از Process Death دوباره باز می‌شود.

**اصلاح پیشنهادی:** نگهداری مجموعه‌ی `quotaNumber`های «دیده‌شده» در `rememberSaveable` و نمایش دیالوگ فقط وقتی هشدار *جدیدی* که قبلاً دیده نشده وجود دارد.

---

### ۳.۸ 🟠 لاگ‌گیری از شماره کوتاژها در هر Recomposition

**محل:** `QuotasListScreen.kt:387-393`

```kotlin
val groupedQuotas = remember(quotas, currentGroupingMode, /* ... */) {
    // ... محاسبات گروه‌بندی
}.also { result ->
    result.forEach { (groupName, items) ->
        Log.d("atkcargo", "  Group '$groupName': ${items.size} items - ${items.joinToString(", ") { it.number }}")
    }
    shareGroupedQuotas = result
}
```

سه اشکال در یک بلوک:

**الف) نشت داده‌ی تجاری در Logcat.** `Log.d` در بیلد Release هم اجرا می‌شود مگر اینکه با ProGuard صراحتاً حذف شود. این خط **تمام شماره کوتاژهای کشتی** را به همراه نام گروه (شرکت حمل یا صاحب کالا) در لاگ سیستم می‌نویسد. هر اپ دیگری با مجوز `READ_LOGS` یا هر گزارش باگ (`bugreport`) این داده را برمی‌دارد.

**ب) اجرا در هر Recomposition.** `.also` روی *نتیجه‌ی* `remember` اعمال می‌شود، نه داخل بلوک `remember`. یعنی برخلاف محاسبات گروه‌بندی که کش می‌شوند، این بلوک در **هر بار Recomposition** اجرا می‌شود: پیمایش کامل همه‌ی گروه‌ها، `joinToString` روی همه‌ی کوتاژها، و ساخت رشته‌ی جدید. با اسکرول لیست، این کار ده‌ها بار در ثانیه تکرار می‌شود.

**ج) نوشتن در State حین Composition.** `shareGroupedQuotas = result` یک نوشتن به `MutableState` در بدنه‌ی Composable بدون `SideEffect` است. اینجا اتفاقاً کرش نمی‌دهد چون این State فقط داخل یک lambda کلیک خوانده می‌شود، اما الگوی ناامنی است که با یک تغییر کوچک به حلقه‌ی بی‌نهایت Recomposition تبدیل می‌شود.

**اصلاح پیشنهادی:** حذف کامل `Log.d`؛ انتقال `shareGroupedQuotas = result` به داخل بلوک `remember` یا تبدیل آن به `derivedStateOf`؛ یا ساده‌تر: حذف State و محاسبه‌ی مستقیم متن اشتراک‌گذاری از `groupedQuotas` در همان lambda کلیک.

---

## ۴. موارد با اولویت متوسط (P2)

### ۴.۱ 🟡 نبود کش روی دو endpoint سنگین صفحه

**محل:** `AppApiController.php:82-83, 114-115` در برابر `:64-75`

`getShipsList` بعد از گزارش قبلی مجهز به `MicroCache` (۸ ثانیه) و `ETag` + `Cache-Control` شد:

```php
$this->sendCacheableJsonResponse($ships, $etagSource, 8);
```

اما `getShipDetails` و `getQuotasList` — دو کوئری‌ای که این صفحه به آن‌ها وابسته است و هر دو شامل Derived Table روی `CargoInfo` هستند — از `sendJsonResponse` ساده استفاده می‌کنند: بدون `MicroCache`، بدون `ETag`، بدون `Cache-Control`.

پیامد عملی: کاربر وارد جزئیات کشتی می‌شود (۲ کوئری سنگین) ← روی یک انبار می‌زند ← برمی‌گردد ← `LaunchedEffect(initialShipName)` دوباره اجرا می‌شود، `clearCurrentShipData()` داده را پاک می‌کند و هر دو کوئری از نو اجرا می‌شوند. برای کاربری که بین چند انبار جابه‌جا می‌شود، این یعنی ده‌ها بار اجرای همان دو کوئری در چند دقیقه.

**اصلاح پیشنهادی:** پیچیدن هر دو در `MicroCache::remember` با کلید شامل نام کشتی و TTL کوتاه (۵-۸ ثانیه)، و ارسال `ETag` مانند `getShipsList`. کلیدها باید در `editQuota`/`deleteQuota`/`toggleQuotaStatus` هم `forget` شوند.

---

### ۴.۲ 🟡 Derived Table کاملاً تکراری

**محل:** `AppApiController.php:1101-1116` و `:966-984`

در `getQuotasList` دو Derived Table تعریف شده‌اند:

```sql
LEFT JOIN (
    SELECT ..., SUM(c.netWeight) as loadedTonnage,
           COUNT(DISTINCT c.trackingNumber) as exitVoucherCount, MAX(c.exitDate) as lastExitDate
    FROM CargoInfo c WHERE c.status = 'خروج' AND c.shipName = ?
    GROUP BY c.loadingQuotaNumber, c.shipName, c.loadingWarehouse, c.shippingCompany, c.cargoType
) exit_data ON ...

LEFT JOIN (
    SELECT ..., COUNT(DISTINCT c.trackingNumber) as voucherCount
    FROM CargoInfo c WHERE c.status = 'خروج' AND c.shipName = ?
    GROUP BY c.loadingQuotaNumber, c.shipName, c.loadingWarehouse, c.shippingCompany, c.cargoType
) all_vouchers ON ...
```

شرط `WHERE`، `GROUP BY` و شرط `ON` هر دو **کاملاً یکسان** است، و `all_vouchers.voucherCount` دقیقاً همان `exit_data.exitVoucherCount` است. یعنی MySQL همان اسکن و همان تجمیع را دو بار روی `CargoInfo` انجام می‌دهد — تقریباً دو برابر شدن هزینه‌ی سنگین‌ترین بخش کوئری. عیناً همین تکرار در `getFilteredQuotas` هم وجود دارد (با شش شرط تاریخ اضافی در هر دو).

نام‌گذاری `all_vouchers` نشان می‌دهد نیت اولیه شمردن *همه‌ی* حواله‌ها (ورودی + خروجی) بوده، ولی شرط `status = 'خروج'` روی آن هم اعمال شده است.

**اصلاح پیشنهادی:** حذف کامل `all_vouchers` و استفاده از `exit_data.exitVoucherCount` برای هر دو فیلد — یا اگر واقعاً شمارش کل حواله‌ها مد نظر است، حذف شرط `status` از این یکی.

---

### ۴.۳ 🟡 زیرکوئری همبسته‌ی `totalVoucherCount` به ازای هر انبار

**محل:** `AppApiController.php:561`

```sql
(SELECT COUNT(DISTINCT c.trackingNumber) FROM CargoInfo c
 WHERE c.shipName = i.shipName AND c.status = 'خروج') as totalVoucherCount
```

این زیرکوئری به `i.shipName` وابسته است، پس به ازای **هر گروه انبار** یک بار اجرا می‌شود — در حالی که نتیجه‌اش برای همه‌ی گروه‌ها یکسان است (به همین دلیل حلقه‌ی PHP هم بدون مشکل بازنویسی‌اش می‌کند). کشتی با ۵ انبار = ۵ بار `COUNT(DISTINCT)` روی `CargoInfo`.

ضمناً این فیلد در مدل `Ship` مقدار پیش‌فرض `0` دارد و در این صفحه اصلاً استفاده نمی‌شود.

**اصلاح پیشنهادی:** خارج کردن آن به یک کوئری مستقل، یا حذف کامل اگر مصرف‌کننده‌ای ندارد.

---

### ۴.۴ 🟡 ماشین‌آلات مرده‌ی صفحه‌بندی

**محل:** `ShipDetailsScreen.kt:104-105, 127-133, 239-269`

```kotlin
var selectedTabIndex by remember { mutableIntStateOf(0) }
val pagerState = rememberPagerState(pageCount = { 1 })   // ← فقط یک صفحه

LaunchedEffect(pagerState.currentPage) { selectedTabIndex = pagerState.currentPage }
LaunchedEffect(selectedTabIndex) { pagerState.animateScrollToPage(selectedTabIndex) }

HorizontalPager(state = pagerState, userScrollEnabled = false, /* ... */) { page ->
    AnimatedContent(
        targetState = page,
        transitionSpec = {
            if (targetState > initialState) { slideInHorizontally { it } + fadeIn() togetherWith ... }
            else { slideInHorizontally { -it } + fadeIn() togetherWith ... }
        },
        label = "Page transition"
    ) { targetPage ->
        when (targetPage) { 0 -> ship?.let { /* ... */ } }
    }
}
```

یک `HorizontalPager` با **یک صفحه** و `userScrollEnabled = false`، به‌علاوه‌ی یک `AnimatedContent` با `transitionSpec` کاملی که هیچ‌وقت اجرا نمی‌شود (چون `targetState` همیشه `0` است)، و دو `LaunchedEffect` که یک متغیر همیشه‌صفر را با یک صفحه‌ی همیشه‌صفر همگام می‌کنند.

`selectedTabIndex` و `pagerState` هیچ مصرف دیگری ندارند. تب‌بندی واقعی صفحه با `selectedSection` داخل `WarehousesAndQuotasTab` انجام می‌شود که ربطی به این‌ها ندارد.

هزینه: یک لایه‌ی `Pager` + `AnimatedContent` اضافی در درخت Composition، دو Coroutine، و ~۳۵ خط کدی که خواننده باید بفهمد قبل از اینکه بفهمد بی‌اثر است.

**اصلاح پیشنهادی:** حذف کامل `pagerState`، `selectedTabIndex`، هر دو `LaunchedEffect`، `HorizontalPager` و `AnimatedContent`؛ جایگزینی با فراخوانی مستقیم `WarehousesAndQuotasTab`.

---

### ۴.۵ 🟡 ارسال فیلدهای بی‌استفاده در پاسخ

**محل:** `AppApiController.php:1138-1161` (کوتاژها) و `:593-631` (کشتی)

فیلدهایی که سرور می‌فرستد و مدل‌های کاتلین اصلاً ندارند (Gson آن‌ها را دور می‌ریزد):

در هر آیتم کوتاژ: `percentageLoaded`، `entryVoucherCount`، `exitVoucherCount`، `pendingVoucherCount`، `loadableTonnage`، `avgVoucherWeight`، `lastExitDate`، `quotaKey`

در پاسخ کشتی: `percentageLoaded`، `lastUpdated`، و در هر انبار: `uniqueQuotaCombinations`، `shippingCompanyCount`، `cargoTypeCount`

نکته‌ی قابل توجه: `entryVoucherCount` و `pendingVoucherCount` **همیشه به‌صورت ثابت `0`** ارسال می‌شوند (خطوط ۱۱۴۹ و ۱۱۵۱) — یعنی حتی اگر مصرف‌کننده‌ای داشتند، بی‌معنا بودند.

برای کشتی با ۴۰۰ کوتاژ، `quotaKey` به‌تنهایی (رشته‌ی الحاقی پنج‌بخشی) حجم قابل توجهی به پاسخ اضافه می‌کند بدون هیچ مصرفی.

**اصلاح پیشنهادی:** حذف فیلدهای بی‌مصرف — دقیقاً همان کاری که در commit اخیر برای `getShipsList` انجام شد (`7b4e389`).

---

### ۴.۶ 🟡 `remember` به‌جای `rememberSaveable`

**محل:** `ShipDetailsScreen.kt:99, 337, 338`، `QuotasListScreen.kt:173, 186`

```kotlin
var showWarningDialog by remember { mutableStateOf(false) }        // ShipDetails
var selectedSection by remember { mutableIntStateOf(0) }          // WarehousesAndQuotasTab
var searchQuery by remember { mutableStateOf("") }                // WarehousesAndQuotasTab
var expandedGroup by remember { mutableStateOf<String?>(null) }   // QuotasList
```

با چرخش صفحه یا بازگشت از Process Death: تب انتخابی به «کوتاژها» برمی‌گردد، متن جستجو پاک می‌شود، و گروه بازشده بسته می‌شود. همین مورد در گزارش قبلی برای `ShipsListScreen` شناسایی و رفع شد ولی اینجا اعمال نشده است.

**اصلاح پیشنهادی:** جایگزینی با `rememberSaveable` برای هر چهار مورد.

---

### ۴.۷ 🟡 محاسبه‌ی دوباره‌ی `loadedTonnage` در کلاینت

**محل:** `ShipDetailsScreen.kt:462`

```kotlin
val loadedTonnage = shipDetails.totalTonnage - shipDetails.remainingTonnage
```

مدل `Ship` فیلد `loadedTonnage` را دارد و سرور آن را از قبل محاسبه‌شده می‌فرستد (`AppApiController.php:625`). کامنت صریح در `ReportModels.kt:71-74` می‌گوید:

> هر دو `getShipsList` و `getShipDetails` این فیلد را از قبل محاسبه‌شده ارسال می‌کنند؛ کلاینت دیگر نباید `totalTonnage - remainingTonnage` را دوباره حساب کند (منطق تکراری بین سرور و کلاینت)

این صفحه دقیقاً همان کاری را می‌کند که کامنت منع کرده است.

**اصلاح پیشنهادی:** `val loadedTonnage = shipDetails.loadedTonnage`

---

### ۴.۸ 🟡 `Float` برای کیلوگرم و خطای دقت در نمایش

**محل:** `ShipDetailsScreen.kt:550, 557`

```kotlin
Text(text = "مانده: ${formatNumber(shipDetails.remainingTonnage.toInt())}")
Text(text = "کل: ${formatNumber(shipDetails.totalTonnage.toInt())}")
```

`Float` فقط ۲۴ بیت مانتیس دارد؛ اعداد صحیح بالای ۱۶٬۷۷۷٬۲۱۶ دیگر دقیق نمایش داده نمی‌شوند. تناژ یک کشتی فله‌بر متوسط ۲۵٬۰۰۰ تن = **۲۵٬۰۰۰٬۰۰۰ کیلوگرم** است — یعنی درست بالای این آستانه. عدد نمایش‌داده‌شده می‌تواند تا چند کیلوگرم با مقدار واقعی دیتابیس اختلاف داشته باشد، و این اختلاف در جمع‌های بعدی انباشته می‌شود.

مشکل دوم: این دو مقدار **بدون واحد** نمایش داده می‌شوند («مانده: ۲۵,۰۰۰,۰۰۰») در حالی که تابع `formatWeightWithDetail` در همین پروژه وجود دارد و در `QuotaWarningDialog` استفاده می‌شود («۲۵ هزار تن»). کاربر در دو صفحه‌ی مجاور دو فرمت متفاوت می‌بیند.

**اصلاح پیشنهادی:** استفاده از `formatWeightWithDetail` برای یکدستی؛ و در بلندمدت، تغییر تایپ تناژ از `Float` به `Long` (کیلوگرم صحیح) یا `Double` در سراسر مدل‌ها.

---

### ۴.۹ 🟡 آستانه‌های جادویی در کد UI

**محل:** `ShipDetailsScreen.kt:681, 702`

```kotlin
if (quota.percentage != null && quota.percentage == 0.0 && remainingTonnage < 5000f) { /* ... */ }
// ...
val warningThreshold = 9000f
```

دو قاعده‌ی تجاری مهم (آستانه‌ی هشدار موجودی کم، و آستانه‌ی نزدیکی به سقف درصد) به‌صورت عدد خام داخل یک فایل UI نوشته شده‌اند. برای تغییرشان باید اپ دوباره بیلد و منتشر شود.

**اصلاح پیشنهادی:** انتقال به `Constants.kt` یا بهتر از آن، دریافت از سرور در پاسخ پیکربندی.

---

### ۴.۱۰ 🟡 قدرمطلق در آستانه‌ی هشدار، کوتاژهای عبورکرده را از قلم می‌اندازد

**محل:** `ShipDetailsScreen.kt:704-705`

```kotlin
val diff = abs(remainingTonnage - percentageAmount)
if (diff <= warningThreshold) { /* هشدار بده */ }
```

استفاده از `abs` یعنی هشدار فقط در بازه‌ی `percentageAmount ± 9000` صادر می‌شود. کوتاژی که مانده‌اش **بیش از ۹۰۰۰ کیلوگرم زیر** سقف درصد رفته — یعنی محدودیت درصد را قاطعانه نقض کرده و بحرانی‌ترین حالت ممکن است — هیچ هشداری نمی‌گیرد و کاملاً از دیالوگ حذف می‌شود.

نمودار وضعیت:

| مانده نسبت به سقف درصد | `diff` | هشدار؟ | آیا درست است؟ |
|---|---|---|---|
| ۲۰٬۰۰۰ کیلو بالاتر | ۲۰٬۰۰۰ | ❌ | ✅ درست — هنوز فاصله دارد |
| ۵٬۰۰۰ کیلو بالاتر | ۵٬۰۰۰ | ✅ | ✅ درست — نزدیک شده |
| دقیقاً روی سقف | ۰ | ✅ | ✅ درست |
| ۵٬۰۰۰ کیلو پایین‌تر | ۵٬۰۰۰ | ✅ | ✅ درست — نقض شده |
| ۲۰٬۰۰۰ کیلو پایین‌تر | ۲۰٬۰۰۰ | ❌ | 🔴 **غلط — شدیداً نقض شده ولی هشدار نمی‌دهد** |

**اصلاح پیشنهادی:** جایگزینی با شرط جهت‌دار: `if (remainingTonnage - percentageAmount <= warningThreshold)`.

---

### ۴.۱۱ 🟡 گروه‌بندی با کلید nullable

**محل:** `QuotasListScreen.kt:336-343, 396-403`

```kotlin
.groupBy {
    when (currentGroupingMode) {
        WarehouseQuotaGroupingMode.BY_SHIPPING_COMPANY -> it.shippingCompany
        WarehouseQuotaGroupingMode.BY_CARGO_OWNER -> it.cargoOwner        // ← String?
        WarehouseQuotaGroupingMode.BY_WAREHOUSE -> "${it.warehouse} | ${it.cargoOwner}"
    }
}
// ...
items(
    items = groupedQuotas.entries.toList(),
    key = { (groupName, _) -> groupName ?: "" }     // ← null و "" به یک کلید نگاشت می‌شوند
) { (groupName, sortedQuotas) ->
    groupName?.let { /* فقط اینجا رندر می‌شود */ }
}
```

دو ریسک:
- اگر `cargoOwner` برای کوتاژی `null` باشد، گروهش ساخته می‌شود ولی `groupName?.let` از رندر کردنش صرف‌نظر می‌کند — کوتاژها **بی‌صدا از لیست حذف می‌شوند** بدون هیچ نشانه‌ای.
- اگر هم‌زمان یک گروه با کلید `null` و یک گروه با کلید `""` وجود داشته باشد، هر دو کلید `""` می‌گیرند و `LazyColumn` با کلید تکراری **کرش می‌کند** — همان کرشی که در گزارش قبلی برای لیست کشتی‌ها رخ داده بود.

فعلاً سرور `'cargoOwner' => $row['cargoOwner'] ?? ''` می‌فرستد پس `null` نمی‌آید و مشکلی بروز نمی‌کند، اما `BY_CARGO_OWNER` **حالت پیش‌فرض** این صفحه است و اتکا به یک `?? ''` سمت سرور برای جلوگیری از کرش، تضمین شکننده‌ای است.

**اصلاح پیشنهادی:** نگاشت `null`/`""` به یک برچسب صریح («نامشخص») در همان `groupBy` — دقیقاً کاری که `getGroupedQuotas` سمت سرور انجام می‌دهد (`AppApiController.php:1357-1358`).

---

### ۴.۱۲ 🟡 مرتب‌سازی الفبایی بدون `Collator` فارسی

**محل:** `QuotasListScreen.kt:359-361`

```kotlin
GroupSortingMode.ALPHABETICAL -> { groupedMap.entries.sortedBy { it.key } }
```

`sortedBy` روی `String` از ترتیب کد یونی‌کد استفاده می‌کند، نه ترتیب الفبایی فارسی. حروف «ی»/«ي» و «ک»/«ك» (عربی و فارسی) در کدپوینت‌های کاملاً متفاوتی هستند و نام‌های مشابه در جاهای دور از هم مرتب می‌شوند. همین مورد در گزارش قبلی برای لیست کشتی‌ها شناسایی و با `Collator` رفع شد.

**اصلاح پیشنهادی:** استفاده از `java.text.Collator.getInstance(Locale("fa"))` مانند راه‌حل به‌کاررفته در `ShipsListScreen`.

---

### ۴.۱۳ 🟡 فیلتر انبارها بدون `remember`

**محل:** `ShipDetailsScreen.kt:449-454`

```kotlin
1 -> WarehousesSection(
    warehouses = shipDetails.warehouses.filter {
        it.name.contains(searchQuery, ignoreCase = true)
    },
    onWarehouseSelected = onWarehouseSelected
)
```

این `filter` در هر Recomposition یک لیست جدید می‌سازد. چون لیست جدید با لیست قبلی `equals` است، `WarehousesSection` عملاً recompose اضافی نمی‌شود، ولی تخصیص حافظه‌ی بی‌مورد در هر فریم تایپ کردن رخ می‌دهد. برخلاف تب کوتاژها که فیلتر و گروه‌بندی‌اش داخل `remember` است، اینجا هیچ کشی وجود ندارد.

**اصلاح پیشنهادی:** پیچیدن در `remember(shipDetails.warehouses, searchQuery) { ... }`.

---

### ۴.۱۴ 🟡 «کشتی یافت نشد» با کد وضعیت ۵۰۰

**محل:** `AppApiController.php:612-614, 304-306` و `ReportsViewModel.kt:97-98`

```php
if (empty($warehouses)) {
    throw new Exception("کشتی با نام '$shipName' یافت نشد.");
}
// ...
} catch (Exception $e) {
    $this->sendJsonResponse(['error' => $e->getMessage()], 500);   // ← 500 برای «یافت نشد»
}
```

و تشخیص سمت کلاینت:

```kotlin
private fun isShipNotFoundError(e: Exception): Boolean =
    e.message?.contains("یافت نشد") == true
```

دو مشکل:
- **کد وضعیت اشتباه:** «منبع وجود ندارد» باید `404` باشد نه `500`. الان هر پایش سلامت یا لاگ خطای سروری، جست‌وجوی یک کشتی حذف‌شده را به‌عنوان خطای داخلی سرور ثبت می‌کند و نویز تولید می‌کند. جالب اینکه `getQuotaDetails` در همین فایل (خط ۱۰۳) درست عمل می‌کند و `404` برمی‌گرداند.
- **تشخیص با تطبیق رشته‌ی فارسی:** منطق ناوبری اپ (بازگشت خودکار به لیست کشتی‌ها) به وجود عبارت «یافت نشد» در متن پیام خطا وابسته است. هر ویرایش متن پیام سمت سرور — حتی اصلاح یک نیم‌فاصله — این رفتار را بی‌صدا خراب می‌کند. ضمناً این عبارت در چند پیام دیگر هم هست (`checkQuotaStatus`، `getLoadableTonnage`، `updateTemporaryTonnage`) که می‌تواند به تشخیص اشتباه منجر شود.

**اصلاح پیشنهادی:** برگرداندن `404` با یک کد خطای ماشین‌خوان (مثلاً `{"error": "...", "code": "SHIP_NOT_FOUND"}`) و تشخیص سمت کلاینت بر اساس `response.code() == 404` یا فیلد `code`.

---

### ۴.۱۵ 🟡 نبود محافظت روی پوشه‌ی `log/`

**محل:** `protected_proxy.php:9-11, 25-27`

```php
const PROXY_LOG_DIR = 'log';
const PROXY_ACCESS_LOG = PROXY_LOG_DIR . '/proxy_access.log';

if (!is_dir(PROXY_LOG_DIR)) {
    mkdir(PROXY_LOG_DIR, 0755, true);
}
```

جست‌وجو در پروژه نشان می‌دهد **هیچ فایل `.htaccess`ای** در `PHP/` یا زیرپوشه‌هایش وجود ندارد. اگر وب‌سرور فایل‌های استاتیک این مسیر را سرو کند، `https://<host>/log/proxy_access.log` مستقیماً قابل دانلود است و شامل موارد زیر خواهد بود:

- آدرس IP همه‌ی کاربران اپ
- `User-Agent` کامل (مدل دستگاه، نسخه‌ی اندروید)
- زمان دقیق و الگوی استفاده‌ی هر کاربر
- نام فایل هدف هر درخواست

فایل `log/blocked_ips.txt` و فایل‌های `rate_limit_*.txt` هم در همین پوشه‌اند.

نکته‌ی دوم: `PROXY_LOG_DIR` یک مسیر **نسبی** است و به Working Directory فرایند PHP وابسته است. اگر پیکربندی وب‌سرور تغییر کند، لاگ‌ها در جای غیرمنتظره‌ای نوشته می‌شوند (یا نوشتن با خطا مواجه می‌شود).

**اصلاح پیشنهادی:** افزودن `PHP/log/.htaccess` با محتوای `Require all denied` (و معادل `location` برای Nginx)، و تبدیل مسیر به مطلق با `__DIR__ . '/log'`. بهترین حالت: انتقال کامل پوشه‌ی لاگ به بیرون از Document Root.

---

## ۵. موارد کم‌اولویت (P3) — کدنویسی تمیز

### ۵.۱ 🟢 تکرار گسترده‌ی کد

**`WarehouseQuotasTabs`** (`ShipDetailsScreen.kt:570-672`): دو بلوک `Surface` تقریباً یکسان با ~۴۰ خط تکرار، که تنها تفاوتشان `Icons.Default.Receipt`/`Warehouse`، متن، شمارنده و اندیس است. تمام عبارات `if (selectedTabIndex == 0)` هشت بار در بلوک اول و هشت بار در بلوک دوم تکرار شده‌اند.

**بلوک‌های خطا** (`ShipDetailsScreen.kt:138-174` و `:199-236`): دو بلوک ۳۶ خطی با ساختار کاملاً یکسان (همان `Box` + `Column` + `clip` + `background(errorContainer)` + `Icon(Error)` + دو `Text`) که فقط متن عنوان و منبع پیامشان فرق می‌کند.

**اصلاح پیشنهادی:** استخراج `TabItem(icon, label, count, isSelected, onClick)` و `ErrorStateCard(title, message)` به‌عنوان Composableهای خصوصی — کاهش ~۱۲۰ خط به ~۴۰ خط.

### ۵.۲ 🟢 توابع Wrapper بی‌فایده

**محل:** `QuotaWarningDialog.kt:361-391`

```kotlin
@Composable
private fun WarningDialogHeader(currentPage: Int, totalPages: Int, onClose: () -> Unit) {
    DialogHeader(currentPage, totalPages, onClose)
}

@Composable
private fun ElegantQuotaCard(warning: WarningStatus, viewModel: ReportsViewModel, /* ... */) {
    QuotaCard(warning = warning, viewModel = viewModel, /* ... */)
}

@Composable
private fun WarningPageNavigation(pagerState: PagerState, pageCount: Int) {
    PageNavigation(pagerState, pageCount)
}
```

سه Composable که کاری جز فراخوانی مستقیم Composable دیگری با همان پارامترها انجام نمی‌دهند. هر کدام یک گره اضافی در درخت Composition ایجاد می‌کند و خواندن کد را دو مرحله‌ای می‌کند.

### ۵.۳ 🟢 محاسبات اضافی در مسیر مرتب‌سازی

**محل:** `QuotasListScreen.kt:321-323, 363-381`

```kotlin
fun getWeightInKg(tonnage: Float): Float { return tonnage * 1000f }
```

این ضرب فقط برای *مقایسه* استفاده می‌شود؛ ضرب همه‌ی مقادیر در یک عدد ثابت مثبت ترتیب را تغییر نمی‌دهد. تنها اثر واقعی‌اش، بردن مقادیر به محدوده‌ای است که خطای دقت `Float` بیشتر می‌شود.

```kotlin
groupedMap.entries.sortedBy { (groupName, _) ->
    val totalRemainingKg = groupedMap[groupName]?.sumOf { /* ... */ } ?: 0.0
    // ...
}
```

داخل Comparator، `groupedMap[groupName]` جست‌وجو می‌شود در حالی که مقدار در همان destructuring در دسترس است. برای `n` گروه، `O(n log n)` جست‌وجوی Map غیرضروری به‌علاوه‌ی محاسبه‌ی دوباره‌ی مجموع در هر مقایسه. راه‌حل استاندارد: محاسبه‌ی یک‌باره‌ی مجموع‌ها در یک `Map` و مرتب‌سازی بر اساس آن.

### ۵.۴ 🟢 دسترس‌پذیری (Accessibility)

**محل:** `ShipDetailsScreen.kt:587-593, 629-635`

تب‌ها با `Modifier.clickable` روی `Surface` ساخته شده‌اند بدون `Role.Tab` و بدون `semantics { selected = ... }`. TalkBack آن‌ها را به‌عنوان «تب انتخاب‌شده ۱ از ۲» اعلام نمی‌کند. همچنین `Surface(onClick = ...)` (که در `QuotasListScreen` استفاده شده) به‌جای `Modifier.clickable` رفتار Ripple و اندازه‌ی هدف لمسی بهتری می‌دهد — همان اصلاحی که در گزارش قبلی برای `ShipCard` انجام شد.

---

## ۶. جمع‌بندی و ترتیب پیشنهادی اجرا

**گام اول — توقف نشت داده (فوری):**
1. افزودن `AND cargoType = ?` به `DELETE` جدول `CargoInfo` (مورد ۲.۱)
2. محدود کردن `updateQuotaPercentage`/`updateQuotaPercentageRestriction`/`toggleQuotaStatus` به کلید یکتا (مورد ۲.۳)
3. رفع `!!` در `calculateWarningStatus` (مورد ۲.۴)

**گام دوم — امنیت:**
4. اتصال `PermissionManager` به تمام actionهای نوشتنی + گیت کردن دکمه‌های UI (مورد ۲.۲)
5. تبدیل شش action نوشتنی به `POST` + `Cache-Control: no-store` (مورد ۲.۵)
6. افزودن `.htaccess` روی `log/` (مورد ۴.۱۵)

**گام سوم — صحت عملکرد:**
7. جایگزینی `sanitizeInput` با `validateIdentifier` در پنج تابع (مورد ۳.۱)
8. اصلاح دکمه‌ی گروهی دیالوگ هشدار (مورد ۳.۲)
9. رفع اسپینر بارگذاری (مورد ۳.۳)، `isActive` کشتی (مورد ۳.۵)، کیبورد جستجو (مورد ۳.۶)
10. رفع بازشدن مکرر دیالوگ (مورد ۳.۷) و حذف `Log.d` (مورد ۳.۸)
11. اصلاح آستانه‌ی جهت‌دار هشدار (مورد ۴.۱۰)

**گام چهارم — عملکرد:**
12. افزودن `MicroCache` + `ETag` به `getShipDetails` و `getQuotasList` (مورد ۴.۱)
13. حذف Derived Table تکراری (مورد ۴.۲) و زیرکوئری همبسته (مورد ۴.۳)
14. حذف فیلدهای بی‌مصرف پاسخ (مورد ۴.۵)

**گام پنجم — پاکسازی:**
15. حذف ماشین‌آلات مرده‌ی Pager (مورد ۴.۴)، استخراج Composableهای تکراری (مورد ۵.۱)، حذف Wrapperها (مورد ۵.۲)
16. `rememberSaveable` (مورد ۴.۶)، `Collator` فارسی (مورد ۴.۱۲)، و بقیه‌ی موارد P2/P3

---

**نکته‌ی پایانی:** چند مورد از این گزارش (`sanitizeInput` روی شرط `WHERE`، `rememberSaveable`، `Collator` فارسی، محاسبه‌ی دوباره‌ی `loadedTonnage`، ارسال فیلدهای بی‌مصرف) دقیقاً همان مشکلاتی هستند که در گزارش «لیست کشتی‌ها» شناسایی و رفع شدند، ولی اصلاحشان فقط به فایل‌های آن صفحه اعمال شد. پیشنهاد می‌شود پس از رفع، یک بازبینی افقی روی سایر صفحات (`WarehouseDetailsScreen`، `QuotaDetailsScreen`) هم انجام شود تا این الگوها به‌صورت سیستماتیک حذف شوند.

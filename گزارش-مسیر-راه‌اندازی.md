# گزارش بررسی مسیر راه‌اندازی — Splash → Login → منوی ناوبری

**دامنه بررسی:** از لحظه‌ی start شدن پروسه تا رندر شدن منوی اصلی (HomeScreen)
**تاریخ:** ۱۴۰۵/۰۵/۱۸ (۲۰۲۶-۰۸-۰۹)
**روش:** تحلیل استاتیک کامل کد مسیر بحرانی (بدون اجرا روی دستگاه)

**فایل‌های بررسی‌شده:**
`AtkCargoApplication.kt` · `MainActivity.kt` · `SplashScreen.kt` · `ServerSyncingScreen.kt` · `MainScreen.kt` · `LoginScreen.kt` · `AuthViewModel.kt` · `AuthRepositoryImpl.kt` · `SecurityVerifier.kt` · `CryptoManager.kt` · `UserPreferencesManager.kt` · `HardwarePerformanceEvaluator.kt` · `AnimationManager.kt` · `PermissionPoller.kt` · `RetrofitClient.kt` · `UpdateManager.kt` · `HomeScreen.kt` · `Theme.kt` / `Typography.kt` / `Font.kt` · `AndroidManifest.xml` · `themes.xml` · `build.gradle.kts`

> ⚠️ **هیچ تغییری در کد اعمال نشده است.** این گزارش فقط تشخیص است تا با هم تصمیم بگیریم چه چیزی و با چه ترتیبی اجرا شود. اعداد زمانی جز مواردی که در کد hard-code شده‌اند (مثل ۴۵۰۰ms) تخمینی هستند و نیاز به اندازه‌گیری واقعی با Macrobenchmark دارند.

---

## 📊 خلاصه اجرایی

| موضوع | وضعیت |
|---|---|
| کف زمان راه‌اندازی (hard-coded) | **۴٫۵ ثانیه** برای هر بار اجرا، مستقل از آماده بودن برنامه |
| تعداد درخواست شبکه در cold start | **۷ درخواست HTTP + ۲ پروب سوکت DNS** |
| endpoint تکراری | `check_update.php` **دو بار** فراخوانی می‌شود |
| پروب اینترنت | ۲ بار سوکت به `8.8.8.8:53` + fallback به `https://soft98.ir` (سایت شخص ثالث) |
| Stack های HTTP همزمان | **۳ تا** (OkHttp رتروفیت، OkHttp اختصاصی UpdateManager، HttpURLConnection خام) |
| بدترین حالت انتظار قبل از دیدن UI | تئوریک تا **~۶۰ ثانیه** (۲ تلاش × ۱۵s connect + ۱۵s read در SecurityVerifier) |
| کار روی Main Thread قبل از اولین فریم | بارگذاری کتابخانه JNI + ساخت Retrofit/OkHttp/Gson + I/O دیسک (StatFs) + ساخت ExoPlayer |
| نمونه‌های تکراری `UserPreferencesManager` | **۵ نمونه** (به‌جای ۱ singleton که در Koin ثبت شده) |
| مشکلات امنیتی جدی | **۶ مورد** (۲ مورد بحرانی) |
| باگ‌های منطقی واقعی | **۵ مورد** |
| کد مرده در مسیر راه‌اندازی | **۴ مورد** |

**سه گلوگاه اصلی به ترتیب اهمیت:**
1. `delay(4500)` ثابت در splash — تنها عامل قابل حذف با بزرگ‌ترین اثر ادراکی
2. سریال/موازی نامنظم بودن ۷ درخواست شبکه‌ی gating که همه باید تمام شوند تا UI بیاید
3. کار سنگین و بی‌فایده روی main thread (بنچمارک سخت‌افزار، `System.gc()`، ExoPlayer، JNI)

---

## 🔭 تایم‌لاین واقعی راه‌اندازی (بر اساس کد)

```
┌─ Process Start
│
├─ [ContentProvider] WorkManagerInitializer  ← auto-init اندروید (حذف نشده از مانیفست)
│
├─ Application.onCreate()
│   ├─ startKoin { androidLogger() }         ← لاگ verbose در release هم فعال
│   └─ WorkManager.initialize(...)           ← ⛔ استثنا پرتاب می‌کند (قبلاً init شده) و catch می‌شود
│
├─ MainActivity.onCreate()                   ══ همه روی MAIN THREAD ══
│   ├─ initializeDependencies()
│   │   ├─ ViewModelProvider → UpdateManager()  → ساخت OkHttpClient سوم
│   │   ├─ AppDatabase.getDatabase()            → builder (lazy، اوکی)
│   │   ├─ RetrofitClient.apiService            → System.loadLibrary("secrets") + Retrofit + Gson + OkHttp
│   │   └─ initializeHardwarePerformanceEvaluation()
│   │        └─ lifecycleScope.launch (Main!) → getScoreTimestamp() + StatFs (I/O دیسک) روی Main
│   │             └─ در صورت cache-miss: بنچمارک CPU/GPU/RAM + System.gc()  ⛔
│   │
│   └─ setContent { }                          ← اولین composition
│       ├─ collectAsState(themeColor)          → فریم اول با رنگ پیش‌فرض، بعد recompose کل درخت
│       ├─ ATKCargoTheme                       → createTypography() بدون remember (۱۵+ TextStyle)
│       │                                        + buildAppColorScheme بدون remember
│       └─ SplashScreen
│            ├─ ExoPlayer.Builder().build()    ← روی Main، در بدنه‌ی composition
│            ├─ prepare() splash.mp4 (۸۷۵KB)   ← init کدک ویدیو
│            └─ delay(800) → fade متن
│
├─ LaunchedEffect(Unit)  ─ بلاک gating شبکه ─────────────────────
│   ├─ AppNotificationManager().setupChannels()            [IO]
│   │
│   ├─ networkJob = async {
│   │   ├─ versionJob   → isInternetAvailable() [socket 8.8.8.8] → GET check_update.php
│   │   ├─ securityJob  → verifySecurityStatus()
│   │   │                   ├─ POST signature_check   (HttpURLConnection, timeout 15s)
│   │   │                   ├─ GET  license_info      (sequential)
│   │   │                   └─ POST license_check     (sequential)
│   │   ├─ updateJob    → isInternetAvailable() [socket 8.8.8.8 دوباره] → GET check_update.php دوباره
│   │   └─ sessionJob   → POST check_session
│   │ }
│   │
│   ├─ splashTimer = launch { delay(4500) ; isSplashVisible = false }   ⛔ کف ثابت ۴٫۵ ثانیه
│   ├─ await(isSplashVisible == false)
│   ├─ if (networkJob.isActive) → ServerSyncingScreen  (بدون timeout / retry / cancel)
│   ├─ networkJob.await()
│   ├─ اگر session نامعتبر → clearUserCredentials + Toast
│   ├─ اگر session معتبر → startLoadingNotificationService() [+ درخواست مجوز POST_NOTIFICATIONS]
│   │                     + startChatNotificationWorker()
│   │                     + checkInitialChatMessages()  → refreshMessages() شبکه + DB
│   └─ requestBatteryOptimizationIfNeeded()  → دیالوگ سیستمی در هر اجرا
│
├─ MainScreen()
│   ├─ UserPreferencesManager(context) ← نمونه‌ی جدید (چهارمین)
│   ├─ PermissionPoller(...)           ← CoroutineScope که هرگز cancel نمی‌شود
│   ├─ AnimatedContent(showSplash)     ← ⛔ کد مرده، showSplash هرگز true نمی‌شود
│   ├─ if (!isSessionValid) LoginScreen  else NavHost
│   └─ NavHost با ۶ بلاک transition تکراری inline (۶۰ خط هرکدام)
│
└─ HomeScreen()
    ├─ UserPreferencesManager(context) ← نمونه‌ی پنجم
    ├─ AnimatedContent → اسلاید ۵۰۰ms کل صفحه
    ├─ Canvas full-screen با ۲ radialGradient (هر recompose بازترسیم)
    └─ delay(150) → نمایش گرید منو
```

**جمع تأخیرهای ادراکی بعد از پاسخ سرور:** ۵۰۰ms اسلاید + ۱۵۰ms تأخیر گرید = **~۶۵۰ms** اضافه.

---

# 🔴 اولویت ۰ — بحرانی

### C-1 | کف ثابت ۴٫۵ ثانیه برای هر اجرا
**محل:** `MainActivity.kt:129-132`

```kotlin
val splashTimer = launch {
    delay(4500.milliseconds)
    isSplashVisible = false
}
```

splash تا ۴٫۵ ثانیه نمایش داده می‌شود **حتی اگر تمام کارهای شبکه در ۴۰۰ms تمام شده باشند**. تنها راه رد کردن، **دو بار ضربه‌ی سریع (زیر ۵۰۰ms)** است که هیچ نشانه‌ی بصری ندارد و کاربر از وجودش خبر ندارد (`SplashScreen.kt:96-103`).

این تنها بزرگ‌ترین عدد در کل زمان راه‌اندازی است و کاملاً مصنوعی است.

**پیشنهاد:** splash تا **حداکثر** ۴٫۵ ثانیه نمایش داده شود ولی به‌محض آماده شدن `networkJob` بسته شود (با یک کف کوتاه مثل ۸۰۰ms–۱٫۲s برای جلوگیری از flicker). یعنی:

```
splash تا زمان min(زمان آماده شدن, 4500ms) و نه دقیقاً 4500ms
```

اثر تخمینی: **۲–۳٫۵ ثانیه کاهش** در اجرای معمولی روی شبکه‌ی سالم.

---

### C-2 | برنامه در حالت آفلاین کاملاً قفل می‌شود، بدون هیچ راه بازگشت
**محل:** `MainActivity.kt:72-73, 192-198` + `SecurityVerifier.kt:129-149`

```kotlin
private var isSecurityCheckPassed by mutableStateOf(false)   // پیش‌فرض: رد شده
private var isSecurityCheckLoading by mutableStateOf(true)
```

اگر `verifySecurityStatus()` به هر دلیل شبکه‌ای شکست بخورد (قطعی اینترنت، DNS، سرور down، SSL) نتیجه `Pair(false, NETWORK_ERROR)` است → برنامه روی `SecurityBlockScreen` می‌ماند:

- **هیچ دکمه‌ی «تلاش مجدد» وجود ندارد**
- **هیچ grace period آفلاین وجود ندارد**
- تنها راه خروج، kill کردن برنامه است

بدتر اینکه وضعیت لایسنس در `updateLicenseStatusLocally()` **نوشته می‌شود ولی هیچ‌جا خوانده نمی‌شود** (`SecurityVerifier.kt:236`) — یک کش مرده. زیرساخت grace period نصفه رها شده است.

برای یک اپلیکیشن عملیاتی انبار/بارگیری که ممکن است در محیط با آنتن ضعیف استفاده شود، این یعنی **برنامه در میدان از کار می‌افتد**.

**پیشنهاد:** grace period مبتنی بر همان کش موجود — اگر آخرین تأیید موفق لایسنس کمتر از N روز پیش بوده و خطا از نوع `NETWORK_ERROR` است، اجازه‌ی ورود داده شود. به‌علاوه دکمه‌ی retry روی `SecurityBlockScreen`.

---

### C-3 | تناقض کامل در سیاست fail: امنیت fail-closed، نشست fail-open
**محل:** `MainActivity.kt:421-424`

```kotlin
} catch (_: Exception) {
    // در صورت خطا، جلسه را معتبر فرض می‌کنیم تا کاربر بتواند به کار خود ادامه دهد
    true
}
```

بررسی نشست در هر خطایی **معتبر** فرض می‌شود، در حالی که بررسی امنیتی در هر خطایی **نامعتبر**. نتیجه:

- کاربری که نشستش روی سرور باطل شده، با ایجاد یک خطای شبکه (مثل حالت هواپیما در لحظه‌ی راه‌اندازی) وارد برنامه می‌شود
- منطق «یک دستگاه در هر زمان» (کد ۴۰۹ در `AuthRepositoryImpl`) با همین یک خط دور زده می‌شود
- در همین حال کاربر قانونی با همان خطای شبکه بلاک می‌شود

این دو سیاست باید یکی شوند: **grace period محدود و کنترل‌شده برای هر دو**، نه اینکه یکی همیشه ببندد و یکی همیشه باز کند.

---

### C-4 | `CryptoManager` در خطا، داده‌ی حساس را plaintext ذخیره می‌کند — بی‌صدا
**محل:** `CryptoManager.kt:62-64, 80-82`

```kotlin
fun encrypt(rawString: String): String {
    ...
    } catch (e: Exception) {
        rawString          // ⛔ متن خام برمی‌گردد
    }
}

fun decrypt(encryptedBase64: String): String {
    ...
    } catch (e: Exception) {
        encryptedBase64    // ⛔ ciphertext برمی‌گردد
    }
}
```

سناریوی واقعی: کلید AndroidKeyStore بی‌اعتبار می‌شود (تغییر قفل صفحه در برخی دستگاه‌ها، restore از بکاپ، پاک شدن keystore).

- `encrypt` → **`username` و `sessionToken` به‌صورت متن خام در DataStore نوشته می‌شوند** و هیچ لاگ/سیگنالی وجود ندارد
- `decrypt` → رشته‌ی base64 ciphertext برگردانده می‌شود که بعداً **به‌عنوان username به سرور ارسال می‌شود** → خطاهای غیرقابل تشخیص در سمت سرور

این fallback خطر را از «خطای قابل تشخیص» به «نقض بی‌صدای محرمانگی» تبدیل کرده است.

**پیشنهاد:** در خطا، مقدار خالی برگردانده شود + رخداد لاگ/گزارش شود + نشست باطل و کاربر به login هدایت شود. هرگز نباید plaintext را به‌جای ciphertext قبول کرد.

---

### C-5 | تمام `onCreate` در یک `try/catch` خالی — احتمال صفحه‌ی سفید کامل
**محل:** `MainActivity.kt:88, 212-214`

```kotlin
try {
    initializeDependencies()
    ...
    setContent { ... }
    observeApplicationStates()
} catch (_: Exception) {
    // خطای کلی در راه‌اندازی برنامه
}
```

اگر `setContent` یا `initializeDependencies` استثنا بدهد، کاربر یک **Activity کاملاً خالی و بدون هیچ UI** می‌بیند؛ نه کرش، نه پیام، نه لاگ. این بدترین حالت ممکن برای عیب‌یابی در میدان است — کاربر می‌گوید «برنامه باز نمی‌شود» و هیچ اثری در logcat یا crash report نیست.

به‌علاوه `chatRepository` از نوع `lateinit` است؛ اگر مقداردهی شکست بخورد، `HomeScreen` هنگام فراخوانی `getChatRepository()` با `UninitializedPropertyAccessException` کرش می‌کند — یعنی catch خالی خطا را فقط **به تعویق** انداخته و به یک نقطه‌ی نامرتبط منتقل کرده است.

**پیشنهاد:** حداقل لاگ + گزارش خطا؛ و یک صفحه‌ی خطای قابل مشاهده با دکمه‌ی retry به‌جای صفحه‌ی سفید.

---

# 🟠 اولویت ۱ — مهم (عملکرد)

### P1-1 | `check_update.php` دو بار در هر اجرا فراخوانی می‌شود
**محل:** `MainActivity.kt:113, 115` + `UpdateManager.kt:124, 244`

`isCurrentVersionAllowed()` و `checkForUpdate()` **دقیقاً همان URL با همان پارامترها** را صدا می‌زنند:

```
${Constants.BASE_URL}/check_update.php?current_version=...&api_key=...
```

هر دو هم قبلش `isInternetAvailable()` را صدا می‌زنند → **دو پروب سوکت جداگانه به `8.8.8.8:53`**.

**پیشنهاد:** یک بار فراخوانی، یک پاسخ، و استخراج هر دو مقدار (`min_allowed_version` و اطلاعات آپدیت) از همان JSON. صرفه‌جویی: ۱ درخواست HTTP + ۱ پروب سوکت (تا ~۵s در شبکه‌ی بد).

---

### P1-2 | پروب اینترنت به `8.8.8.8` و fallback به `soft98.ir`
**محل:** `UpdateManager.kt:100-121`

```kotlin
socket.connect(InetSocketAddress("8.8.8.8", 53), 5000)
...
val url = URL("https://soft98.ir")
```

سه مشکل:
1. **DNS گوگل در ایران بلاک/فیلتر است** → معمولاً پروب اول با ۵ ثانیه timeout شکست می‌خورد → مسیر fallback فعال می‌شود
2. **`soft98.ir`** یک سایت دانلود نرم‌افزار شخص ثالث است که به‌عنوان health-check اپلیکیشن سازمانی استفاده شده — نه قابل اتکا، نه مناسب، و افشای الگوی ترافیک به یک دامنه‌ی بی‌ربط
3. کل این پروب **زائد** است؛ `ConnectivityManager.NetworkCapabilities` این اطلاعات را در چند میکروثانیه و بدون شبکه می‌دهد. اگر هم لازم باشد، خود درخواست به سرور خودمان کافی است

**پیشنهاد:** حذف کامل `isInternetAvailable()` و استفاده از `ConnectivityManager` (یا حتی حذف بررسی و اتکا به خطای خود درخواست).
اثر تخمینی: **۵–۱۰ ثانیه کاهش** در شبکه‌های فیلترشده/ضعیف.

---

### P1-3 | timeout های ۱۵ ثانیه × ۲ تلاش در SecurityVerifier، روی مسیر بحرانی
**محل:** `SecurityVerifier.kt:41-42, 107, 184-185, 247-248, 261-262`

```kotlin
private const val BUFFER_DURATION = 15000    // connect + read هرکدام
private const val CONNECTION_ATTEMPTS = 2
```

با ۳ درخواست (signature / license_info / license_check) که دو تای آخر **سریال** هستند، و ۲ تلاش کلی:

بدترین حالت = `2 × (15+15) × چند درخواست` ≈ **بیش از ۶۰ ثانیه** انتظار روی `ServerSyncingScreen` که **هیچ timeout، هیچ retry دستی و هیچ دکمه‌ی cancel ندارد**.

به‌علاوه این‌ها از `HttpURLConnection` خام استفاده می‌کنند، در حالی که پروژه یک OkHttp با connection pool تنظیم‌شده دارد (`RetrofitClient`) — یعنی connection reuse، HTTP/2 و TLS session resumption همه از دست می‌روند.

**پیشنهاد:**
- timeout به ۸s/۸s کاهش یابد
- `license_info` و `license_check` **موازی** شوند (در حال حاضر بی‌دلیل سریال هستند)
- انتقال به همان OkHttpClient مشترک
- یک timeout کلی (مثلاً ۱۲s) روی کل `networkJob` با `withTimeoutOrNull` + مسیر fallback

---

### P1-4 | بنچمارک سخت‌افزار در startup، با `System.gc()` و I/O روی Main Thread
**محل:** `MainActivity.kt:240-257` + `HardwarePerformanceEvaluator.kt`

```kotlin
lifecycleScope.launch {                      // ⛔ Dispatchers.Main
    val userPreferencesManager = UserPreferencesManager(this@MainActivity)   // نمونه‌ی تکراری
    val performanceScore = hardwareEvaluator.evaluatePerformance()
```

`evaluatePerformance()` **قبل از** رسیدن به `withContext(Dispatchers.Default)` این کارها را روی **Main** انجام می‌دهد:
- `getScoreTimestamp()` و `getDeviceSpecs()` → خواندن DataStore
- `generateAdvancedDeviceSpecs()` → `StatFs(...)` = **I/O دیسک روی main thread** + `ActivityManager.getMemoryInfo` + `DisplayManager`

و در حالت cache-miss (اجرای اول، یا هر ۲۴ ساعت، یا هر تغییر در specs) روی `Dispatchers.Default`:
- `runCPUBenchmark`: ۱۰۰٬۰۰۰ بار `sin`/`cos`
- `runGPUBenchmark`: ۱۰۰۰ بار `drawColor` روی bitmap
- `runMemoryBenchmark`: تخصیص ۱۰۰ آرایه + **`System.gc()`** ← ⛔ GC اجباری در بحرانی‌ترین لحظه‌ی startup، که همه‌ی thread ها را متوقف می‌کند

**و همه‌ی این‌ها برای چه؟** نتیجه فقط در `AnimationManager` ذخیره می‌شود که تنها در **۳ فایل و ۵ نقطه** خوانده می‌شود (`CargoCounterComponents`, `RegisterCargoScreen`, `SelectInfoHeaderSection`) و فقط تعیین می‌کند یک انیمیشن `fadeIn()` باشد یا `fadeIn() + expandVertically()`.

نسبت هزینه به فایده کاملاً نامتوازن است. علاوه بر این، `evaluateThermal()` در واقع **سطح باتری** را می‌سنجد نه دما، و `evaluateAdvancedRAM()` با تقسیم عدد صحیح (`/ 1024³`) روی دستگاه ۳GB مقدار `3` و روی ۳٫۹GB هم `3` می‌دهد — دقت ادعاشده وجود ندارد.

**پیشنهاد (سه گزینه، به ترتیب اولویت من):**
1. **حذف کامل** بنچمارک و جایگزینی با یک heuristic ارزان: `ActivityManager.isLowRamDevice` + `availableProcessors` + `refreshRate`. صفر هزینه، همان کیفیت تصمیم.
2. اگر بنچمارک لازم است: انتقال به **بعد از** رندر اولین فریم صفحه‌ی اصلی (lazy، در پس‌زمینه)، و حذف `System.gc()`.
3. حداقلی: انتقال کل تابع به `Dispatchers.IO` و حذف `System.gc()`.

---

### P1-5 | کار سنگین روی Main Thread قبل از اولین فریم
**محل:** `MainActivity.kt:217-238`, `SplashScreen.kt:107-118`

روی main thread و قبل از اولین فریم:

| کار | هزینه تخمینی |
|---|---|
| `System.loadLibrary("secrets")` (از طریق `RetrofitClient.apiService` → `Secrets.getBaseUrl()`) | ۱۰–۳۰ms |
| ساخت `Retrofit` + `Gson` + `OkHttpClient` (اول) | ۲۰–۵۰ms |
| ساخت `OkHttpClient` سوم در `UpdateManager` | ۱۰–۳۰ms |
| `StatFs` + `ActivityManager` + `DisplayManager` | ۵–۲۰ms (I/O) |
| `ExoPlayer.Builder().build() + prepare()` روی ویدیوی ۸۷۵KB | ۱۰۰–۳۰۰ms |
| `CryptoManager()` × ۲ → `KeyStore.getInstance().load(null)` | ۱۰–۴۰ms هرکدام |

**پیشنهاد:**
- `RetrofitClient` و `UpdateManager` به‌صورت lazy و خارج از `onCreate` ساخته شوند (Retrofit خودش `by lazy` است ولی دسترسی به `apiService` در `initializeDependencies` آن را eager می‌کند)
- `System.loadLibrary` در یک `androidx.startup.Initializer` یا در thread پس‌زمینه‌ی `Application` بارگذاری شود
- `ExoPlayer` خارج از بدنه‌ی composable و ترجیحاً به یک راه‌حل سبک‌تر منتقل شود (بخش P1-6)

---

### P1-6 | نبود Splash Screen API — فلش سفید و ویدیوی سنگین
**محل:** `themes.xml`, `AndroidManifest.xml`, `SplashScreen.kt`

```xml
<style name="Theme.ATKCargo" parent="android:Theme.Material.Light.NoActionBar" />
```

- **هیچ `windowBackground` تعریف نشده** و هیچ استفاده‌ای از `androidx.core.splashscreen` وجود ندارد → بین شروع پروسه و اولین فریم Compose، کاربر یک **پنجره‌ی سفید خالی** می‌بیند
- تم پایه `Material.Light` است → این فلش **حتی در حالت dark هم سفید** است
- سپس یک ویدیوی ۸۷۵KB با ExoPlayer در حالت `REPEAT_MODE_ONE` و `volume = 0f` پخش می‌شود — یعنی یک ویدیوی بی‌صدای loop شونده که فقط ۴٫۵ ثانیه دیده می‌شود. ExoPlayer برای این کار ابزار سنگینی است (init کدک، SurfaceView، بافرینگ)

**پیشنهاد:**
1. افزودن `androidx.core:core-splashscreen` با یک `windowSplashScreenBackground` هم‌رنگ برند → حذف کامل فلش سفید
2. جایگزینی ویدیو با یک انیمیشن Lottie (پروژه از قبل Lottie دارد — `lottie_logo.json` موجود است) یا یک انیمیشن Compose ساده. اثر: حذف ۱۰۰–۳۰۰ms init کدک + ۸۷۵KB از APK
3. اگر ویدیو باید بماند: `repeatMode = REPEAT_MODE_OFF` و ساخت player خارج از composition

---

### P1-7 | تغییر رنگ تم باعث recompose کل درخت + بازسازی Typography می‌شود
**محل:** `MainActivity.kt:99-101`, `Theme.kt:31-68`

```kotlin
val themeColorLong by userPreferencesManager.themeColor.collectAsState(initial = 0xFF137fecL)
```

`initial` مقدار پیش‌فرض است و DataStore کمی بعد مقدار واقعی را emit می‌کند. اگر کاربر رنگ تم را عوض کرده باشد:
1. فریم اول با رنگ پیش‌فرض رندر می‌شود (پرش رنگ قابل مشاهده)
2. سپس **کل درخت UI** recompose می‌شود

و در `ATKCargoTheme` هیچ‌کدام از این‌ها `remember` نشده‌اند:

```kotlin
val colorScheme = if (darkTheme) buildAppDarkColorScheme(primaryColor) else buildAppLightColorScheme(primaryColor)
val typography = createTypography()        // ⛔ ۱۵+ TextStyle در هر recomposition
val spacing = Spacing(); val dimensions = Dimensions(); ...   // ⛔ ۶ data class جدید هر بار
```

**پیشنهاد:**
- رنگ تم **قبل از** `setContent` به‌صورت blocking (یا در splash) یک‌بار خوانده شود تا فریم اول درست باشد
- `remember(primaryColor, darkTheme)` روی `colorScheme`؛ `remember` روی `typography` و توکن‌های ثابت (`Spacing`, `Dimensions`, `Elevation`, `Motion`, `AppShapes` می‌توانند حتی `val` سطح فایل باشند چون بدون پارامتر هستند)

---

### P1-8 | ۵ نمونه‌ی جداگانه از `UserPreferencesManager` (در حالی که singleton در Koin ثبت شده)
**محل:** `AppModule.kt:26` (singleton) در مقابل:
- `MainActivity.kt:69` → inject ✅
- `MainActivity.kt:243` → `UserPreferencesManager(this)` ⛔
- `MainActivity.kt:443` → `UserPreferencesManager(this)` ⛔
- `MainScreen.kt:90` → `remember { UserPreferencesManager(context) }` ⛔
- `HomeScreen.kt:156` → `remember { UserPreferencesManager(context) }` ⛔

هر `new` یک `CryptoManager` جدید می‌سازد → `KeyStore.getInstance("AndroidKeyStore").load(null)` جدید (`CryptoManager.kt:14-16`). فایل DataStore به‌خاطر extension property مشترک است، پس داده خراب نمی‌شود، ولی:
- ۴ بار بارگذاری اضافی keystore (هرکدام ۱۰–۴۰ms)
- ۴ جریان `catch/map` جداگانه روی همان `dataStore.data` → هر کدام رمزگشایی AES مستقل برای همان مقادیر
- شکستن قاعده‌ی DI پروژه

**پیشنهاد:** همه‌جا از Koin تزریق شود (`koinInject()` در composable ها).

---

### P1-9 | `PermissionPoller` نشتی scope دارد و در background هم poll می‌کند
**محل:** `MainScreen.kt:94`, `PermissionPoller.kt:47`

```kotlin
val permissionPoller = remember { PermissionPoller(userPreferencesManager) }
```

- `PermissionPoller` یک `CoroutineScope(SupervisorJob() + Dispatchers.IO)` می‌سازد که **هیچ‌گاه cancel نمی‌شود** — نه `DisposableEffect` ای وجود دارد، نه scope در `onDestroy` بسته می‌شود. با هر بازسازی Activity یک poller جدید و زنده اضافه می‌شود
- polling با `LaunchedEffect(isSessionValid)` شروع می‌شود ولی **آگاه به lifecycle نیست** → هر ۳ دقیقه، ۲۴ ساعته، حتی وقتی برنامه در پس‌زمینه است، یک درخواست به سرور می‌رود
- `fetchAndApply` سه بار `.first()` روی DataStore می‌زند = سه بار رمزگشایی AES در هر poll

**پیشنهاد:** انتقال به یک ViewModel با `viewModelScope` (که خودکار cancel می‌شود) + `repeatOnLifecycle(STARTED)` برای توقف در background.

---

### P1-10 | ورود به صفحه‌ی اصلی ~۶۵۰ms انیمیشن اضافه دارد
**محل:** `HomeScreen.kt:161-172, 269-272`

```kotlin
AnimatedContent(targetState = isSessionValid && username.isNotEmpty(),
    transitionSpec = { fadeIn(tween(500)) + slideInVertically(tween(500), ...) ... })
...
LaunchedEffect(Unit) { delay(150.milliseconds); showGridAnimation = true }
```

بعد از اینکه شبکه پاسخ داد، کاربر **۵۰۰ms اسلاید کل صفحه از بالا** + **۱۵۰ms تأخیر** تا دیدن گرید منو صبر می‌کند. یعنی زمان تا «تعامل واقعی» ۶۵۰ms بعد از زمان «آماده بودن داده» است.

به‌علاوه `Canvas` تمام‌صفحه با دو `radialGradient` (`HomeScreen.kt:183-212`) در هر recomposition بازترسیم می‌شود و `Brush.radialGradient` هر بار object جدید می‌سازد.

**پیشنهاد:** کاهش انیمیشن ورود به ۲۰۰–۲۵۰ms، حذف `delay(150)`، و `remember` کردن brush های Canvas.

---

# 🟡 اولویت ۲ — باگ‌های منطقی

### B-1 | `deviceId` در logout با `deviceId` در login یکی نیست — logout سمت سرور کار نمی‌کند
**محل:** `MainScreen.kt:176` و `HomeScreen.kt:230` در مقابل `AuthRepositoryImpl.kt:38-63`

login از `Settings.Secure.ANDROID_ID` (یا UUID ذخیره‌شده) استفاده می‌کند:
```kotlin
val androidId = Settings.Secure.getString(context.contentResolver, Settings.Secure.ANDROID_ID)
```

logout از `Build.DISPLAY` استفاده می‌کند:
```kotlin
val deviceId = Build.DISPLAY ?: UUID.randomUUID().toString()
```

`Build.DISPLAY` رشته‌ی build دستگاه است (مثل `TP1A.220624.014`) و **هیچ ربطی به deviceId نشست ندارد**. یعنی درخواست logout با یک deviceId اشتباه به سرور می‌رود و رکورد نشست پیدا نمی‌شود.

این باگ **مخفی مانده** چون در هر دو شاخه‌ی موفق و ناموفق، کلاینت `clearUserCredentials()` را صدا می‌زند و کاربر فکر می‌کند خروج انجام شد — در حالی که **نشست روی سرور باز می‌ماند**. با توجه به منطق «یک دستگاه در هر زمان»، این یعنی کاربر بعداً با خطای ۴۰۹ روبرو می‌شود.

این کد در **دو جای مستقل تکرار شده** (`MainScreen` و `HomeScreen.Header`) با ~۴۰ خط یکسان.

---

### B-2 | `return@async` زودهنگام هیچ کاری را لغو نمی‌کند
**محل:** `MainActivity.kt:118-119`

```kotlin
val isVersionAllowed = versionJob.await()
if (!isVersionAllowed) return@async Pair(false, false)
```

نیت این بود که اگر نسخه منقضی است، بقیه‌ی درخواست‌ها بی‌خود اجرا نشوند. ولی `securityJob`، `updateJob` و `sessionJob` **فرزندان همان coroutine** هستند؛ `async` تا اتمام همه‌ی فرزندانش کامل نمی‌شود. پس `networkJob.await()` باز هم منتظر همه‌ی درخواست‌ها می‌ماند و این return زودهنگام **هیچ زمانی صرفه‌جویی نمی‌کند**.

**پیشنهاد:** استفاده از `coroutineScope` جداگانه + `cancel()` صریح، یا بازآرایی به شکل sequential-guard.

---

### B-3 | `handleIntent` سه بار برای همان intent اجرا می‌شود
**محل:** `MainActivity.kt:92, 172-174`

```kotlin
handleIntent(intent)                                    // ۱ — در onCreate
...
LaunchedEffect(Unit) { handleIntent(intent) }            // ۲ — بعد از setContent
```
و `LaunchedEffect(Unit)` اصلی هم قبلش. عملاً idempotent است (فقط یک boolean flag ست می‌کند) پس باگ قابل مشاهده‌ای ندارد، ولی نشانه‌ی این است که کسی مطمئن نبوده کدام یکی کار می‌کند.

---

### B-4 | دیالوگ بهینه‌سازی باتری در **هر** اجرا نمایش داده می‌شود
**محل:** `MainActivity.kt:294-330`

```kotlin
if (pm.isIgnoringBatteryOptimizations(packageName)) return
// در غیر این صورت: startActivity(ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS)
```

اگر کاربر یک بار «نه» بگوید، **در هر بار راه‌اندازی دوباره** این دیالوگ (یا در fallback، صفحه‌ی تنظیمات باتری، یا در fallback دوم **کل صفحه‌ی تنظیمات اندروید**) باز می‌شود و درست بعد از بسته شدن splash روی UI می‌آید.

- تجربه‌ی کاربری بد و آزاردهنده
- `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` از موارد حساس Google Play است و درخواست مکرر آن ریسک policy دارد
- `startActivity(ACTION_SETTINGS)` به‌عنوان fallback عملاً کاربر را از برنامه بیرون می‌اندازد

**پیشنهاد:** حداکثر یک بار (با ذخیره‌ی flag در DataStore)، و بهتر است داخل تنظیمات برنامه با توضیح، نه در startup.

---

### B-5 | درخواست مجوز `POST_NOTIFICATIONS` بدون rationale و بدون handle کردن نتیجه
**محل:** `MainActivity.kt:570-585`

`ActivityCompat.requestPermissions` صدا زده می‌شود ولی `onRequestPermissionsResult` **پیاده‌سازی نشده** است — نتیجه هرگز خوانده نمی‌شود. به‌علاوه از داخل `startLoadingNotificationService()` فراخوانی می‌شود که در startup اجرا می‌شود → دیالوگ مجوز روی اولین فریم می‌آید، بدون هیچ توضیحی برای کاربر.

**پیشنهاد:** استفاده از `ActivityResultContracts.RequestPermission` (API مدرن) + نمایش rationale + انتقال به لحظه‌ای که کاربر واقعاً به نوتیفیکیشن نیاز دارد.

---

# 🔵 اولویت ۳ — امنیت (تحلیل تفصیلی)

### S-1 | `API_KEY` به‌صورت plaintext در کد Kotlin
**محل:** `Constants.kt:6`

```kotlin
const val API_KEY = "atk_nk_9290VV42-38XQ02DI-F2WY4L2K-EJA7V682"
```

تمام رازهای دیگر پروژه از مسیر JNI + XOR obfuscation عبور می‌کنند (`secrets.cpp`)، ولی این یکی یک رشته‌ی ثابت Kotlin است. `const val` در بایت‌کد به constant pool تبدیل می‌شود و با یک `strings classes.dex` یا هر decompiler ساده استخراج می‌شود. ProGuard رشته‌ها را مبهم نمی‌کند.

این ناهمگونی کل زحمت obfuscation بقیه‌ی رازها را بی‌اثر می‌کند اگر همین کلید دسترسی مهمی بدهد.

**پیشنهاد:** انتقال به `Secrets` (JNI) مثل بقیه.

---

### S-2 | `verifyLocalAppSignature` یک no-op رمزنگاری انجام می‌دهد
**محل:** `SecurityVerifier.kt:152-169`

```kotlin
val signatureHash = calculateSignatureHash(primarySignature)
val encryptedHash = encryptSignatureLocal(signatureHash)
val decryptedHash = decryptSignatureLocal(encryptedHash)
decryptedHash == EXPECTED_SIGNATURE_HASH
```

hash رمز می‌شود و **بلافاصله در همان تابع** رمزگشایی می‌شود. این دقیقاً معادل `signatureHash == EXPECTED_SIGNATURE_HASH` است، با یک AES encrypt + یک AES decrypt + تولید IV تصادفی اضافه. هیچ ارزش امنیتی ندارد و فقط CPU مصرف می‌کند.

بدتر: اگر رمزنگاری شکست بخورد (که در S-3 خواهیم دید محتمل است)، `encryptSignatureLocal` رشته‌ی `""` برمی‌گرداند → `decryptSignatureLocal("")` هم `""` → مقایسه شکست می‌خورد → **برنامه بی‌دلیل با خطای TAMPERED بلاک می‌شود**. یعنی این کد بی‌فایده، یک نقطه‌ی شکست جدید هم اضافه کرده است.

---

### S-3 | نام یک کلید SharedPreferences به‌عنوان کلید AES استفاده شده
**محل:** `SecurityVerifier.kt:70, 95`

```kotlin
private val LICENSE_STATUS_PREF_KEY: String by lazy { Secrets.getLicenseStatusPrefKey() }
...
val secretKey = SecretKeySpec(LICENSE_STATUS_PREF_KEY.toByteArray(), "AES")
```

یک رشته که کارش نام‌گذاری یک preference است، مستقیماً به‌عنوان کلید AES استفاده شده. مشکلات:
- طول کلید AES باید دقیقاً ۱۶/۲۴/۳۲ بایت باشد؛ اگر این رشته طول دیگری داشته باشد `SecretKeySpec` استثنا می‌دهد → catch می‌شود → `""` → مسیر شکست S-2
- دو نقش کاملاً متفاوت (شناسه‌ی ذخیره‌سازی و کلید رمزنگاری) به یک مقدار وصل شده‌اند؛ تغییر یکی، دیگری را می‌شکند
- IV تصادفی است ولی چون خروجی هیچ‌جا ذخیره یا منتقل نمی‌شود (S-2)، کل ساختار بی‌معنی است

**پیشنهاد:** با حذف S-2، این مورد هم به‌طور خودکار حل می‌شود.

---

### S-4 | تشخیص Frida/Xposed عملاً همیشه `false` برمی‌گرداند
**محل:** `SecurityVerifier.kt:314-322`

```kotlin
val procDir = File("/proc")
procDir.list()?.take(20)?.any { it.contains(processName, ignoreCase = true) }
```

`/proc` شامل نام دایرکتوری‌ها است که اکثراً **شماره‌ی PID** هستند (`1`, `2`, `137`, ...). هیچ‌گاه رشته‌ی `"frida"` در نام یک PID وجود ندارد. `take(20)` هم فقط ۲۰ ورودی اول را (بدون ترتیب مشخص) بررسی می‌کند.

نتیجه: **این تابع همیشه `false` است**. و حتی اگر `true` بود، نتیجه فقط `Log.w` می‌شود و هیچ اقدامی انجام نمی‌گیرد (`SecurityVerifier.kt:307-311`). همین‌طور `Debug.isDebuggerConnected()` هم فقط لاگ می‌شود.

این کد حس امنیت کاذب ایجاد می‌کند. **پیشنهاد:** یا حذف شود (صادقانه‌تر)، یا به‌درستی پیاده شود (بررسی `/proc/self/maps` برای کتابخانه‌های تزریق‌شده، پورت پیش‌فرض ۲۷۰۴۲ فریدا، و **اقدام واقعی** در صورت تشخیص).

---

### S-5 | `sessionToken` به‌عنوان header احراز هویت استفاده نمی‌شود
**محل:** `RetrofitClient.kt:70-78` + همه‌ی درخواست‌ها

`headersInterceptor` فقط `Accept` و `Content-Type` می‌گذارد. توکن نشست در **body هر درخواست** دستی ارسال می‌شود (`SessionCheckRequest`, `LogoutRequest`, `PermissionSyncRequest`, ...). نتیجه:
- هیچ اعمال یکنواخت احراز هویت وجود ندارد؛ فراموش کردن توکن در یک endpoint جدید سایلنت است
- هیچ منطق مرکزی برای refresh یا مدیریت ۴۰۱ وجود ندارد
- `checkSession` توکن خالی را `null` می‌فرستد (`MainActivity.kt:414`) → اگر سرور با توکن null سهل‌گیر باشد، بررسی نشست بی‌اثر است

**پیشنهاد:** یک `Interceptor` احراز هویت + `Authenticator` برای مدیریت ۴۰۱ به‌صورت متمرکز. (خارج از دامنه‌ی این گزارش ولی مرتبط با مسیر لاگین.)

---

### S-6 | موارد پیکربندی مانیفست و بیلد
**محل:** `AndroidManifest.xml`, `build.gradle.kts`

| مورد | مشکل |
|---|---|
| `targetSdk = 34` با `compileSdk = 36` | دو نسخه عقب؛ از بهبودهای عملکرد و امنیت SDK 35/36 محروم است و مهلت Play Store دارد |
| `android:largeHeap="true"` | heap بزرگ‌تر = GC pause های طولانی‌تر؛ معمولاً پوشاندن یک مشکل حافظه است نه راه‌حل |
| `<uses-feature android:name="android.hardware.camera" android:required="true" />` | دستگاه‌های بدون دوربین کاملاً از نصب محروم می‌شوند |
| `REQUEST_INSTALL_PACKAGES` | مجوز حساس (برای آپدیت درون‌برنامه‌ای) — نیاز به declaration در Play Console |
| `WRITE/READ_EXTERNAL_STORAGE` | با `maxSdkVersion=32` محدود شده ✅ ولی بررسی شود که آیا هنوز لازم است |
| `USE_FULL_SCREEN_INTENT` | از اندروید ۱۴ نیاز به مجوز ویژه دارد |
| `androidLogger()` در Koin | لاگ verbose در build های release هم فعال است — باید `if (BuildConfig.DEBUG)` باشد |

---

# ⚪ اولویت ۴ — کد تمیز و ساختار

### Q-1 | کد مرده در مسیر راه‌اندازی

| مورد | محل |
|---|---|
| `SplashScreen` داخل `MainScreen` با `showSplash` که هرگز `true` نمی‌شود | `MainScreen.kt:88, 127-135` |
| مسیر `loginScreen(...)` داخل `NavHost` که هرگز قابل دسترسی نیست (login خارج از NavHost رندر می‌شود) | `MainScreen.kt:162-167` |
| `validateServerSession` — **دو فایل کاملاً یکسان** | `api/SessionValidator.kt` و `core/domain/session/SessionValidator.kt` — هر دو بدون استفاده |
| `WorkManager.initialize()` که همیشه استثنا می‌دهد | `AtkCargoApplication.kt:24-30` |
| `updateLicenseStatusLocally` — نوشته می‌شود، هرگز خوانده نمی‌شود | `SecurityVerifier.kt:236` |
| `LAST_READ_MESSAGE_ID_KEY` — setter دارد، getter ندارد | `UserPreferencesManager.kt:338, 346` |

`AnimatedContent` مرده در `MainScreen` یک لایه‌ی layout اضافه دور **کل برنامه** می‌گذارد.

---

### Q-2 | `MainActivity` بیش از حد مسئولیت دارد (۵۸۹ خط)
تنها یک Activity، این کارها را انجام می‌دهد: DI دستی، بررسی نسخه، بررسی امنیتی، بررسی نشست، بررسی آپدیت، مدیریت دانلود، مدیریت مجوزها، راه‌اندازی سرویس foreground، enqueue کردن worker، همگام‌سازی چت، ارزیابی سخت‌افزار، مدیریت intent، و نگهداری ۱۰ عدد `mutableStateOf` برای UI.

`isSessionValid`، `pendingNavigationDestination` و `shouldOpenWarningsDialog` به‌صورت `public` روی Activity هستند و از `MainScreen`/`HomeScreen` با `LocalContext.current as MainActivity` خوانده می‌شوند.

**پیشنهاد:** یک `StartupViewModel` که وضعیت راه‌اندازی را به‌صورت یک `sealed class` مدل کند:

```kotlin
sealed interface StartupState {
    data object Splash : StartupState
    data object Syncing : StartupState
    data class Blocked(val reason: SecurityErrorType) : StartupState
    data object VersionExpired : StartupState
    data class UpdateRequired(val info: UpdateInfo) : StartupState
    data object NeedsLogin : StartupState
    data object Ready : StartupState
}
```

این جایگزین ۵ متغیر boolean مستقل (`isSplashVisible`, `isServerSyncing`, `isVersionAllowedState`, `isSecurityCheckPassed`, `isSecurityCheckLoading`) می‌شود که در حال حاضر می‌توانند در **ترکیب‌های نامعتبر** قرار بگیرند (مثلاً `isServerSyncing=true` همراه با `isSecurityCheckPassed=false`) و ترتیب `when` تنها چیزی است که از بروز باگ جلوگیری می‌کند.

---

### Q-3 | `LocalContext.current as MainActivity` — cast سخت
**محل:** `MainScreen.kt:102`, `HomeScreen.kt:155`

```kotlin
@SuppressLint("ContextCastToActivity")
val mainActivity = LocalContext.current as MainActivity
```

- `@Preview` ها برای این صفحات کار نمی‌کنند
- تست UI غیرممکن است
- کامپوزبل به یک Activity مشخص گره خورده
- `getChatRepository()` (که `lateinit` است) از Activity گرفته می‌شود — به‌جای DI

`SuppressLint` اینجا اخطار درست lint را خفه کرده است.

---

### Q-4 | ۶ بلاک `transitionSpec` تکراری، ۶۰ خط هرکدام
**محل:** `MainScreen.kt:208-337`

شش `composable<Route>` با انیمیشن‌های تقریباً یکسان (فقط `initialScale` بین `0.86f` تا `0.90f` تفاوت دارد). ~۳۶۰ خط که می‌تواند ~۲۰ خط باشد:

```kotlin
private fun standardTransitions(scale: Float = 0.90f) = ...
```

مهم‌تر از خوانایی: این lambda ها **در هر recomposition از `MainScreen` بازسازی می‌شوند**، و `MainScreen` با تغییر `username`, `userType`, `livePermissions`, `storedPermissions`, `isSessionValid` recompose می‌شود — که همه از DataStore/شبکه می‌آیند و اوایل startup چند بار emit می‌کنند.

---

### Q-5 | منطق logout در دو جا کپی شده
**محل:** `MainScreen.kt:173-203` و `HomeScreen.kt:224-261`

~۴۰ خط یکسان شامل ساخت `LogoutRequest`، فراخوانی API، `when` روی کدهای خطا با پیام‌های فارسی، و `clearUserCredentials`. هر دو نسخه باگ B-1 را دارند. باید یک `LogoutUseCase` شود — که **در پروژه از قبل وجود دارد** (`feature/auth/domain/LogoutUseCase.kt`) و استفاده نمی‌شود.

---

### Q-6 | تکرار ۹ بلاک `catch { emit(emptyPreferences()) }` در `UserPreferencesManager`
**محل:** `UserPreferencesManager.kt:29-132`

همان ۷ خط برای ۹ فیلد کپی شده (و برای ۵ فیلد دیگر مثل `chatFontSize` **فراموش شده** — یعنی آن‌ها در صورت خطای IO کرش می‌کنند). قابل تبدیل به یک flow پایه:

```kotlin
private val prefs = dataStore.data.catch { if (it is IOException) emit(emptyPreferences()) else throw it }
val username = prefs.map { cryptoManager.decrypt(it[USERNAME_KEY] ?: "") }
```

این هم کد را نصف می‌کند و هم رفتار را یکدست می‌کند.

---

### Q-7 | Baseline Profile ساخته نشده است
**محل:** `baselineprofile/`, `build.gradle.kts:207`

زیرساخت کامل آماده است (`BaselineProfileGenerator`, buildType `benchmark`, `profileinstaller`) ولی:
- `automaticGenerationDuringBuild = false`
- هیچ فایل `baseline-prof.txt` در مخزن نیست
- generator فقط `startActivityAndWait()` می‌کند — یعنی حتی اگر اجرا شود، مسیر login و منوی اصلی را پروفایل نمی‌کند

Baseline Profile معمولاً **۱۵–۳۰٪ بهبود در cold start** می‌دهد و اینجا مجانی روی میز مانده است.

**پیشنهاد:** اجرای generator و گسترش آن به مسیر کامل: splash → login (تایپ + submit) → منوی اصلی.

---

## 🧾 جمع‌بندی: تخمین اثر تجمعی

| اقدام | صرفه‌جویی تخمینی (شبکه‌ی سالم) | صرفه‌جویی (شبکه‌ی ضعیف/فیلترشده) |
|---|---|---|
| C-1: splash تطبیقی به‌جای ۴۵۰۰ms ثابت | ۲۵۰۰–۳۵۰۰ms | ۰–۱۰۰۰ms |
| P1-2: حذف پروب `8.8.8.8` / `soft98.ir` | ۵۰–۲۰۰ms | ۵۰۰۰–۱۰۰۰۰ms |
| P1-1: حذف فراخوانی تکراری `check_update.php` | ۱۰۰–۳۰۰ms | ۵۰۰–۵۰۰۰ms |
| P1-3: موازی‌سازی + کاهش timeout امنیتی | ۲۰۰–۶۰۰ms | ۵۰۰۰–۳۰۰۰۰ms |
| P1-4: حذف/تعویق بنچمارک سخت‌افزار | ۵۰–۴۰۰ms | همان |
| P1-5 + P1-6: Splash API + Lottie به‌جای ExoPlayer | ۱۵۰–۴۰۰ms + حذف فلش سفید | همان |
| P1-7: remember کردن تم/typography | ۳۰–۱۰۰ms + حذف پرش رنگ | همان |
| P1-10: کاهش انیمیشن ورود | ۴۰۰–۶۵۰ms | همان |
| Q-7: Baseline Profile | ۱۵–۳۰٪ کل زمان | همان |
| **جمع** | **~۳٫۵–۵٫۵ ثانیه** | **~۱۵–۵۰ ثانیه** |

---

## 🗺️ پیشنهاد من برای ترتیب اجرا

من پیشنهاد می‌کنم در **چهار فاز مستقل** جلو برویم تا هر فاز جداگانه قابل تست و rollback باشد:

**فاز ۱ — «برد سریع»، بدون تغییر ساختار (کم‌ریسک، بیشترین اثر)**
`C-1` (splash تطبیقی) · `P1-1` (حذف فراخوانی تکراری) · `P1-2` (حذف پروب DNS) · `P1-3` (timeout و موازی‌سازی) · `P1-10` (کاهش انیمیشن ورود)
→ تخمین: **۳–۴ ثانیه سریع‌تر**، تغییرات محدود و قابل بازگشت

**فاز ۲ — پایداری و امنیت (ضروری قبل از انتشار بعدی)**
`C-2` (grace period آفلاین) · `C-3` (یکسان‌سازی سیاست fail) · `C-4` (رفع fallback خطرناک CryptoManager) · `C-5` (حذف catch خالی) · `B-1` (باگ deviceId در logout) · `S-1` (انتقال API_KEY به JNI)
→ این فاز باگ‌های واقعی را رفع می‌کند، نه فقط سرعت

**فاز ۳ — پاکسازی و صرفه‌جویی منابع**
`P1-4` (بنچمارک) · `P1-5` (کار main thread) · `P1-6` (Splash API + Lottie) · `P1-7` (تم) · `P1-8` (singleton) · `P1-9` (نشتی poller) · `B-4`, `B-5` (مجوزها) · `Q-1` (کد مرده)

**فاز ۴ — بازآرایی ساختاری (نیاز به تست بیشتر)**
`Q-2` (StartupViewModel) · `Q-3` (حذف cast) · `Q-4`, `Q-5`, `Q-6` (رفع تکرار) · `Q-7` (Baseline Profile) · `S-4` (تصمیم درباره‌ی anti-tamper)

---

## 📐 پیشنهاد اندازه‌گیری (قبل و بعد)

برای اینکه تصمیم‌هایمان بر پایه‌ی عدد واقعی باشد و نه تخمین، پیشنهاد می‌کنم قبل از شروع فاز ۱ یک بار اندازه‌گیری کنیم:

```bash
adb shell am start -W -n com.atk.atk_cargo/.MainActivity
```

و برای عدد دقیق‌تر (TTID/TTFD) یک `StartupBenchmark` با Macrobenchmark در ماژول `baselineprofile` که همین حالا موجود است — این کار حدود نیم‌ساعت است و به ما اجازه می‌دهد اثر هر فاز را دقیق بسنجیم.

---

**سؤال‌هایی که برای شروع نیاز به تصمیم شما دارند:**

1. **نمایش ویدیوی splash:** آیا ۴٫۵ ثانیه ویدیو یک **الزام برندینگ** است یا فقط پر کردن زمان انتظار بود؟ اگر الزام است، رویکرد فاز ۱ عوض می‌شود (ویدیو می‌ماند ولی کار شبکه پشتش کامل موازی می‌شود).
2. **سیاست آفلاین:** برنامه در نبود اینترنت باید چه رفتاری داشته باشد؟ (کاملاً بلاک / grace period چند روزه / حالت فقط-خواندنی)
3. **بنچمارک سخت‌افزار:** موافق با حذف کامل و جایگزینی با heuristic ارزان هستید، یا ترجیح می‌دهید نگه داریم و فقط به پس‌زمینه منتقل شود؟
4. **anti-tamper (S-4):** تشخیص Frida را حذف کنیم (چون کار نمی‌کند) یا درست پیاده‌سازی کنیم؟
5. **`targetSdk`:** ارتقا به ۳۵/۳۶ در همین چرخه انجام شود یا جدا؟

# پلن جامع بررسی و ارزیابی کیفیت کد PHP (Code Quality Audit)

هدف این پلن: ارزیابی دقیق و بی‌طرفانه‌ی کدبیس یک پروژه‌ی PHP از نظر تمیزی کد، اصول حرفه‌ای، امنیت، معماری، عملکرد و ارتباط با دیتابیس، و در نهایت تولید یک **گزارش جامع** که نقاط قوت و ضعف را بر اساس اهمیت دسته‌بندی می‌کند تا تیم توسعه بتواند اصلاحات را با اولویت درست انجام دهد — **بدون تغییر در منطق، عملکرد یا خروجی سیستم**.

---

## ۱. اصل بنیادین کل پروسه

> **بررسی، تشخیصی است نه اصلاحی.** هر تغییری که پیشنهاد می‌شود باید Behavior-Preserving باشد؛ یعنی ورودی/خروجی و منطق کسب‌وکار دست‌نخورده باقی بماند. جزئیات رعایت این اصل در بخش ۷ آمده است.

---

## ۲. مراحل اجرای بررسی (Methodology)

| فاز | شرح | خروجی |
|---|---|---|
| فاز ۰ — شناخت پروژه | بررسی مستندات موجود، فریم‌ورک/عدم فریم‌ورک، نسخه PHP، وابستگی‌ها، معماری فعلی، حجم کدبیس | نقشه کلی پروژه |
| فاز ۱ — بررسی خودکار (Static Analysis) | اجرای ابزارهای تحلیل استاتیک روی کل کدبیس | لیست خام مشکلات + متریک‌ها |
| فاز ۲ — بررسی دستی و عمیق | بررسی انسانی فایل‌به‌فایل بر اساس ۹ حوزه ارزیابی (بخش ۳) | یافته‌های کیفی، مسائلی که ابزار تشخیص نمی‌دهد (منطق، معماری، تصمیمات طراحی) |
| فاز ۳ — بررسی امنیتی هدفمند | تمرکز ویژه روی نقاط ورودی کاربر، احراز هویت، دیتابیس، آپلود فایل | لیست آسیب‌پذیری‌ها با سطح ریسک |
| فاز ۴ — بررسی دیتابیس و کوئری‌ها | بررسی schema، ایندکس‌ها، کوئری‌های سنگین، تراکنش‌ها | لیست مشکلات دیتابیس |
| فاز ۵ — امتیازدهی و تدوین گزارش | جمع‌بندی، وزن‌دهی، اولویت‌بندی | گزارش نهایی (بخش ۶) |
| فاز ۶ — نقشه راه اصلاح | تعیین ترتیب رفع مشکلات بدون شکستن عملکرد | Remediation Roadmap |

---

## ۳. حوزه‌های ارزیابی و چک‌لیست هر حوزه

### الف) کدنویسی تمیز و اصولی (Clean Code)
- نام‌گذاری معنادار متغیرها، توابع، کلاس‌ها (به جای `$d`, `$tmp`, `func1`)
- رعایت استانداردهای PSR-1 و PSR-12 (فرمت‌بندی، فاصله‌گذاری، ساختار فایل)
- عدم تکرار کد (اصل DRY) و وجود منطق مشترک در توابع/کلاس‌های قابل استفاده مجدد
- طول توابع و کلاس‌ها (توابع خیلی طولانی = بوی بد کد)
- پیچیدگی سیکلوماتیک بالا (if/else و switch تودرتوی زیاد)
- استفاده از `declare(strict_types=1)` و Type Hinting برای پارامترها و مقادیر بازگشتی
- رعایت اصول SOLID (به‌ویژه Single Responsibility)
- کامنت‌گذاری مفید (نه اضافی) و مستندسازی با PHPDoc برای متدهای عمومی
- عدم استفاده از کد مرده (Dead Code) و کامنت‌های قدیمی رها شده

### ب) معماری و ساختار پروژه
- جداسازی لایه‌ها (Presentation / Business Logic / Data Access) — الگوی MVC یا معادل آن
- استفاده صحیح از Dependency Injection به‌جای وابستگی سخت (hard-coded `new`)
- ساختار پوشه‌بندی منطقی و استفاده درست از namespace ها (PSR-4)
- استفاده مناسب از الگوهای طراحی (Repository, Factory, Strategy و ...) در جای درست — نه Over-engineering
- امکان توسعه و افزودن قابلیت جدید بدون دست‌زدن گسترده به کدهای موجود (Open/Closed)
- جداسازی تنظیمات (config) از کد

### ج) امنیت (بالاترین اولویت برای محصول تجاری)
- **SQL Injection**: استفاده اجباری از Prepared Statements / PDO یا ORM به‌جای الحاق مستقیم رشته در کوئری
- **XSS**: Escape کردن خروجی‌های نمایشی (htmlspecialchars یا معادل قالب/فریم‌ورک)
- **CSRF**: وجود توکن CSRF در فرم‌ها و عملیات حساس
- **احراز هویت و مدیریت Session**: هش امن رمز عبور (bcrypt/argon2)، تنظیمات امن کوکی (HttpOnly، Secure، SameSite)
- **کنترل دسترسی (Authorization)**: بررسی دسترسی در سمت سرور برای هر Endpoint، نه فقط مخفی‌کردن دکمه در UI
- **اعتبارسنجی ورودی**: Validation و Sanitization تمام ورودی‌های کاربر (GET/POST/Headers/Files)
- **آپلود فایل**: بررسی نوع/پسوند/سایز واقعی فایل، عدم اجرای فایل آپلودشده
- **مدیریت خطا**: عدم افشای Stack Trace یا اطلاعات حساس در پیام خطای محیط Production
- **مدیریت Secrets**: عدم قرارگیری کلید API/رمز دیتابیس در کد؛ استفاده از `.env` و خارج بودن آن از Git
- **وابستگی‌های آسیب‌پذیر**: بررسی کتابخانه‌های Composer با نسخه‌های دارای CVE شناخته‌شده
- **Headerهای امنیتی**: CSP، HSTS، X-Frame-Options و ...
- **Rate Limiting**: محافظت در برابر Brute-force روی لاگین و APIها

### د) ارتباط با دیتابیس
- استفاده صحیح و یکدست از PDO/ORM (بدون کوئری خام ناامن پراکنده در کد)
- طراحی Schema (نرمال‌سازی مناسب، نوع داده صحیح ستون‌ها)
- وجود ایندکس روی ستون‌های پرکاربرد در WHERE/JOIN
- مدیریت Transaction برای عملیات چندمرحله‌ای حساس
- عدم وجود مشکل N+1 Query
- وجود سیستم Migration و کنترل نسخه Schema
- مدیریت Connection (Pooling، Timeout، Retry)

### ه) عملکرد و بهینه‌سازی
- شناسایی کوئری‌های کند/سنگین
- استفاده از Caching (OPcache، Redis/Memcached) در جای مناسب
- بارگذاری Lazy/Eager مناسب برای روابط داده‌ای
- بهینه بودن Autoloading (Composer PSR-4 به‌جای include دستی)
- حجم و زمان پاسخ صفحات/APIها

### و) مدیریت خطا و لاگ‌گیری
- استفاده از Exception Handling ساختاریافته (به‌جای `@` یا سرکوب خطا)
- سیستم لاگ‌گیری استاندارد (مثل Monolog) با سطح‌بندی (Info/Warning/Error/Critical)
- تفاوت درست بین رفتار محیط Development و Production

### ز) تست‌پذیری
- وجود Unit Test / Integration Test برای منطق حیاتی
- میزان Test Coverage
- طراحی کد به شکلی که قابل تست باشد (کوپلینگ پایین)

### ح) وابستگی‌ها و DevOps
- به‌روز بودن نسخه PHP و کتابخانه‌ها
- سلامت composer.json/composer.lock
- وجود CI/CD و بررسی خودکار کیفیت کد در Pipeline

### ط) مستندسازی و کنترل نسخه
- کیفیت تاریخچه Commit و استفاده صحیح از Branching
- وجود README و مستندات API (مثل OpenAPI/Swagger)

---

## ۴. ابزارهای پیشنهادی برای فاز بررسی خودکار

| ابزار | کاربرد |
|---|---|
| PHPStan یا Psalm | تحلیل استاتیک، شناسایی باگ‌های احتمالی و مشکلات Type |
| PHP_CodeSniffer (phpcs) | بررسی انطباق با PSR-12 |
| PHP Mess Detector (PHPMD) | شناسایی کد پیچیده، کد مرده، بوی بد کد |
| Composer Audit / Local PHP Security Checker | شناسایی وابستگی‌های دارای آسیب‌پذیری شناخته‌شده |
| PHPUnit | اجرا/نوشتن تست‌های رگرسیون پیش از هر اصلاح |
| Rector | Refactoring خودکار و **ایمن** بدون تغییر رفتار برنامه |
| Xdebug / Blackfire | Profiling و شناسایی گلوگاه‌های عملکردی |

---

## ۵. روش امتیازدهی و تعیین سطح محصول تجاری

هر حوزه از ۱۰۰ نمره می‌گیرد و در نمره نهایی وزن متفاوتی دارد (امنیت و معماری بیشترین اهمیت را دارند):

| حوزه | وزن در نمره نهایی |
|---|---|
| امنیت | ۳۰٪ |
| معماری و ساختار | ۲۰٪ |
| ارتباط با دیتابیس | ۱۵٪ |
| عملکرد و بهینه‌سازی | ۱۵٪ |
| کدنویسی تمیز | ۱۰٪ |
| تست‌پذیری | ۵٪ |
| مستندسازی و DevOps | ۵٪ |

**سطح‌بندی نهایی محصول:**

| بازه نمره | سطح |
|---|---|
| ۹۰-۱۰۰ | سطح Enterprise — آماده کامل برای محصول تجاری بزرگ |
| ۷۵-۸۹ | سطح حرفه‌ای — قابل عرضه، با بهبودهای جزئی |
| ۶۰-۷۴ | سطح متوسط — نیازمند بهبود پیش از عرضه گسترده |
| ۴۰-۵۹ | سطح پایه — نیازمند بازنگری جدی |
| زیر ۴۰ | غیرقابل‌قبول برای محصول تجاری — نیازمند بازطراحی بخش‌های اساسی |

---

## ۶. ساختار گزارش نهایی

گزارش نهایی باید شامل بخش‌های زیر باشد:

1. **خلاصه اجرایی** — نمره کلی، سطح محصول، مهم‌ترین ریسک‌ها در ۳-۵ خط
2. **جدول امتیاز به تفکیک حوزه** (۹ حوزه بخش ۳ به همراه نمره و وزن)
3. **نقاط قوت** — دسته‌بندی‌شده بر اساس اهمیت (بالا / متوسط / کم)
4. **نقاط ضعف و باگ‌ها** — دسته‌بندی بر اساس شدت:
   - 🔴 **بحرانی (Critical)**: آسیب‌پذیری امنیتی فعال، ریسک نشت داده یا از کار افتادن سیستم
   - 🟠 **بالا (High)**: مشکل جدی در معماری/عملکرد/دیتابیس با تاثیر گسترده
   - 🟡 **متوسط (Medium)**: مشکل قابل توجه ولی غیر فوری
   - 🟢 **کم (Low)**: بهبود کیفیت کد، بدون ریسک عملیاتی

   برای **هر مورد** باید ثبت شود:
   - عنوان و شرح فنی مشکل
   - محل دقیق (فایل و خط)
   - سطح ریسک/تاثیر بر کسب‌وکار
   - راه‌حل پیشنهادی
   - آیا رفع آن نیاز به تغییر رفتار سیستم دارد یا خیر (Behavior-Safe یا Breaking)
5. **نقشه راه اصلاح (Roadmap)** — ترتیب پیشنهادی رفع مشکلات، از بحرانی به کم‌اهمیت
6. **پیوست** — ابزارهای استفاده‌شده و جزئیات متدولوژی

---

## ۷. اصول حیاتی هنگام اصلاح — عدم تغییر عملکرد، منطق و خروجی

از آنجا که الزام صریح پروژه «عدم تغییر عملکرد و خروجی» است، هر اصلاح باید طبق این اصول انجام شود:

1. **رفکتور، نه بازنویسی**: تغییر ساختار داخلی کد بدون تغییر رفتار بیرونی آن
2. **تست رگرسیون قبل از تغییر**: در صورت نبود تست، ابتدا Characterization Test نوشته شود تا رفتار فعلی (حتی اگر ایده‌آل نیست) ثبت و تضمین شود
3. **تغییرات کوچک و اتمی**: هر Commit فقط یک اصلاح مشخص را پوشش دهد تا در صورت بروز مشکل، Rollback ساده باشد
4. **استفاده از ابزار رفکتور ایمن** (مثل Rector) به‌جای ویرایش دستی برای تغییرات مکانیکی
5. **مقایسه ورودی/خروجی پیش و پس از تغییر** (Snapshot Testing) برای تایید یکسان بودن نتیجه
6. **بازبینی کد (Code Review) توسط نفر دوم** پیش از merge شدن هر اصلاح
7. **استقرار تدریجی**: تست در محیط Staging، سپس انتشار محدود، و در نهایت انتشار کامل با برنامه Rollback آماده

---

## ۸. زمان‌بندی پیشنهادی (قابل تنظیم بر اساس حجم پروژه)

| مرحله | زمان تخمینی |
|---|---|
| شناخت پروژه + راه‌اندازی ابزارها | ۰.۵ - ۱ روز |
| بررسی خودکار (Static Analysis) | ۰.۵ روز |
| بررسی دستی عمیق (۹ حوزه) | بسته به حجم کد؛ معمولاً بزرگ‌ترین بخش زمان |
| بررسی امنیتی هدفمند | ۱-۲ روز |
| بررسی دیتابیس | ۰.۵-۱ روز |
| تدوین گزارش نهایی | ۱ روز |

---

این پلن به‌عنوان چارچوب اجرا آماده است. مرحله بعدی طبیعی، اجرای فاز ۰ و ۱ روی کدبیس واقعی پروژه است — کافی است فایل‌ها یا مخزن پروژه در اختیار قرار بگیرد تا بررسی طبق همین ساختار آغاز شود.

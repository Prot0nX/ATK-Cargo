# گزارش بررسی جامع صفحه «لیست کشتی‌ها» (فعال / غیرفعال)

**تاریخ گزارش اولیه:** ۱۴۰۵/۰۵/۱۹ (2026-08-10)
**تاریخ به‌روزرسانی (اجرای رفع مشکلات):** ۱۴۰۵/۰۵/۱۹ (2026-08-10)
**دامنه بررسی:** امنیت، صحت عملکرد (باگ)، کدنویسی تمیز، بهینه‌سازی عملکرد — هر دو سمت اندروید و سرور
**وضعیت کلی:** ۲۶ از ۲۶ مورد اولویت‌دار (P0/P1/P2) رفع و روی محیط تست تأیید شد. علاوه بر آن، حین اجرا ۲ باگ جدید و از قبل موجود کشف و رفع شد.

**فایل‌های اصلی موضوع بررسی:**
- `app/src/main/java/com/atk/atk_cargo/feature/reports/presentation/ships/ShipsListScreen.kt`
- `app/src/main/java/com/atk/atk_cargo/feature/reports/presentation/ships/components/ShipCard.kt`

**فایل‌های مسیر داده که در بررسی و رفع لحاظ شدند:**
- `app/src/main/java/com/atk/atk_cargo/ui/viewmodel/ReportsViewModel.kt`
- `app/src/main/java/com/atk/atk_cargo/data/repository/ReportsRepository.kt`
- `app/src/main/java/com/atk/atk_cargo/data/model/ReportModels.kt`, `AuthModels.kt`
- `app/src/main/java/com/atk/atk_cargo/api/ApiService.kt`, `RetrofitClient.kt`, `AuthSession.kt` (جدید)، `UserPreferencesManager.kt`, `AtkCargoApplication.kt`
- `app/src/main/java/com/atk/atk_cargo/feature/reports/domain/ReportsDomain.kt`
- `app/src/main/java/com/atk/atk_cargo/feature/reports/presentation/dialogs/RealTimeLoadingBottomSheet.kt`
- `app/src/main/java/com/atk/atk_cargo/ui/screens/ManageReportsScreen.kt`
- `PHP/protected_proxy.php`, `PHP/app_api.php`
- `PHP/src/Controllers/AppApiController.php`, `CargoController.php`
- `PHP/src/Services/CargoService.php`, `SessionService.php`
- `PHP/src/Repositories/CargoRepository.php`, `SessionRepository.php`
- `PHP/src/Core/MicroCache.php`, `DatabaseManager.php`
- دیتابیس: جدول `CargoInfo` (ایندکس‌ها + نوع ستون‌های `netWeight`/`loadingQuotaNumber`)

---

## ۱. خلاصه اجرایی

بررسی اولیه سه دسته مشکل جدی را روی صفحه‌ی لیست کشتی‌ها شناسایی کرد: نبود کامل احراز هویت روی API، یک کرش قطعی (`LazyColumn`)، و بلعیده‌شدن خطاهای شبکه در UI. هر ۴ مورد بحرانی (P0)، هر ۸ مورد بالا (P1)، و هر ۱۳ مورد متوسط (P2) رفع شدند و روی یک محیط تست واقعی (اپ اندروید + سرور PHP + دیتابیس MySQL) تأیید شدند.

در حین اجرا، دو مسیر واقعی تست (لاگین و ثبت حواله‌ی جدید) دو **باگ جدی و از قبل موجود** را آشکار کرد که در بازبینی اولیه‌ی کد دیده نشده بودند؛ هر دو رفع شدند (بخش ۷).

### جدول اولویت‌بندی و وضعیت اجرا

| # | اولویت | مورد | وضعیت |
|---|--------|------|-------|
| ۱ | 🔴 P0 | نبود احراز هویت روی `app_api.php` | ✅ رفع شد |
| ۲ | 🔴 P0 | `Access-Control-Allow-Origin: *` روی پروکسی بدون auth | ✅ رفع شد |
| ۳ | 🔴 P0 | کرش `LazyColumn` به‌دلیل کلید تکراری نام کشتی | ✅ رفع شد |
| ۴ | 🔴 P0 | Whitelist پروکسی = تمام فایل‌های `*.php` | ✅ رفع شد (بخشی) |
| ۵ | 🟠 P1 | بلعیده شدن خطای شبکه → لیست خالی به‌جای خطا | ✅ رفع شد |
| ۶ | 🟠 P1 | نادیده گرفتن `uiState` (بدون Loading/Error/Retry) | ✅ رفع شد |
| ۷ | 🟠 P1 | دو بار فراخوانی `loadShips()` هنگام ورود | ✅ رفع شد |
| ۸ | 🟠 P1 | `RealTimeLoadingBottomSheet` مرده اما پرهزینه | ✅ رفع شد |
| ۹ | 🟠 P1 | ناسازگاری قرارداد API با مدل `Ship` (فیلد non-null با مقدار null) | ✅ رفع شد (بخشی) |
| ۱۰ | 🟠 P1 | `htmlspecialchars` روی `shipName` → «کشتی یافت نشد» | ✅ رفع شد |
| ۱۱ | 🟠 P1 | نبود URL-encode روی نام کشتی در مسیر ناوبری | ✅ رفع شد |
| ۱۲ | 🟠 P1 | نشت پیام خطای داخلی سرور به کلاینت | ✅ رفع شد |
| ۱۳ | 🟡 P2 | کوئری سنگین بدون ایندکس‌پذیری و بدون فیلتر | ✅ رفع شد |
| ۱۴ | 🟡 P2 | نبود `Cache-Control`/`ETag` و کش HTTP در OkHttp | ✅ رفع شد |
| ۱۵ | 🟡 P2 | ارسال فیلدهای بی‌استفاده در پاسخ | ✅ رفع شد |
| ۱۶ | 🟡 P2 | `remember` به‌جای `rememberSaveable` برای جستجو و تب | ✅ رفع شد |
| ۱۷ | 🟡 P2 | استفاده نادرست از `derivedStateOf` داخل `remember(keys)` | ✅ رفع شد |
| ۱۸ | 🟡 P2 | از دست رفتن موقعیت اسکرول هنگام تعویض تب | ✅ رفع شد |
| ۱۹ | 🟡 P2 | `Modifier.clickable` روی `Surface` به‌جای `Surface(onClick)` | ✅ رفع شد |
| ۲۰ | 🟡 P2 | ناسازگاری متن placeholder جستجو با رفتار واقعی | ✅ رفع شد |
| ۲۱ | 🟡 P2 | مرتب‌سازی الفبایی بدون `Collator` فارسی | ✅ رفع شد |
| ۲۲ | 🟡 P2 | استفاده از `Float` برای تناژ کیلوگرمی + `toInt()` | ✅ رفع شد (بخشی — نگاه کنید به یادداشت) |
| ۲۳ | 🟡 P2 | نبود مدیریت تناژ منفی / بارگیری بیش از کل | ✅ رفع شد |
| ۲۴ | 🟡 P2 | `Log.d` داخل `derivedStateOf` | ✅ رفع شد |
| ۲۵ | 🟡 P2 | Rate limit بر مبنای `REMOTE_ADDR` (مشکل NAT سازمانی) | ⏭️ باقی‌مانده |
| ۲۶ | 🟢 P3 | موارد تمیزی کد و معماری (۹ مورد) | ✅ ۶ مورد رفع شد / ۳ مورد آگاهانه صرف‌نظر شد |

---

## ۲. یادداشت‌های اجرا (نکات مهمی که فقط در تست واقعی معلوم شدند)

### مورد ۹ — به‌جای تفکیک کامل مدل، مقدار پیش‌فرض ایمن
راه‌حل کامل پیشنهادی (جداسازی `ShipSummary`/`ShipDetails`) چون در دهها فایل دیگر (`ShipDetailsScreen`, `ChatInputBar`, ...) هم از `Ship` استفاده می‌شود، ریسک بالایی داشت. در عوض `warehouses`/`totalVoucherCount` مقدار پیش‌فرض گرفتند (`ReportModels.kt`) تا مشکل اصلی (نشتِ non-null کاتلین توسط Gson) بدون تغییر قرارداد API رفع شود.

### مورد ۲۲ — بدون تبدیل کامل Float→Double
`toInt()`→`roundToInt()` در `ShipCard.kt` رفع شد (خطای گرد کردن به سمت پایین). تبدیل کامل نوع داده از `Float` به `Double`/`Long` انجام **نشد** چون `Float` برای تناژ در ده‌ها فایل دیگر اپ استفاده می‌شود و تغییرش خارج از دامنه‌ی این دو صفحه بود. ریسک عملی این محدودیت پایین است: مقادیر `cargoWeight` در دیتابیس `INT` هستند و بیشینه‌ی مشاهده‌شده در تست (~۱۳.۶ میلیون) کاملاً زیر آستانه‌ی دقت `Float` (~۱۶.۷ میلیون) است.

### مورد ۴ — Whitelist پروکسی
فقط `check_table_structure.php` (ابزار تشخیصی که کل ساختار جدول `user_sessions` را فاش می‌کرد) از whitelist حذف شد. مکانیزم `glob('*.php')` به یک allow-list کاملاً دستی تبدیل **نشد**، چون بدون تأیید همه‌ی ~۳۰ فایل موجود در آن پوشه (برخی مصرف‌کننده‌ی نامشخص داشتند)، ریسک از کار افتادن یک قابلیت فعال زیاد بود.

### مورد ۱۵ — حذف فیلدهای بی‌استفاده
`shippingCompanyCount`, `cargoTypeCount`, `percentageLoaded` و بلاک `statistics` از پاسخ `getShipsList` حذف شدند (تأیید شد هیچ‌جای دیگری، از جمله `getRealTimeData` که همین متد را دوباره صدا می‌زند، به آن‌ها وابسته نیست). به‌جای صرف حذف، راه‌حل «بهتر» گزارش هم اجرا شد: `loadedTonnage` (که سرور از قبل در `getShipsList` و `getShipDetails` محاسبه می‌کرد) به مدل `Ship` اضافه و محاسبه‌ی تکراری `totalTonnage - remainingTonnage` در `ShipCard.kt` و `sortShips` حذف شد.

### مورد ۲۵
کم‌ریسک و کم‌اولویت تشخیص داده شد و در این دور اجرا نشد؛ در صورت نیاز جداگانه قابل انجام است.

### بخش اولویت ۳ (تمیزی کد)
رفع شد: تکرار ۸۵ خطی `ShipsTabSelector` (حلقه‌ی داده‌محور)، انتقال `sortShips` به لایه‌ی دامنه + تست واحد، حذف `ShipsTabAccent` اضافی، حذف `Box` بیرونی بی‌استفاده، افزودن ۵ تست واحد.
آگاهانه صرف‌نظر شد: تفکیک رنگ‌ها به تم مرکزی (تصمیم طراحی)، انتقال رشته‌های فارسی به `strings.xml` (ناهماهنگ با بقیه‌ی اپ که همه‌جا رشته را inline دارد)، تغییر الگوی «ViewModel مستقیم در Composable» (الگوی یکدست کل اپ است).

---

## ۳. جزئیات فنی رفع هر مورد

### 🔴 P0 — بحرانی

**۱. احراز هویت `app_api.php`.** یک گیت واقعی اضافه شد: `AppApiController::requireAuthenticatedSession()` هدرهای `X-Username`/`X-Device-Id`/`X-Session-Token` را می‌خواند و با `SessionService::isValidToken()` (متد جدید، تطبیق دقیق نام‌کاربری+دستگاه+توکن، برخلاف `isSessionActive` قدیمی که توکن را چک نمی‌کرد) اعتبارسنجی می‌کند. سمت اندروید: `AuthSession` (فایل جدید) هویت نشست را در حافظه نگه می‌دارد، `RetrofitClient.headersInterceptor` آن را به هر درخواست اضافه می‌کند، و `UserPreferencesManager` هنگام لاگین/خروج آن را به‌روز نگه می‌دارد.

**۲. CORS باز.** `Access-Control-Allow-Origin: *` از `protected_proxy.php` حذف شد (این پروکسی فقط توسط اپ اندروید مصرف می‌شود، به CORS نیازی ندارد).

**۳. کرش `LazyColumn`.** کلید ترکیبی `"${ship.name}|${ship.cargoType}"` جایگزین `ship.name` شد.

**۴. Whitelist پروکسی.** `check_table_structure.php` حذف و نشت `available_files` از پاسخ‌های خطا برداشته شد.

### 🟠 P1 — بالا

**۵-۶. خطا/Loading/Error.** `ReportsRepository.getShipsList` دیگر خطا را نمی‌بلعد (مطابق الگوی بقیه‌ی متدهای همان فایل). `ShipsListScreen` حالا `uiState` را می‌خواند و `CircularProgressIndicator`/`ErrorState` (با دکمه‌ی تلاش مجدد) نشان می‌دهد.

**۷. فراخوانی دوگانه.** `init { loadShips() }` از `ReportsViewModel` حذف شد؛ فقط صفحه بارگذاری را آغاز می‌کند.

**۸. BottomSheet مرده.** کل بلاک `RealTimeLoadingBottomSheet` و متغیرهای وابسته از `ShipsListScreen` حذف شد؛ خود کامپوننت هم یک `if (!isOpen) return` گرفت.

**۹. مدل `Ship`.** توضیح در بخش ۲.

**۱۰. `htmlspecialchars`.** یک متد جدید `validateIdentifier()` (بدون escape، فقط trim+طول) جایگزین `sanitizeInput()` در `getShipDetails` شد.

**۱۱. URL-encode.** `Uri.encode`/`Uri.decode` در مسیرهای `shipDetails`/`warehouseDetails` در `ManageReportsScreen.kt` اضافه شد.

**۱۲. نشت خطا.** `app_api.php` حالا `Exception` (پیام‌های فارسی امن) را از `Throwable`/`Error` (خطای داخلی، فقط لاگ سرور) تفکیک می‌کند؛ `DatabaseManager::prepare` دیگر `$conn->error` خام را برنمی‌گرداند.

### 🟡 P2 — متوسط

**۱۳. ایندکس‌گذاری دیتابیس.** با `EXPLAIN ANALYZE` واقعی تأیید شد که ۹۷٪ زمان کوئری (۲۳۹ از ۲۴۵ میلی‌ثانیه) صرف Scan کامل ۵۸هزار ردیفی `CargoInfo` بدون ایندکس می‌شد. دو ایندکس ترکیبی پوششی (`idx_cargo_status_group`, `idx_cargo_ship_lookup`) ساخته شد؛ کوئری به ~۱۰۰ میلی‌ثانیه رسید. TTL کش از ۸ به ۲۰ ثانیه افزایش یافت و `MicroCache::forget()` در تمام مسیرهای نوشتنی مؤثر (`editQuota`, `toggleQuotaStatus`, `deleteQuota` در `AppApiController`؛ ثبت/ویرایش/تأیید/حذف حواله در `CargoService`/`CargoController`) اضافه شد.

**۱۴. کش HTTP.** سرور برای `getShipsList` هدر `ETag`/`Cache-Control` می‌فرستد (ETag فقط از `activeShips`/`inactiveShips` محاسبه می‌شود، نه از `timestamp` لحظه‌ای). اندروید یک `okhttp3.Cache` دیسکی دارد (`RetrofitClient.init()` از `AtkCargoApplication.onCreate` صدا زده می‌شود).

**۱۶-۲۴.** همه در گزارش اولیه توضیح داده شده بودند؛ به‌طور کامل طبق راه‌حل پیشنهادی پیاده‌سازی شدند (`rememberSaveable`، `LazyListState` مستقل هر تب، `Surface(onClick)`+`semantics`، اصلاح فیلتر/placeholder جستجو، `Collator` فارسی، `roundToInt`+رنگ هشدار برای تناژ منفی، حذف `Log.d`).

---

## ۴. باگ‌های جدید کشف‌شده حین اجرا (خارج از دامنه‌ی گزارش اولیه)

این دو مورد در گزارش اولیه دیده نشده بودند چون فقط با اجرای واقعی روی دستگاه/دیتابیس آشکار شدند — نمونه‌ی خوبی از اینکه چرا بررسی کد به‌تنهایی کافی نیست.

### 🐛 ناسازگاری فیلد `session_token`/`sessionToken` در پاسخ لاگین
**محل:** `PHP/src/Controllers/AuthController.php:120` در برابر `app/.../AuthModels.kt` (قبل از رفع)

سرور توکن نشست را با کلید `session_token` (snake_case) برمی‌گرداند اما مدل Gson اندروید (`LoginResponse`) انتظار `sessionToken` (camelCase) داشت. نتیجه: `body.sessionToken` همیشه `null` بود و `AuthRepositoryImpl` همیشه رشته‌ی **خالی** به‌جای توکن واقعی ذخیره می‌کرد. این باگ سال‌ها بی‌اثر بود چون تا رفع مورد ۱ (بالا) هیچ endpointی واقعاً توکن را اعتبارسنجی نمی‌کرد؛ همین‌که احراز هویت واقعی اضافه شد، این باگ خاموش آشکار و مسدودکننده شد.
**رفع:** افزودن `@SerializedName("session_token")` روی `LoginResponse.sessionToken`.

### 🐛 ستون خیالی `loadingQuotaNumberReversed`
**محل:** `PHP/src/Controllers/AppApiController.php::checkQuotaExistenceCargo()`

کوئری این متد به ستونی به نام `loadingQuotaNumberReversed` ارجاع می‌داد که **هرگز در جدول `InitialInfo` وجود نداشته**. این endpoint احتمالاً از ابتدا با خطای `Unknown column` شکست می‌خورده و کسی متوجه نشده بود. با تست واقعی صفحه‌ی «ثبت کوتاژ جدید» آشکار شد.
**رفع:** جایگزینی با `REVERSE(loadingQuotaNumber)` محاسبه‌شده در لحظه (بدون نیاز به ستون واقعی یا migration).

---

## ۵. Migration دیتابیس (اجراشده، تأییدشده)

جدا از ایندکس‌گذاری (بخش ۳، مورد ۱۳)، یک ناهماهنگی نوع داده در `CargoInfo` شناسایی و با احتیاط رفع شد:

- **قبل:** `netWeight varchar(100)`, `loadingQuotaNumber varchar(100)` — در حالی که `InitialInfo.loadingQuotaNumber` از نوع `int` بود، باعث `CAST(... AS DOUBLE)` ضمنی در JOIN می‌شد.
- **بررسی امنیت migration (سه دور، فقط‌خواندنی، قبل از هر تغییری):** صفر مقدار غیرعددی در ۵۸٬۶۰۲ ردیف، صفر مورد صفر-ابتدایی (leading zero که نشان‌دهنده‌ی کدِ فرمت‌شده به‌جای عدد خالص باشد)، تأیید کاربر که کل اپ فقط عدد صحیح استفاده می‌کند.
- **کشف حین بررسی:** ثبت حواله‌ی جدید (وضعیت «ورود») عمداً رشته‌ی خالی برای `netWeight` می‌فرستد؛ با `sql_mode=STRICT_TRANS_TABLES` فعال، تبدیل مستقیم به `INT` این مسیر را می‌شکست. `CargoRepository::insertCargo()` طوری اصلاح شد که رشته‌ی خالی را به `NULL` واقعی تبدیل کند (نه به ستون `INT` رشته‌ی خالی بدهد).
- **بعد:** هر دو ستون `INT` (`netWeight` به‌صورت `UNSIGNED`، `loadingQuotaNumber` هم‌نوع دقیق با `InitialInfo`).
- **تأیید نهایی:** `SUM(netWeight)` قبل/بعد از migration دقیقاً برابر (۱٬۴۱۷٬۷۰۱٬۴۶۹)، `EXPLAIN ANALYZE` دیگر `CAST` ندارد، تست واقعی ثبت حواله‌ی جدید (بدون وزن) موفق.

اسکریپت‌های تشخیص/migration (`db_inspect_ships_query.sql`, `db_migrate_cargo_indexes.sql`, `db_inspect_type_mismatch.sql`, `db_inspect_leading_zeros.sql`, `db_migrate_cargo_column_types.sql`) به‌صورت موقت در پوشه‌ی `PHP/` ساخته و بعد از اجرای موفق روی دیتابیس پاک شدند؛ تمام کوئری‌ها و نتایج آن‌ها در بخش‌های بالا مستند شده‌اند.

---

## ۶. نقاط قوت موجود (که در طول اجرا حفظ شدند)

- `@Immutable` روی `Ship`/`ShipsData`
- `MicroCache` روی سنگین‌ترین کوئری (حالا با invalidate صریح تکمیل شد)
- `ConnectionPool` سفارشی در OkHttp
- `network_security_config.xml` درست
- `-assumenosideeffects` روی `android.util.Log` در ریلیز
- prepared statement در تمام کوئری‌های SQL (بدون SQL Injection)

---

## ۷. موارد باقی‌مانده (آگاهانه در این دور انجام نشدند)

| مورد | چرا انجام نشد | توصیه |
|------|----------------|-------|
| ۲۵ — Rate limit بر مبنای IP | نیازمند طراحی جداگانه (شناسه‌محور نه IP-محور) | بعد از تثبیت مکانیزم احراز هویت جدید (مورد ۱)، rate limit را به ازای کاربر پیاده‌سازی کنید |
| تفکیک کامل مدل `Ship` (`ShipSummary`/`ShipDetails`) | بلاست ردیوس بالا (دهها فایل دیگر) | یک refactor مستقل با تست رگرسیون کامل |
| تبدیل کامل `Float`→`Double` برای تناژ | بلاست ردیوس بالا (کل اپ) | در صورت رشد داده به نزدیک آستانه‌ی دقت `Float`، به‌عنوان یک migration جداگانه انجام شود |
| Whitelist کاملاً دستی پروکسی | نیاز به تأیید مصرف‌کننده‌ی هر یک از ~۳۰ فایل | یک ممیزی جداگانه‌ی امنیتی کل `protected_proxy.php` |
| رشته‌های فارسی به `strings.xml` | ناهماهنگ با الگوی فعلی کل اپ | تصمیم معماری سطح‌اپ، نه سطح‌صفحه |

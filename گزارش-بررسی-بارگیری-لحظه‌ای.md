# گزارش جامع بررسی دیالوگ «بارگیری لحظه‌ای» (Real-Time Loading)

تاریخ بررسی: ۱۴۰۵/۰۵/۲۱ (۲۰۲۶-۰۸-۱۲) · شاخه: `main` · کامیت پایه: `9cd9bdc`

---

## ۱. دامنه بررسی و نقشه جریان داده

**کلاینت (Android/Compose)**

| لایه | فایل |
|---|---|
| UI اصلی دیالوگ | `app/src/main/java/com/atk/atk_cargo/feature/reports/presentation/dialogs/RealTimeLoadingBottomSheet.kt` |
| کارت‌ها و آمار | `.../dialogs/RealTimeLoadingCardSection.kt` |
| میزبان دیالوگ | `app/src/main/java/com/atk/atk_cargo/ui/screens/ManageReportsScreen.kt:288-295` |
| ViewModel | `app/src/main/java/com/atk/atk_cargo/ui/viewmodel/ReportsViewModel.kt:116-124, 260-290, 881-909` |
| Repository | `app/src/main/java/com/atk/atk_cargo/data/repository/ReportsRepository.kt:419-430` |
| Retrofit | `app/src/main/java/com/atk/atk_cargo/api/ApiService.kt:251-256` |
| مدل‌ها | `app/src/main/java/com/atk/atk_cargo/data/model/ReportModels.kt:46-56, 429-432` |
| مصرف‌کننده دوم (پس‌زمینه) | `app/src/main/java/com/atk/atk_cargo/api/LoadingNotificationService.kt` |

**سرور (PHP)**

| لایه | فایل |
|---|---|
| Entry point | `PHP/realTimeLoadingData.php` |
| Proxy | `PHP/protected_proxy.php` |
| Controller | `PHP/src/Controllers/AnalyticsController.php:30-241` |
| کش | `PHP/src/Core/MicroCache.php` |

**جریان:**
`RealTimeLoadingBottomSheet` → `ReportsViewModel.loadRealTimeData()` → `ReportsRepository.getRealTimeLoadingData(offset)` → `GET protected_proxy.php?target=realTimeLoadingData.php&action=getRealTimeData&shiftOffset=N` → `AnalyticsController::handleRealTimeDataRequest()` → `determineShiftInfo()` + `getRealTimeData()` (کش APCu ۵ ثانیه) → JSON `{shiftInfo, data[]}`.

---

## ۲. خلاصه مدیریتی

| حوزه | وضعیت | مهم‌ترین مورد |
|---|---|---|
| امنیت | 🔴 بحرانی | endpoint کاملاً **بدون احراز هویت** است؛ کل داده‌های تجاری (کشتی، کوتاژ، شرکت باربری، تناژ) با یک درخواست ساده GET قابل استخراج است |
| باگ عملکردی | 🔴 جدی | هر بار refresh **دو درخواست همسان** به سرور می‌زند؛ شکاف زمانی ۰۷:۰۰–۰۷:۳۰ در هیچ شیفتی شمرده نمی‌شود؛ آمار ورودی هیچ فیلتر تاریخی ندارد |
| پرفورمنس | 🟠 متوسط | polling بدون آگاهی از lifecycle، بدون ETag، `AnimatedContent` روی کل لیست (ریست شدن اسکرول)، ساخت `Path` در هر فریم |
| کیفیت کد | 🟠 متوسط | مسئولیت‌های تایمر/شبکه داخل Composable، رنگ‌های محاسبه‌شده بلااستفاده، فیلد مرده در مدل، خطاها بلعیده می‌شوند |
| UX | 🟠 متوسط | خطا هرگز به کاربر نمایش داده نمی‌شود اما پیام «اطلاعات بروزرسانی شد» همیشه نمایش داده می‌شود |

---

## ۳. امنیت

### 🔴 A-1 | نبود کامل احراز هویت روی endpoint (بحرانی)

`AppApiController` برای همه actionها اجباراً `requireAuthenticatedSession()` را صدا می‌زند (`AppApiController.php:39-53, 91`):

```php
$this->requireAuthenticatedSession();   // X-Username / X-Device-Id / X-Session-Token
```

اما `AnalyticsController::handleRealTimeLoadingData()` (`AnalyticsController.php:30-60`) **هیچ بررسی نشست، توکن یا مجوزی ندارد**. کلاینت هدرهای نشست را می‌فرستد (`RetrofitClient.kt:92-94`) ولی سرور آن‌ها را نادیده می‌گیرد.

نتیجه: هر کسی بدون لاگین می‌تواند اجرا کند:

```bash
curl "https://<host>/protected_proxy.php?target=realTimeLoadingData.php&action=getRealTimeData&shiftOffset=0"
```

و کل لیست کشتی‌ها، کوتاژها، شرکت‌های باربری، انبارها و تناژ خالص هر شیفت را دریافت کند. همین فایل مستقیماً هم قابل فراخوانی است (`https://<host>/realTimeLoadingData.php?...`) که در آن حالت حتی rate-limit و لاگ پروکسی هم دور زده می‌شود.

بدتر: `action=getKotazhInfo` روی همین کنترلر، با کوتاژ ۸ رقمی، **تمام حواله‌های یک کوتاژ** (شماره باسکول، وزن، کسری/اضافی، زمان ورود/خروج) را بدون احراز هویت برمی‌گرداند و چون فرمت آن فقط ۸ رقم است، enumeration کامل عملی است.

**اصلاح پیشنهادی** — استخراج گیت احراز هویت به یک trait/کلاس مشترک و افزودن آن به `AnalyticsController`:

```php
public function handleRealTimeLoadingData(): void {
    header('Content-Type: application/json; charset=UTF-8');
    header('Cache-Control: no-store');
    date_default_timezone_set('Asia/Tehran');

    $this->requireAuthenticatedSession();   // همان منطق AppApiController
    ...
}
```

و برای دو فایل `realTimeLoadingData.php` و `quota_remaining_api.php` دسترسی مستقیم را در وب‌سرور ببندید (اجرا فقط از طریق `protected_proxy.php`).

### 🟠 A-2 | نبود هدرهای کنترل کش روی پاسخ

`AnalyticsController::sendJsonResponse()` (`:521-528`) هیچ هدر `Cache-Control` نمی‌فرستد، برخلاف `AppApiController::sendJsonResponse()` که `no-store` می‌گذارد. در نتیجه پاسخ حاوی داده تجاری روی دیسک کش OkHttp (`RetrofitClient.kt:120-122`) و هر پروکسی میانی قابل ذخیره‌شدن است. افزودن `header('Cache-Control: no-store');` لازم است.

### 🟠 A-3 | نبود اعتبارسنجی/کلمپ روی `shiftOffset`

```php
$shiftOffset = (int)($this->request->get('shiftOffset', 0));
$targetTimestamp = time() + ($shiftOffset * 12 * 3600);
```
(`AnalyticsController.php:145-146`)

کلاینت فقط `-14..0` را می‌فرستد، اما سرور هر عددی را می‌پذیرد: مقادیر مثبت داده «آینده» و مقادیر بسیار بزرگ timestamp نامعتبر/سرریز تولید می‌کنند و هر مقدار یک کلید کش جدید در APCu می‌سازد (بردار cache-flooding ساده). کلمپ کنید:

```php
$shiftOffset = max(-14, min(0, (int)$this->request->get('shiftOffset', 0)));
```

### 🟡 A-4 | Rate-limit مشترک و مبتنی بر IP

`protected_proxy.php:22-23` سقف ۶۰ درخواست در دقیقه به‌ازای هر IP دارد. پشت NAT سازمانی، همه کاربران یک IP دارند؛ با polling ۳۰ ثانیه‌ای (که در عمل ۲ درخواست است — بند B-1) بعلاوه سایر pollerها، رسیدن به سقف و ۴۲۹ گرفتن دسته‌جمعی محتمل است. rate-limit باید بر اساس هویت نشست باشد نه IP.

### 🟡 A-5 | نشتی اطلاعات در پیام خطا

`ReportsRepository.kt:427-429` استثنا را دوباره بسته‌بندی می‌کند و `ReportsViewModel.kt:287` آن را در قالب `"خطا در دریافت اطلاعات: ${e.message}"` نگه می‌دارد. `AppApiController` در `catch (Exception $e)` نهایی `$e->getMessage()` خام را با کد ۵۰۰ برمی‌گرداند — در مسیرهای مشابه این می‌تواند جزئیات داخلی را به کلاینت بدهد. (در `AnalyticsController` این مورد درست مدیریت شده و پیام عمومی برگردانده می‌شود.)

---

## ۴. باگ‌ها

### 🔴 B-1 | هر refresh دو درخواست شبکه یکسان می‌زند

در `RealTimeLoadingBottomSheet.kt:172-178` و `:201-207`:

```kotlin
viewModel.loadRealTimeData(isDarkTheme, defaultColor)
onRefresh()          // ← این هم دقیقاً همان کار را می‌کند
```

و در میزبان (`ManageReportsScreen.kt:293`):

```kotlin
onRefresh = { viewModel.loadRealTimeData(isDarkTheme, defaultColor) }
```

یعنی **هر تیک خودکار ۳۰ ثانیه‌ای و هر کلیک دستی، دو درخواست همسان و هم‌زمان** به سرور می‌فرستد. علاوه بر دوبرابر شدن ترافیک و مصرف سهمیه rate-limit، دو `viewModelScope.launch` موازی روی یک `StateFlow` می‌نویسند و ترتیب نوشتن تضمین‌شده نیست.

**اصلاح:** یکی از دو فراخوانی را حذف کنید. ساده‌ترین راه، خالی کردن `onRefresh` در میزبان یا حذف `onRefresh()` از داخل دیالوگ.

### 🔴 B-2 | شکاف ۰۷:۰۰ تا ۰۷:۳۰ — حواله‌های گم‌شده

`determineShiftInfo()` شیفت روز را `07:30:00 – 19:00:00` و شیفت شب را `19:00:00 – 07:00:00` تعریف می‌کند (`AnalyticsController.php:161-187`). بازه‌ی **۰۷:۰۰:۰۰ تا ۰۷:۲۹:۵۹ در هیچ‌کدام از دو کوئری نمی‌افتد**؛ هر حواله‌ای که در این نیم‌ساعت `exitTime` بخورد، در هیچ شیفتی شمرده نمی‌شود (نه در آمار، نه در وزن کل، نه در متن اشتراک‌گذاری). یکی از دو مرز باید اصلاح شود (`endTime` شب = `07:30:00` یا شروع روز = `07:00:00`).

### 🔴 B-3 | `entryVouchers` هیچ فیلتر زمانی ندارد

در هر دو کوئری (`AnalyticsController.php:213` و `:229`):

```sql
WHERE (... exitDate/exitTime شرط شیفت ...) OR (c.status = 'ورود' AND c.exitDate IS NULL)
```

شاخه‌ی دوم **هیچ قید تاریخی ندارد**. یعنی هر رکورد «ورود» بازمانده از روزها/هفته‌های قبل که هرگز خروج نخورده، تا ابد در آمار «ورودی» و در تعداد «کوتاژ فعال» شمرده می‌شود و ردیف‌های مرده را وارد لیست می‌کند. علامتِ آگاهی از این مشکل در کلاینت دیده می‌شود: برای شیفت‌های گذشته ورودی‌ها به‌زور صفر می‌شوند (`RealTimeLoadingBottomSheet.kt:147-149, 325`) — یعنی مشکل با پنهان‌سازی در UI دور زده شده، نه رفع.

**اصلاح:** قید تاریخ ورود اضافه شود، مثلاً `AND c.entryDate >= ?` یا محدودکردن به بازه شیفت جاری.

### 🟠 B-4 | شیفت تکراری در `shiftOffset` بین ۰۰:۰۰ تا ۰۷:۳۰

گام حرکت بین شیفت‌ها ثابت ۱۲ ساعت است (`:146`) در حالی که شیفت روز ۱۱٫۵ ساعت و شب ۱۲٫۵ ساعت است. در ساعت مثلاً ۰۷:۱۰ (شیفت شب)، `offset = -1` به ۱۹:۱۰ روز قبل می‌رسد که **همان شیفت شب جاری** است — کاربر با زدن «شیفت قبل» همان داده را دوباره می‌بیند. ناوبری شیفت باید بر اساس مرزهای واقعی شیفت محاسبه شود، نه جمع ثابت ۱۲ ساعت.

### 🟠 B-5 | مسابقه (race) بین پاسخ‌ها؛ نبود لغو درخواست قبلی

`loadRealTimeData()` (`ReportsViewModel.kt:260-290`) هر بار یک coroutine جدید در `viewModelScope` می‌سازد و job قبلی را لغو نمی‌کند. سناریوهای واقعی:

- کلیک سریع روی فلش‌های شیفت → چند درخواست موازی با offsetهای مختلف؛ **پاسخی که دیرتر برسد برنده است**، نه پاسخ آخرین offset انتخاب‌شده. نتیجه: هدر `shiftInfo` و داده‌ها با فلش‌ها ناهماهنگ می‌شوند.
- refresh دستی هم‌زمان با تیک خودکار (B-1 آن را تشدید می‌کند).

**اصلاح:**

```kotlin
private var realTimeJob: Job? = null

fun loadRealTimeData(isDarkTheme: Boolean, defaultColor: Color) {
    realTimeJob?.cancel()
    realTimeJob = viewModelScope.launch { ... }
}
```

### 🟠 B-6 | خطا هرگز نمایش داده نمی‌شود، ولی پیام موفقیت همیشه نمایش داده می‌شود

`_loadingError` در ViewModel ست می‌شود (`:287`) اما دیالوگ هیچ‌جا آن را `collect` نمی‌کند. در مقابل:

```kotlin
viewModel.loadRealTimeData(...)
onRefresh()
remainingSeconds = 30
delay(800)
isRefreshing = false
Toast.makeText(context, "اطلاعات بروزرسانی شد", Toast.LENGTH_SHORT).show()
```
(`:203-208`)

`loadRealTimeData` یک fire-and-forget است؛ Toast **بدون توجه به موفقیت یا شکست درخواست** و صرفاً بعد از ۸۰۰ میلی‌ثانیه نمایش داده می‌شود. کاربر در قطعی شبکه، داده کهنه + پیام «اطلاعات بروزرسانی شد» می‌بیند. همچنین `isRefreshing` وضعیت واقعی درخواست را نشان نمی‌دهد، فقط یک تایمر است.

### 🟠 B-7 | ناسازگاری آمار با فیلتر جستجو

- تعداد «کوتاژ فعال» در هدر و «کشتی‌های فعال» از `filteredLoadingData` می‌آید (`:196, 275`)
- ولی وزن کل و تعداد ورودی/خروجی در `StatisticItem` از `loadingData` **فیلترنشده** محاسبه می‌شود (`:147-151`)

با جستجو، تعداد کارت‌ها کم می‌شود اما آمار بالا تغییری نمی‌کند — تناقض بصری. یکی از دو رفتار را انتخاب و یکسان کنید.

### 🟠 B-8 | ریسک شکست پارس `totalNetWeight`

مدل کلاینت `totalNetWeight: Int` است (`ReportModels.kt:54`) اما سرور خروجی `SUM(...)` را بدون cast برمی‌گرداند (`AnalyticsController.php:208, 224`)؛ mysqli برای `SUM` روی ستون DECIMAL رشته‌ای مثل `"12345.50"` می‌دهد. Gson برای فیلد `Int` مقدار `"12345.00"` را تحمل می‌کند ولی `"12345.50"` را با `NumberFormatException` رد می‌کند و **کل پاسخ fail می‌شود** (دیالوگ خالی می‌ماند). دو راه:

1. سرور: `(float)$row['totalNetWeight']` یا `CAST(... AS SIGNED)` قبل از خروجی.
2. کلاینت: تبدیل فیلد به `Float` تا از `FloatTypeAdapter` موجود (`RetrofitClient.kt:41-64`) که کاملاً مقاوم است استفاده شود.

توصیه: هر دو.

### 🟡 B-9 | برچسب «ورود/خروج» با ترتیب مقادیر نمی‌خواند

`RealTimeLoadingCardSection.kt:254-259`:

```kotlin
text = if (showEntry) "ورود/خروج" else "خروجی"
text = if (showEntry) "${data.exitVouchers} / ${data.entryVouchers}" else ...
```

برچسب «ورود» را اول می‌آورد اما مقدار **خروج** را اول چاپ می‌کند، و چون `textDirection = Ltr` اجباری است ترتیب نمایش هم برعکسِ خواندن فارسیِ برچسب می‌شود. یا برچسب را به «خروج/ورود» تغییر دهید یا ترتیب مقادیر را.

### 🟡 B-10 | فیلد مرده در مدل

`RealTimeLoadingData.cargoWeight` (`ReportModels.kt:55`) در هیچ‌یک از دو کوئری سرور SELECT نمی‌شود؛ همیشه `0` است. باید حذف شود (در غیر این صورت هر مصرف‌کننده‌ای که به آن اعتماد کند نتیجه غلط می‌گیرد).

### 🟡 B-11 | `LEFT JOIN` که در عمل `INNER JOIN` است

در هر دو کوئری، شرط‌های `WHERE` روی ستون‌های `c.*` هستند؛ بنابراین ردیف‌هایی که JOIN آن‌ها NULL شده حذف می‌شوند و `LEFT JOIN` گمراه‌کننده است. یا به `INNER JOIN` تغییر دهید (خواناتر و برای بهینه‌ساز شفاف‌تر) یا شرط‌ها را به `ON` منتقل کنید اگر واقعاً کوتاژهای بدون تردد باید نمایش داده شوند.

### 🟡 B-12 | فیلتر `isActive` اعمال نمی‌شود

برخلاف `getActiveQuotasRemaining()` که `WHERE i.isActive = 1` دارد (`:314`)، کوئری real-time هیچ فیلتری روی `isActive` ندارد؛ کوتاژهای غیرفعال‌شده هم در «بارگیری لحظه‌ای» ظاهر می‌شوند.

---

## ۵. پرفورمنس

### 🟠 P-1 | Polling بدون آگاهی از lifecycle

`LaunchedEffect(isOpen)` (`:166-182`) تا زمانی که BottomSheet در composition است می‌چرخد، حتی وقتی اپ به پس‌زمینه رفته یا صفحه خاموش است. باید با `repeatOnLifecycle(Lifecycle.State.RESUMED)` محصور شود تا هنگام `onPause` متوقف و در `onResume` با یک fetch فوری از سر گرفته شود.

### 🟠 P-2 | نبود ETag / 304 برای این endpoint

`AppApiController` برای `getShipsList` مکانیزم `sendCacheableJsonResponse` با ETag دارد، اما `AnalyticsController` ندارد. با polling ۳۰ ثانیه‌ای × (۲ درخواست به‌خاطر B-1) × تعداد کاربران، کل payload هر بار روی شبکه منتقل می‌شود در حالی که در بیشتر تیک‌ها هیچ تغییری رخ نداده. افزودن ETag روی `data` (نه `shiftInfo` که ثابت است) بار شبکه را در حالت بی‌تغییر تقریباً صفر می‌کند.

### 🟠 P-3 | `AnimatedContent` روی کل لیست ⇒ ریست شدن اسکرول

```kotlin
AnimatedContent(targetState = filteredLoadingData, ...) { targetLoadingData ->
    LazyColumn { items(groupedLoadingData, key = { it.first }) { ... } }
}
```
(`:284-307`)

هر بار که داده تغییر کند (هر ۳۰ ثانیه در ساعات فعال)، `AnimatedContent` یک محتوای جدید می‌سازد؛ `LazyColumn` جدید یعنی **موقعیت اسکرول کاربر به ابتدای لیست پرت می‌شود** و کل کارت‌ها دوباره ساخته و crossfade می‌شوند. برای لیستی که خودش polling می‌شود این هم باگ UX است هم هزینه ترکیب‌بندی.

**اصلاح:** `AnimatedContent` را حذف کنید و از یک `LazyColumn` پایدار با `rememberLazyListState()`، `key` (که از قبل درست است) و `Modifier.animateItem()` برای انیمیشن ورود/جابه‌جایی ردیف‌ها استفاده کنید.

### 🟡 P-4 | تخصیص `Path` در هر فریم

`RealTimeLoadingCardSection.kt:83-114` داخل `drawBehind` در هر draw pass یک `Path` جدید می‌سازد. با چند ده کارت + `animateContentSize` + انیمیشن‌های infinite، این GC churn قابل توجهی است. `Path` را با `remember` نگه دارید و در `drawBehind` فقط `reset()` + بازسازی کنید، یا با `drawRoundRect` روی یک `clip` ساده جایگزین کنید.

### 🟡 P-5 | کار محاسباتی بلااستفاده در هر refresh

در `loadRealTimeData` هر بار `assignDistinctColors` برای همه کشتی‌ها اجرا و `_quotaColorMap` ساخته می‌شود (`ReportsViewModel.kt:269-284`)، در حالی که در این دیالوگ `shipColorMap` فقط برای رنگ یک `HorizontalDivider` با `alpha = 0.1f` استفاده می‌شود (`:384`) و `quotaColorMap` اصلاً استفاده نمی‌شود. اگر جای دیگری مصرف ندارد، حذف یا به‌صورت `derivedStateOf`/lazy محاسبه شود.

### 🟡 P-6 | انیمیشن‌های infinite همیشه‌فعال در `RefreshOverlay`

`rememberInfiniteTransition` در `RefreshOverlay` (`:504-521`) داخل `AnimatedVisibility` است، پس فقط هنگام نمایش composed می‌شود — این بخش درست است. اما `DialogHeader` یک `animateFloatAsState` با `infiniteRepeatable` دارد (`:756-767`) که استفاده از `animateFloatAsState` با spec بی‌نهایت الگوی نادرستی است (هدف مقدار ثابت `360f` است ولی spec تکرارشونده)؛ بهتر است با `rememberInfiniteTransition` مشروط یا `Animatable` بازنویسی شود.

### 🟡 P-7 | drift تایمر

حلقه `delay(1000)` + زمان درخواست + `delay(500)` باعث می‌شود دوره واقعی همیشه بیش از ۳۰ ثانیه باشد و به‌مرور drift کند. برای شمارنده معکوس بهتر است زمان هدف مطلق (`System.currentTimeMillis() + 30_000`) نگه داشته شود.

### 🟢 P-8 | نکات درست موجود

- `if (!isOpen) return` در ابتدای Composable (`:134`) از collect شدن StateFlowها و recomposition در حالت بسته جلوگیری می‌کند — درست و مؤثر.
- `MicroCache::remember` با TTL ۵ ثانیه به‌ازای هر شیفت (`AnalyticsController.php:193-201`) بار دیتابیس را در polling چندکاربره به‌درستی کم می‌کند.
- استفاده از `remember(key)` برای مجموع‌ها و گروه‌بندی‌ها (`:147-164, 293-305`) درست است.
- `ConnectionPool(10, 5min)` در `RetrofitClient` برای الگوی polling انتخاب مناسبی است.

---

## ۶. کیفیت کد و معماری

### C-1 | منطق تایمر و شبکه داخل Composable

کل چرخه polling، مدیریت `isRefreshing`، شمارنده و trigger شبکه داخل بدنه Composable است (`:166-182`). این باعث می‌شود: تست‌ناپذیر باشد، با تغییر پیکربندی (چرخش صفحه) ریست شود، و وضعیت واقعی درخواست را نشناسد. جای درست این منطق ViewModel است — مثلاً یک `StateFlow<RealTimeUiState>` که شامل `data`, `isLoading`, `error`, `secondsToNextRefresh` باشد و دیالوگ فقط آن را رندر کند.

### C-2 | Composable وابسته به `ReportsViewModel` کامل

`RealTimeLoadingBottomSheet` و `RealTimeShiftNavigation` مستقیماً `viewModel: ReportsViewModel` می‌گیرند (`:129, 421`) در حالی که `loadingData`, `shiftInfo`, `onRefresh` هم به‌صورت پارامتر پاس داده می‌شوند — ترکیب دو الگو. برای تست‌پذیری و preview، بهتر است فقط state و lambdaها پاس داده شوند.

### C-3 | تکرار کد در سرور

`getActiveQuotasRemaining()` و `getShipQuotasRemaining()` (`AnalyticsController.php:308-515`) حدود ۱۰۰ خط تقریباً یکسان دارند. همچنین دو شاخه کوئری در `getRealTimeData()` فقط در `WHERE` تفاوت دارند و می‌توانند یک SELECT مشترک با شرط پارامتری داشته باشند.

### C-4 | نمایش تکراری «شرکت باربری» در کارت

در `RealTimeLoadingCard` مقدار `data.shippingCompany` دو بار نمایش داده می‌شود: یک بار کنار آیکن `Person` در بالای کارت (`:147`) و یک بار در ستون «شرکت باربری» (`:211`).

### C-5 | خطاهای بلعیده‌شده

- `LoadingNotificationService.kt:95-97, 154-156, 293-295` — `catch (_: Exception) { "" }` (یک رشته‌ی بی‌مصرف به‌عنوان بدنه catch). خطاها بی‌صدا گم می‌شوند.
- `ReportsRepository.kt:427-429` — استثنا دوباره بسته‌بندی می‌شود و `HttpStatusException`/کد وضعیت از بین می‌رود؛ کلاینت نمی‌تواند ۴۰۱ را از ۴۲۹ یا timeout تشخیص دهد. (توجه: پروژه از قبل `HttpStatusException` دارد — همان الگو باید اینجا هم استفاده شود.)

### C-6 | فراخوانی ناهماهنگ endpoint در سرویس نوتیفیکیشن

`LoadingNotificationService` همان endpoint را بدون `shiftOffset` صدا می‌زند (`:189`) و هر ۵ دقیقه در پس‌زمینه اجرا می‌شود. با دیالوگ باز، همان داده دوباره در پس‌زمینه fetch می‌شود. اشتراک یک لایه repository با کش کوتاه در کلاینت این تکرار را حذف می‌کند.

### C-7 | `gregorianToJalali` دستی در ViewModel

`ReportsViewModel.kt:911-944` یک تبدیل تاریخ شمسی دست‌نویس با تورفتگی شکسته (`:942`) دارد که فقط به‌عنوان fallback در `shareRealTimeLoadingData` استفاده می‌شود. سرور از قبل `jdf.php` دارد و پروژه احتمالاً یک utility مشترک دارد؛ این کپی باید حذف شود.

---

## ۷. اولویت‌بندی اقدامات

**اولویت ۱ — همین هفته**

1. **A-1**: افزودن `requireAuthenticatedSession()` به `AnalyticsController` + بستن دسترسی مستقیم به `realTimeLoadingData.php`.
2. **B-1**: حذف فراخوانی دوگانه `loadRealTimeData` / `onRefresh` (تک‌خطی، ۵۰٪ کاهش ترافیک این صفحه).
3. **B-2**: بستن شکاف زمانی ۰۷:۰۰–۰۷:۳۰.
4. **B-3**: افزودن قید تاریخ به شاخه‌ی `status = 'ورود'`.

**اولویت ۲ — اسپرینت جاری**

5. **B-5**: لغو job قبلی در `loadRealTimeData`.
6. **B-6**: نمایش واقعی خطا و اتصال Toast به نتیجه واقعی درخواست.
7. **B-8**: تبدیل `totalNetWeight` به `Float` + cast سمت سرور.
8. **P-3**: حذف `AnimatedContent` و حفظ موقعیت اسکرول.
9. **A-2** و **A-3**: هدر `no-store` و کلمپ `shiftOffset`.

**اولویت ۳ — بدهی فنی**

10. **C-1**: انتقال منطق polling/تایمر به ViewModel با یک UiState واحد.
11. **P-1** و **P-2**: lifecycle-aware polling + ETag.
12. **B-4, B-7, B-9, B-10, B-11, B-12, P-4, P-5, C-3..C-7**.

---

## ۸. نکات مثبت قابل حفظ

- الگوی `if (!isOpen) return` برای جلوگیری از recomposition پنهان — درست و باید در دیالوگ‌های مشابه هم استفاده شود.
- استفاده کامل از prepared statement در همه کوئری‌ها؛ هیچ SQL Injection در این مسیر یافت نشد.
- `MicroCache` با TTL کوتاه و کلید مشتق از شیفت — طراحی مناسب برای endpoint پر-poll.
- `@Immutable` روی مدل‌های داده و استفاده از `key` در `items` — پایه‌ی درست برای بهینه‌سازی Compose.
- جداسازی `RealTimeLoadingCardSection.kt` از فایل اصلی — تفکیک ساختاری خوب.
- `ProtectedProxy` مسیرهای `..`, `/`, `\` را می‌بندد و whitelist دارد — Path Traversal در آن یافت نشد.

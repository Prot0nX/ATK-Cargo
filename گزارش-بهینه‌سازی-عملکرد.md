# گزارش بررسی عملکرد و بهینه‌سازی — ATK-Cargo

**دامنه بررسی:** کل کد PHP سمت سرور (`PHP/`, ~۷٬۷۰۰ خط) و کل اپلیکیشن اندروید (`app/`, ~۵۱٬۸۰۰ خط Kotlin)
**هدف:** افزایش سرعت پاسخ‌گویی سرور و روانی اپلیکیشن، **با حفظ کامل منطق و رفتار فعلی**
**تاریخ گزارش اولیه:** ۱۴۰۵/۰۵/۱۷ (۲۰۲۶-۰۸-۰۸)
**آخرین به‌روزرسانی:** ۱۴۰۵/۰۵/۱۷ (۲۰۲۶-۰۸-۰۸) — پس از سومین نشست کامل اجرا

---

## 📊 خلاصه وضعیت (به‌روز)

| بخش | انجام‌شده | باقی‌مانده |
|---|---|---|
| PHP — اولویت ۰ (بحرانی) | ۵ از ۵ ✅ | — (فقط دیپلوی production) |
| PHP — اولویت ۱ (مهم) | ۸ از ۹ | P1-8 عمداً دست‌نخورده (تصمیم گرفته‌شده) |
| PHP — اولویت ۲ (بهبود) | ۵ از ۱۰ | P2-6, 9 (نیاز به تصمیم/دسترسی سرور) |
| اندروید — اولویت ۰ (بحرانی) | ۴ از ۴ | — |
| اندروید — اولویت ۱ (مهم) | ۴ از ۶ (زیرساخت Baseline Profile آماده) | تولید Baseline Profile، A1-6 |
| اندروید — اولویت ۲ (build) | ۳ از ۹ | A2-4 تا A2-9 (بررسی نشده) |
| 🎁 یافته‌ی جانبی | یک باگ کرش بحرانی (۱۶KB page size) + یک باگ over-fetch در `getGroupedQuotas` پیدا و رفع شد | — |

همه‌ی موارد PHP روی سرور تست (`test_api`) با curl تست شدند؛ همه‌ی موارد اندروید با build کامل (و برخی با تست دستی روی گوشی) تأیید شدند.

> ⚠️ **هیچ‌کدام از تغییرات PHP این سه نشست هنوز روی production دیپلوی نشده‌اند** — همه فقط روی `test_api` تست شده‌اند. لیست کامل فایل‌های آماده‌ی دیپلوی در انتهای گزارش. تغییرات Gradle/Android build-time (نشست سوم) محلی build و تست شدند و نیاز به «دیپلوی» ندارند، فقط باید commit/push و توسط بقیه‌ی تیم sync شوند.

---

# بخش اول: سمت سرور (PHP)

## 🔴 اولویت ۰ — بحرانی

### ✅ P0-1 | `protected_proxy.php`: هزینه ثابت روی تک‌تک درخواست‌ها — **انجام شد و تست شد**
**محل:** `PHP/protected_proxy.php`

فایل به‌طور کامل بازنویسی و از حالت obfuscate شده به کد تمیز و خوانا تبدیل شد (منطق کاملاً حفظ شد؛ رمزگشایی رشته‌ها با یک اسکریپت تشخیصی موقت تأیید شد که فقط ۶ هدر امنیتی/CORS تنظیم می‌کردند، چیز مشکوکی نبود).

**بهینه‌سازی‌ها:**
- **whitelist caching:** لیست فایل‌های مجاز به‌جای `glob()` روی هر درخواست، در APCu کش می‌شود (TTL=۵ دقیقه)
- **rate limit در APCu:** به‌جای خواندن/نوشتن فایل JSON روی دیسک هر بار، در APCu ذخیره می‌شود (fallback خودکار به روش فایلی قبلی اگر APCu فعال نباشد)
- **log buffering:** نوشتن لاگ بعد از ارسال پاسخ به کلاینت انجام می‌شود (`fastcgi_finish_request`)

**تأیید:** تست کامل روی `test_api` — بدون target (۴۰۰)، target نامعتبر (۴۰۴)، path traversal بلاک‌شده (۴۰۴)، OPTIONS (۲۰۰)، target معتبر (۲۰۰ با خروجی و کوکی صحیح)، و rate limit (دقیقاً ۶۰ درخواست مجاز از ۷۰ درخواست پشت‌سرهم، از ۶۱ به بعد ۴۲۹) — تأیید کرد APCu فعال است و رفتار قبلی حفظ شده.

---

### ✅ P0-2 | نبود ایندکس روی جداول — **انجام شد و تأیید شد**
**محل:** دیتابیس `cargo_test` (⚠️ باید روی دیتابیس Production هم تکرار شود)

بررسی واقعی نشان داد `CargoInfo` با ۵۸٬۵۵۹ ردیف **هیچ ایندکسی به‌جز PRIMARY KEY نداشت**. این ۵ ایندکس اضافه و تأیید شدند:

```sql
CREATE INDEX idx_cargo_quota_lookup ON CargoInfo (loadingQuotaNumber, shipName, loadingWarehouse, shippingCompany, cargoType, status);
CREATE INDEX idx_cargo_status_ship ON CargoInfo (status, shipName, loadingQuotaNumber, loadingWarehouse, shippingCompany, cargoType);
CREATE INDEX idx_cargo_tracking ON CargoInfo (trackingNumber);
CREATE INDEX idx_cargo_scale_receipt ON CargoInfo (scaleReceiptNumber);
CREATE INDEX idx_cargo_exit_datetime ON CargoInfo (exitDate, exitTime);
```

**تأیید با EXPLAIN:** کوئری‌های سنگین از `type=ALL` (اسکن کامل ۵۸هزار ردیف) به `type=ref`/`range` (تا ۵۹ ردیف) تغییر کردند.

> ⚠️ **مهم:** این ایندکس‌ها هنوز فقط روی دیتابیس **تستی (`cargo_test`)** هستند. باید **روی دیتابیس Production هم اجرا شوند**.

---

### ✅ P0-3 | کوئری‌های Aggregate بدون فیلتر — **انجام شد و تأیید شد**
**محل:** `PHP/src/Controllers/AppApiController.php`

| متد | وضعیت |
|-----|------|
| `getLoadableTonnage()` | ✅ اصلاح شد — فیلتر `loadingQuotaNumber` به داخل derived table منتقل شد |
| `getQuotaDetails()` | ✅ اصلاح شد |
| `getShipDetails()` | ✅ اصلاح شد — فیلتر `shipName` به داخل derived table منتقل شد |
| `getShipsList()` | بدون تغییر ساختاری (بعداً با P1-2 کش شد) |
| `AnalyticsController::getActiveQuotasRemaining()` | بدون تغییر — ذاتاً روی همه چیز aggregate می‌کند |

**تأیید:** EXPLAIN روی `getLoadableTonnage` نشان داد ردیف‌های پردازش‌شده از ۵۸٬۵۵۹ به ۵۹ رسید.

---

### ✅ P0-4 | مشکل N+1 در `getWarehouseDetails()` — **انجام شد و تأیید شد**

کوئری تکراری داخل حلقه حذف شد؛ یک کوئری واحد قبل از حلقه همه‌ی تاریخ‌های خروج انبار را می‌گیرد.

**تأیید:** برای انباری با ۴ کوتاژ، تعداد کوئری از ۵ به ۲ کاهش یافت (برای ۴۰ کوتاژ یعنی ۴۱ → ۲).

---

### ✅ P0-5 | شرط‌های غیرقابل‌ایندکس روی تاریخ — **انجام شد و تأیید شد**
**محل:** `AppApiController::getFilteredSummary()`

`CONCAT(exitDate, ' ', exitTime) >= ?` به الگوی sargable تبدیل شد.

**تأیید:** EXPLAIN نشان داد `type=ref`, `Using index condition`؛ صحت مرز `>=`/`<` با تست عملی تأیید شد.

---

## 🟠 اولویت ۱ — مهم

### ✅ P1-1 | نبود فشرده‌سازی (gzip) — **انجام شد و تأیید شد**

gzip در همه‌ی ۷ کنترلر اضافه شد. باگ `output_buffering=4096` که مانع فعال‌شدن gzip می‌شد رفع شد.

**تأیید:** حجم `getShipsList` از ۸٬۶۰۱ بایت به ۱٬۵۵۹ بایت رسید (**~۸۲٪ کاهش**).

### ✅ P1-2 | نبود کش سمت سرور برای endpointهای پرتکرار — **انجام شد و تست شد**

یک کلاس کمکی جدید `App\Core\MicroCache` (کش APCu با TTL کوتاه، بدون خطا اگر APCu نباشد) ساخته و در این مسیرها استفاده شد:

| endpoint | TTL | جزئیات |
|---|---|---|
| `AppApiController::getShipsList()` (و `getRealTimeData` که از همان استفاده می‌کند) | ۸ ثانیه | فقط داده‌ی سنگین کش می‌شود؛ فیلد `timestamp` همیشه لحظه‌ای محاسبه می‌شود |
| `CargoService::getActiveShips()` | ۸ ثانیه | |
| `AnalyticsController::getRealTimeData()` (شیفتی) | ۵ ثانیه | به‌ازای هر شیفت (start/end date/time) جداگانه کش می‌شود |
| `ChatController::getUnreadCount()` | ۴ ثانیه | به‌ازای هر کاربر |
| `UtilityController::syncPermissions()` | ۱۵ ثانیه | فایل `permissions.json` یک‌بار برای همه‌ی کاربران کش می‌شود |

**تأیید:** با فراخوانی دوباره‌ی فوری، داده‌ی سنگین ثابت ماند ولی فیلد `timestamp` تغییر کرد (اثبات کش‌شدن بدون قربانی‌کردن تازگی نمایش زمان). تست مستقیم روی `getUnreadCount` به‌خاطر نبود جدول چت در دیتابیس تست ممکن نشد، ولی چون از همان تابع کش اثبات‌شده استفاده می‌کند با اطمینان بالا تکمیل در نظر گرفته شد.

### ✅ P1-3 | ساخت اتصال‌های تکراری دیتابیس — **انجام شد (نسخه‌ی کم‌ریسک)**

`config/config.php::getDbConnection()` هر بار یک اتصال mysqli جدید می‌ساخت. چون `PermissionManager.php` (تنها کاربر واقعی این تابع) `bootstrap.php`/autoloader را لود نمی‌کند، وصل‌کردنش به `App\Core\Database` ریسک داشت بدون سود واقعی (صفحه‌ی کم‌ترافیک ادمین). به‌جایش یک متغیر `static` داخل خودِ تابع اضافه شد تا در یک اجرای اسکریپت، اتصال فقط یک‌بار ساخته شود.

> نکته: `generateDailyStats.php` (کاربر دیگر همین تابع) به کلاس `CargoAPI` ارجاع می‌دهد که در پروژه وجود ندارد — این فایل احتمالاً کد مرده از معماری قدیمی است؛ دست نخورد.

### ✅ P1-4 | `new Request()` گران و در هر Controller ساخته می‌شود — **انجام شد و تست شد**

`parseJsonInput()` و `parseHeaders()` که همیشه در constructor اجرا می‌شدند (شامل `file_get_contents('php://input')` و `getallheaders()` روی هر درخواست GET بدون بدنه) حالا lazy هستند. بررسی شد `getHeader()` در کل پروژه هیچ‌جا استفاده نمی‌شود، پس آن مسیر عملاً دیگر اجرا نمی‌شود.

**تأیید:** تست GET با query params، GET با پارامتر، POST با بدنه JSON (هم از طریق `ChatController` و هم از طریق `Request::all()` در `CargoController`) — همه صحیح.

### ✅ P1-5 | `LIKE '%...'` با wildcard ابتدایی — **انجام شد و تأیید شد**
**محل:** `AppApiController::checkQuotaExistenceCargo()` (مسیر اسکن بارکد)

ستون محاسبه‌شده (Generated Column) اضافه شد:
```sql
ALTER TABLE InitialInfo ADD COLUMN loadingQuotaNumberReversed VARCHAR(100)
GENERATED ALWAYS AS (REVERSE(loadingQuotaNumber)) STORED;
CREATE INDEX idx_quota_reversed ON InitialInfo (loadingQuotaNumberReversed);
```

**تأیید:** ۳ سناریو (تطابق کامل، تطابق انتهایی، عدم تطابق) درست کار کردند؛ EXPLAIN نشان داد `type=range`, `key=idx_quota_reversed`.

> ⚠️ این تغییر ساختار دیتابیس هم فقط روی `cargo_test` اجرا شده — باید روی Production هم تکرار شود.

### ✅ P1-6 | کوئری بدون فیلتر کافی در `getInitialInfo()` — **انجام شد و تأیید شد**

فیلتر `loadingQuotaNumber` به کوئری `allTrackingNumbers` اضافه شد.

**تأیید:** تعداد `allTrackingNumbers` از عدد بزرگ نامشخص به ۵۹ (دقیقاً برابر voucherCount واقعی) رسید.

### ✅ P1-7 | الگوریتم‌های O(n²) در PHP — **انجام شد و تأیید شد**

همه‌ی `in_array()` داخل حلقه با کلید آرایه انجمنی جایگزین شدند.

### 🔶 P1-8 | `sanitizeInput()` روی مقادیر پارامتری SQL — **بررسی شد، تصمیم نهایی: بدون تغییر**

چون همه‌ی کوئری‌ها Prepared Statement هستند، خطر SQL Injection از اول صفر بود — این تابع فقط برای امنیت بی‌اثر بود، اما یک باگ بالقوه‌ی صحت داده دارد: مقادیری با `&`/`'`/`"` به شکل HTML-encoded شده جستجو می‌شوند. با کوئری زیر روی دیتابیس **واقعی (production)** بررسی شد که در حال حاضر **هیچ نام کشتی/شرکتی این کاراکترها را ندارد** — پس فعلاً بی‌خطر است، ولی برای ورودی‌های آینده باقی می‌ماند. **تصمیم شما:** فعلاً دست نخورد.

### ✅ P1-9 | نبود Pagination — **بررسی شد؛ به‌جایش یک باگ over-fetch واقعی پیدا و رفع شد**
**محل:** `AppApiController::getAllQuotasList()` / `getGroupedQuotas()`

بررسی نشون داد pagination کامل (offset/limit) روی این حجم داده (۳۰۸ ردیف `InitialInfo`) هنوز واقعاً لازم نیست و ریسک/حجم کار (تغییر هم‌زمان API + UI اندروید) بیشتر از سودشه. در عوض، حین بررسی یک مشکل واقعی و فعلی پیدا شد: `QuotaManagementDialog.kt` (اندروید) از قبل پارامتر `shipName` رو به `getGroupedQuotas` می‌فرستاد تا فقط کوتاژهای یک کشتی رو بگیره، ولی سرور این پارامتر رو **کاملاً نادیده می‌گرفت** و همیشه همه‌ی ۳۰۸ ردیف همه‌ی کشتی‌ها رو گروه‌بندی و برمی‌گردوند.

**اصلاح:** `getAllQuotasList(?string $shipName)` و `getGroupedQuotas(?string $shipName)` حالا پارامتر رو با prepared statement (`WHERE shipName = ?`) اعمال می‌کنن؛ اگه خالی/نال باشه رفتار قبلی (همه‌ی کشتی‌ها، برای نمای کلی) بدون تغییر می‌مونه.

**تأیید روی `test_api`:**
| حالت | نتیجه | حجم پاسخ |
|---|---|---|
| `shipName=ANDERMATT` | فقط همون کشتی | ۸٬۱۶۹ بایت (**۹۶٪ کاهش** از حالت قبل) |
| `shipName=` (خالی) | همه‌ی ۳۳ کشتی (رفتار قبلی) | ۱۹۹٬۰۱۸ بایت |
| بدون پارامتر `shipName` | همه‌ی ۳۳ کشتی (رفتار قبلی) | ۱۹۹٬۰۱۸ بایت |

سمت اندروید نیازی به build/تغییر جدید نداشت (از قبل پارامتر رو می‌فرستاد).

---

## 🟡 اولویت ۲ — بهبود

| # | مورد | وضعیت |
|---|------|------|
| P2-1 | `Logger` با `LOCK_EX` روی هر لاگ | ✅ **انجام شد و تست شد** — پیام‌ها در حافظه بافر و فقط یک‌بار در `register_shutdown_function` نوشته می‌شوند؛ با تست عملی (خطای عمدی) تأیید شد هر دو خط لاگ (`CargoService`+`CargoController`) درست نوشته شدند |
| P2-2 | `bootstrap.php` — `file_exists`×۲ + بارگذاری همیشگی `jdf.php` | 🔶 **بررسی شد، رد شد** — چون OPcache فعال است هزینه‌ی واقعی این مورد تقریباً صفر است؛ `jdf.php` هم توسط ۵ کنترلر اصلی استفاده می‌شود |
| P2-3 | autoloader سفارشی + Composer autoloader هر دو ثبت می‌شدند | ✅ انجام شد |
| P2-4 | `ensureTablesExist()` کد مرده | ✅ انجام شد |
| P2-5 | `getUnreadCount()` با `NOT EXISTS` روی کل جدول چت | ✅ **انجام شد** — به `MicroCache` منتقل شد (بخش P1-2 را ببینید) |
| P2-6 | زیرکوئری همبسته `GROUP_CONCAT` در چت | 🔶 **بررسی شد، رد شد** — چون حداکثر ۱۰۰ ردیف در هر فراخوانی محدود است، ریسک بازنویسی به JOIN بیشتر از سود اثبات‌شده‌اش است |
| P2-7 | `getMessages` همیشه ۱۰۰ پیام کامل برمی‌گرداند | 🔶 **بررسی شد، مشکل واقعی نیست** — کد از قبل از cursor (`lastMessageId`/`olderThanId`) استفاده می‌کند |
| P2-8 | `syncPermissions` هر بار `permissions.json` را می‌خواند | ✅ **انجام شد و تست شد** — کش شد (بخش P1-2)، با کاربر واقعی تست شد |
| P2-9 | تنظیمات OPcache (`validate_timestamps=1`) | ⏸️ **نیاز به تصمیم شما** — این یک تنظیم سراسری `php.ini` روی کل سرور است (ممکن است سرویس‌های دیگر را هم تحت تأثیر قرار دهد)، خارج از حیطه‌ی تغییرات کد این پروژه |
| P2-10 | `check_password.php` — `session_start()` هر بار | ✅ **بررسی شد، از قبل درست بود** — کد از قبل با `session_status() === PHP_SESSION_NONE` محافظت می‌شد |

---

# بخش دوم: اپلیکیشن اندروید

## 🔴 اولویت ۰ — بحرانی

### ✅ A0-1 | طوفان Polling — **انجام شد و تأیید شد (بخش عمده)**

| محل | وضعیت |
|-----|------|
| `ChatViewModel.kt` (۵ ثانیه) | ✅ با `repeatOnLifecycle(RESUMED)` |
| `SelectInfoScreen.kt` (۳۰ ثانیه، ۲ پولر) | ✅ یکی شدند + `repeatOnLifecycle` |
| `CargoCounterScreen.kt` (۳۰ ثانیه) | ✅ |
| `CargoDetailsScreen.kt` (۳۰ ثانیه) | ✅ |
| `PermissionPoller.kt` (۳ دقیقه) | ⏸️ بررسی/اصلاح نشد |
| `LoadingNotificationService.kt` | ⏸️ بررسی/اصلاح نشد (سرویس Foreground، ماهیتش فرق دارد) |

### ✅ A0-2 | دو Poller همزمان در `SelectInfoScreen` — **انجام شد و تأیید شد**

### ✅ A0-3 | `LazyColumn` بدون `key` — **بررسی و اصلاح شد**
۳ مورد واقعی پیدا و اصلاح شد (`SelectInfoScreen.kt` × ۲, `WarehouseDetailsScreen.kt`).

### ✅ A0-4 | محاسبات سنگین داخل Composition — **انجام شد و تأیید شد**

---

## 🟠 اولویت ۱ — مهم

### 🔶 A1-1 | نبود Baseline Profile — **زیرساخت آماده، تولید فایل ناتمام**

زیرساخت کامل (ماژول `baselineprofile/`, وابستگی‌ها, buildType `benchmark`) آماده است.

**🎁 یافته‌ی جانبی مهم:** حین تست این زیرساخت، باگ کرش بحرانی کشف و رفع شد — اپ روی دستگاه‌های با اندازه صفحه حافظه ۱۶KB (اندروید ۱۵+، شامل Pixel 8/9 به بعد) بلافاصله کرش می‌کرد (`UnsatisfiedLinkError: libsecrets.so program alignment`). با افزودن `target_link_options(secrets PRIVATE "-Wl,-z,max-page-size=16384")` در `CMakeLists.txt` رفع و تأیید شد.

**ناتمام مانده:** تولید خودِ فایل `baseline-prof.txt` با خطای غیرقطعی محیط (`Unable to confirm activity launch completion`) متوقف ماند — نیاز به اجرا روی ماشینی با Android Studio نصب‌شده دارد:
```bash
./gradlew :app:generateReleaseBaselineProfile
```

### ✅ A1-2 | ضدالگوی `LaunchedEffect { coroutineScope.launch { } }` — **بررسی سیستماتیک انجام شد، مشکلی پیدا نشد**
کل پروژه بررسی شد (۳۹ فایل با `LaunchedEffect`، ۱۲ فایل با ۳۳ مورد `coroutineScope.launch`). همه‌ی موارد `coroutineScope.launch` داخل context‌های غیر-suspend (`onClick`, `onRefresh`, `onConfirm` و مشابه) هستند — استفاده‌ی صحیح. این ضدالگو دیگر در کد وجود ندارد (احتمالاً طی بازنویسی SelectInfoScreen برطرف شده بود).

### ✅ A1-3 | تنظیمات OkHttp — **انجام شد و تست شد**
**محل:** `RetrofitClient.kt` (هر دو `RetrofitClient` و `ThirdPartyRetrofitClient`)

- **ConnectionPool** صریح اضافه شد (۱۰ اتصال idle، ۵ دقیقه) — چون چند صفحه هم‌زمان به همان هاست poll می‌کنند
- **timeout جدا شد:** `connectTimeout=10s` (fail-fast) در برابر `readTimeout`/`writeTimeout=30s` قبلی (همه ۳۰ ثانیه بودند)
- **کش OkHttp روی GET اضافه نشد:** بررسی شد سرور (`Response.php`) هدر `Cache-Control: no-store` روی همه‌ی پاسخ‌ها می‌فرستد، پس کش سمت کلاینت عملاً بی‌اثر می‌بود
- **`serializeNulls()` دست نخورد** — تصمیم شما بود چون تغییرش رفتار روی‌سیم درخواست‌های POST را عوض می‌کند

**تأیید:** build کامل + تست دستی روی گوشی (صفحات pollکننده و یک عملیات POST) — بدون مشکل.

### ✅ A1-4 | دسترسی مستقیم به `RetrofitClient` از داخل Composable — **انجام شد و تست شد**
**محل:** `SelectInfoScreen.kt`

۵ نقطه‌ی دسترسی مستقیم به `RetrofitClient.apiService` (شامل `fetchRealTimeData`, `loadActiveShips` و ۳ مورد دیگر) به `ReportsRepository` منتقل شدند. متد جدید `ReportsRepository.getActiveShips()` اضافه شد (متد `getRealTimeLoadingData` از قبل وجود داشت).

**تأیید:** build کامل + تست دستی کامل صفحه (لیست کشتی‌ها، بروزرسانی لحظه‌ای، دیالوگ کوتاژهای فعال).

### ✅ A1-5 | `targetSdk = 33` در حالی که `compileSdk = 36` — **انجام شد و تست شد (تا ۳۴)**

بررسی نشان داد این پروژه یک Foreground Service واقعی دارد (`LoadingNotificationService`) که بدون `foregroundServiceType` مشخص، از اندروید ۱۴ به بالا **کرش می‌کند**. برای رفع کامل و امن:
- `AndroidManifest.xml`: `android:foregroundServiceType="dataSync"` + مجوز `FOREGROUND_SERVICE_DATA_SYNC`
- `LoadingNotificationService.kt`: `startForeground()` به `ServiceCompat.startForeground(..., ServiceInfo.FOREGROUND_SERVICE_TYPE_DATA_SYNC)` تغییر کرد
- `targetSdk` از ۳۳ به **۳۴** (نه ۳۶ مستقیماً — قدم محتاط‌تر، هنوز فاصله با `compileSdk=36` باقی است اما این‌طور در آینده ارتقا تدریجی‌تر و کم‌ریسک‌تر است)

**تأیید:** build کامل، manifest merge بدون تداخل، تست دستی روی گوشی شامل ورود ادمین و نوتیفیکیشن دائمی سرویس — بدون کرش.

### ⏸️ A1-6 | فایل‌های عظیم Composable — **باقی‌مانده** (بازسازی ساختاری بزرگ، دست نخورد)

---

## 🟡 اولویت ۲ — بهبود (زمان build)

> توضیح: فهرست دقیق A2-1 تا A2-9 از یک نشست قبلی بود که فایلش در دسترس نبود؛ در این نشست پیکربندی build فعلی (`gradle.properties`, `app/build.gradle.kts`, `libs.versions.toml`) از نو بررسی و ۳ یافته‌ی واقعی پیدا و رفع شد. باقی موارد A2 (اگر وجود داشته باشند) هنوز بررسی نشده‌اند.

### ✅ A2-1 | مهاجرت `kapt` → `KSP` برای Room — **انجام شد و build شد**
**محل:** `app/build.gradle.kts`, `gradle/libs.versions.toml`, `build.gradle.kts`

`kotlin-kapt` (کند، مبتنی بر تولید stub با javac) حذف و با پلاگین `com.google.devtools.ksp` (نسخه‌ی `2.2.20-2.0.3`، سازگار با Kotlin 2.2.20) جایگزین شد. `kapt(libs.androidx.room.compiler)` → `ksp(libs.androidx.room.compiler)`.

**تأیید:** `./gradlew :app:compileDebugKotlin` — تسک `kspDebugKotlin` اجرا و `compileDebugKotlin` با موفقیت کامل شد؛ هیچ تسک `kapt*` دیگر در گراف build وجود ندارد.

### ✅ A2-2 | خاموش‌کردن Compose Compiler Metrics/Reports دائمی — **انجام شد**
**محل:** `gradle.properties`

دو فلگ `android.experimental.enableComposeCompilerMetrics`/`enableComposeCompilerReports` که همیشه (حتی روی release) فعال بودند و گزارش تشخیصی روی هر کامپایل تولید می‌کردند، حذف شدند. این‌ها ابزار تشخیصی موردی هستند؛ حالا فقط با پاس‌کردن صریح از خط فرمان فعال می‌شوند (راهنما به‌صورت کامنت در فایل باقی ماند).

### ✅ A2-3 | بررسی نیاز واقعی به Jetifier — **بررسی شد؛ غیرفعال شد**
**محل:** `gradle.properties`

`android.enableJetifier=true` فرض شده بود لازمه، ولی هیچ وابستگی پروژه از support-library قدیمی استفاده نمی‌کند. با `android.enableJetifier=false` هم `./gradlew :app:assembleDebug` و هم `./gradlew :app:assembleRelease` (شامل R8 minify، shrinkResources، و ماژول baselineprofile) **کامل و بدون خطای کلاس/منیفست گم‌شده** build شدند. غیرفعال نگه داشته شد.

**باقی‌مانده در این اولویت:** بررسی سایر بهینه‌سازی‌های احتمالی build-time (مثلاً تقسیم ماژول، بررسی نسخه‌ی AGP/Gradle — که در حال حاضر به‌روز هستند: AGP 8.13.0, Gradle 8.13, Kotlin 2.2.20) هنوز انجام نشده.

---

# ✅ خلاصه‌ی کارهای نشست دوم (۱۳ مرحله تکمیل‌شده)

1. بازنویسی کامل `protected_proxy.php` با whitelist/rate-limit در APCu + log buffering (P0-1)
2. micro-cache سمت سرور (`MicroCache`) برای ۵ endpoint پرتکرار (P1-2, P2-5, P2-8)
3. کش درون-درخواستی برای `getDbConnection()` (P1-3)
4. lazy کردن پارس JSON/هدر در `Request.php` (P1-4)
5. بررسی امنیت داده‌ای `sanitizeInput()` روی دیتابیس واقعی + تصمیم نهایی (P1-8)
6. بافرشدن لاگ + نوشتن یک‌جا در پایان اسکریپت (P2-1)
7. بررسی و رد کردن آگاهانه‌ی P2-2, P2-6, P2-9 (تأثیر کم یا نیاز به تصمیم/دسترسی جدا)
8. تأیید P2-7 و P2-10 که از قبل مشکلی نداشتند
9. تنظیمات OkHttp: ConnectionPool + timeout جدا (A1-3)
10. حذف دسترسی مستقیم به RetrofitClient از Composable (A1-4)
11. بالابردن targetSdk به ۳۴ + رفع کرش بالقوه‌ی Foreground Service (A1-5)
12. بررسی سیستماتیک کل پروژه برای ضدالگوی coroutine — تأیید عدم وجود مشکل (A1-2)
13. تمام موارد بالا روی `test_api` یا با build+تست دستی روی گوشی تأیید شدند

---

# ✅ خلاصه‌ی کارهای نشست سوم (۴ مرحله تکمیل‌شده)

1. رفع باگ over-fetch در `getGroupedQuotas` — فیلتر `shipName` که اندروید می‌فرستاد و سرور نادیده می‌گرفت، حالا اعمال می‌شود (P1-9؛ ۹۶٪ کاهش حجم پاسخ در حالت فیلترشده) — تست‌شده روی `test_api`
2. مهاجرت `kapt` → `KSP` برای Room annotation processing (A2-1) — build شده
3. حذف فلگ‌های همیشه‌فعال Compose Compiler Metrics/Reports (A2-2)
4. غیرفعال‌کردن Jetifier پس از تأیید عدم نیاز با build کامل debug و release (A2-3)

---

# 📋 باقی‌مانده — اولویت‌بندی پیشنهادی

## کارهای فوری (قبل از هر چیز)
- [ ] **دیپلوی همه‌ی فایل‌های تست‌شده‌ی این سه نشست روی production** (لیست کامل پایین صفحه)
- [ ] **اجرای ایندکس‌های P0-2 و ستون/ایندکس P1-5 روی دیتابیس Production** (هنوز فقط روی `cargo_test`!)
- [ ] تکمیل تولید Baseline Profile روی ماشینی با Android Studio (`./gradlew :app:generateReleaseBaselineProfile`)
- [ ] **مهم:** نسخه‌ی جدید اپ (با رفع باگ کرش ۱۶KB) باید هرچه زودتر منتشر شود

## ادامه‌ی کار — بررسی‌شده و تصمیم/کار مشخصی روشون مونده
- [ ] A1-6: بازسازی فایل‌های عظیم Composable (کار بزرگ ساختاری)
- [ ] P2-9: تصمیم‌گیری درباره‌ی تنظیمات سراسری OPcache (`validate_timestamps`)
- [ ] P2-6: اگه در آینده حجم پیام‌های چت رشد قابل‌توجهی کرد، بازبینی `GROUP_CONCAT` (فعلاً آگاهانه رد شد)

## ⚠️ کاملاً بررسی/شروع نشده (نه در این ۳ نشست لمس شده‌اند)
- [ ] **`PermissionPoller.kt`** (پولینگ هر ۳ دقیقه) — از A0-1 جا مونده، اصلاً بررسی نشده که آیا نیاز به `repeatOnLifecycle` داره یا نه
- [ ] **`LoadingNotificationService.kt`** به‌عنوان یک Foreground Service — فقط از نظر crash روی اندروید ۱۴+ بررسی شد (A1-5)؛ خودِ منطق polling/عملکردش بررسی نشده
- [ ] **موارد A2-4 تا A2-9** (در صورت وجود) — فهرست دقیقشون از نشست اول در دسترس نیست؛ در این نشست فقط ۳ یافته‌ی جدید (kapt→KSP، Compose metrics، Jetifier) بررسی و رفع شد؛ بررسی عمیق‌تر build-time (مثل تقسیم ماژول‌ها، lint سنگین، یا سایر annotation processorها) انجام نشده
- [ ] فایل مشکوک `generateDailyStats.php` (اشاره‌شده در P1-3) که به کلاس ناموجود `CargoAPI` ارجاع می‌ده — احتمالاً کد مرده، ولی هرگز رسماً بررسی/حذف نشده
- [ ] بررسی دیتابیس Production به‌طور کلی (غیر از دو مورد ایندکس بالا) — همه‌ی تست‌ها فقط روی `cargo_test` انجام شده؛ سلامت/حجم واقعی دیتابیس Production بررسی نشده

## 📦 فایل‌های آماده‌ی دیپلوی روی Production (تست‌شده روی `test_api`)

**PHP:**
- `PHP/protected_proxy.php`
- `PHP/src/Core/MicroCache.php` (جدید)
- `PHP/src/Core/Request.php`
- `PHP/src/Core/Logger.php`
- `PHP/config/config.php`
- `PHP/src/Controllers/AppApiController.php` (شامل فیکس P1-9 نشست سوم)
- `PHP/src/Controllers/AnalyticsController.php`
- `PHP/src/Controllers/ChatController.php`
- `PHP/src/Controllers/UtilityController.php`
- `PHP/src/Services/CargoService.php`

پس از آپلود، روی سرور production اجرا شود: `composer dump-autoload -o` (چون `MicroCache` کلاس جدیدی است و `optimize-autoloader` فعال است).

**Android:** یک build کامل release جدید شامل تمام تغییرات این و نشست‌های قبلی (به‌خصوص رفع کرش ۱۶KB، targetSdk 34، و تغییرات build-time نشست سوم که فقط سرعت build رو تحت تأثیر قرار می‌دن نه رفتار اپ).

# گزارش بررسی عملکرد و بهینه‌سازی — ATK-Cargo

**دامنه بررسی:** کل کد PHP سمت سرور (`PHP/`, ~۷٬۷۰۰ خط) و کل اپلیکیشن اندروید (`app/`, ~۵۱٬۸۰۰ خط Kotlin)
**هدف:** افزایش سرعت پاسخ‌گویی سرور و روانی اپلیکیشن، **با حفظ کامل منطق و رفتار فعلی**
**تاریخ گزارش اولیه:** ۱۴۰۵/۰۵/۱۷ (۲۰۲۶-۰۸-۰۸)
**آخرین به‌روزرسانی:** ۱۴۰۵/۰۵/۱۷ (۲۰۲۶-۰۸-۰۸) — پس از یک نشست کامل اجرا

---

## 📊 خلاصه وضعیت (به‌روز)

| بخش | انجام‌شده | باقی‌مانده |
|---|---|---|
| PHP — اولویت ۰ (بحرانی) | ۴ از ۵ | P0-1 (`protected_proxy.php`) |
| PHP — اولویت ۱ (مهم) | ۳ از ۹ | P1-2, 3, 4, 8, 9 |
| PHP — اولویت ۲ (بهبود) | ۲ از ۱۰ | P2-1, 2, 5, 6, 7, 8, 9, 10 |
| اندروید — اولویت ۰ (بحرانی) | ۴ از ۴ | — |
| اندروید — اولویت ۱ (مهم) | نیمه (زیرساخت آماده) | تولید Baseline Profile، A1-2 تا A1-6 |
| اندروید — اولویت ۲ (build) | ۰ از ۹ | همه |
| 🎁 یافته‌ی جانبی | یک باگ کرش بحرانی پیدا و رفع شد | — |

همه‌ی موارد انجام‌شده روی سرور واقعی (`atk-nk.ir`) تست و با EXPLAIN/مقایسه‌ی مقادیر تأیید شدند.

---

# بخش اول: سمت سرور (PHP)

## 🔴 اولویت ۰ — بحرانی

### ⏸️ P0-1 | `protected_proxy.php`: هزینه ثابت روی تک‌تک درخواست‌ها — **باقی‌مانده**
**محل:** `PHP/protected_proxy.php`

تمام درخواست‌های اپلیکیشن بدون استثنا از این فایل عبور می‌کنند. کد (obfuscate شده اما منطقش کاملاً روشن است) روی **هر** درخواست این کارها را انجام می‌دهد:

1. `is_dir("log")` و در صورت نبود، `mkdir`
2. `glob("*.php")` — لیست کردن کل دایرکتوری برای ساخت whitelist
3. خواندن فایل blocklist با `file()`
4. **Rate limit:** خواندن `log/rate_limit_$ip.txt` → `json_decode` → فیلتر → `json_encode` → **نوشتن مجدد فایل**
5. **Log:** `file_put_contents(..., FILE_APPEND | LOCK_EX)` روی یک فایل مشترک
6. `ob_start()` + `include $target` + `ob_get_contents()`

**اثر:**
- حداقل ۵ عملیات I/O قبل از اینکه یک خط کد منطقی اجرا شود.
- `LOCK_EX` روی یک فایل لاگ مشترک یعنی **تمام درخواست‌های همزمان پشت یک قفل صف می‌کشند**.
- فایل‌های `log/rate_limit_*.txt` هرگز پاک نمی‌شوند.

**راه‌حل پیشنهادی (بدون تغییر رفتار):**
- whitelist را از `glob` به یک آرایه ثابت (یا کش `APCu`/فایل کش با TTL) تبدیل کنیم.
- Rate limit را به APCu / Redis (یا حداقل `shmop`) منتقل کنیم.
- لاگ را buffer کنیم یا از `error_log` استفاده کنیم.
- روتیشن + پاکسازی خودکار فایل‌های rate limit.

> **نکته مهم کشف‌شده امروز:** بررسی کردیم که gzip از طریق این پروکسی هم به‌درستی کار می‌کند (چون `exit` داخل کنترلرها، بافرهای تودرتو را به‌درستی flush می‌کند) — پس اصلاح gzip در خود این فایل لازم نبود و کاری هم انجام نشد.

---

### ✅ P0-2 | نبود ایندکس روی جداول — **انجام شد و تأیید شد**
**محل:** دیتابیس `cargo_test` (و باید روی دیتابیس Production هم تکرار شود)

بررسی واقعی نشان داد `CargoInfo` با ۵۸٬۵۵۹ ردیف **هیچ ایندکسی به‌جز PRIMARY KEY نداشت**. این ۵ ایندکس اضافه و تأیید شدند:

```sql
CREATE INDEX idx_cargo_quota_lookup ON CargoInfo (loadingQuotaNumber, shipName, loadingWarehouse, shippingCompany, cargoType, status);
CREATE INDEX idx_cargo_status_ship ON CargoInfo (status, shipName, loadingQuotaNumber, loadingWarehouse, shippingCompany, cargoType);
CREATE INDEX idx_cargo_tracking ON CargoInfo (trackingNumber);
CREATE INDEX idx_cargo_scale_receipt ON CargoInfo (scaleReceiptNumber);
CREATE INDEX idx_cargo_exit_datetime ON CargoInfo (exitDate, exitTime);
```

**تأیید با EXPLAIN:** کوئری‌های سنگین از `type=ALL` (اسکن کامل ۵۸هزار ردیف) به `type=ref`/`range` (تا ۵۹ ردیف) تغییر کردند.

> ⚠️ **مهم برای فردا:** این ایندکس‌ها فقط روی دیتابیس **تستی (`cargo_test`)** ساخته شدند. باید **روی دیتابیس Production هم اجرا شوند** (همان ۵ دستور بالا، از طریق phpMyAdmin سرور اصلی).

---

### ✅ P0-3 | کوئری‌های Aggregate بدون فیلتر — **انجام شد و تأیید شد**
**محل:** `PHP/src/Controllers/AppApiController.php`

| متد | وضعیت |
|-----|------|
| `getLoadableTonnage()` | ✅ اصلاح شد — فیلتر `loadingQuotaNumber` به داخل derived table منتقل شد |
| `getQuotaDetails()` | ✅ اصلاح شد |
| `getShipDetails()` | ✅ اصلاح شد — فیلتر `shipName` به داخل derived table منتقل شد |
| `getShipsList()` | بدون تغییر — این متد ذاتاً روی همه‌ی کشتی‌ها Aggregate می‌کند (فیلتری برای push کردن وجود ندارد)؛ با ایندکس P0-2 به‌اندازه‌ی کافی سریع شد |
| `AnalyticsController::getActiveQuotasRemaining()` | بدون تغییر — همان دلیل بالا |

**تأیید:** EXPLAIN روی `getLoadableTonnage` نشان داد ردیف‌های پردازش‌شده از ۵۸٬۵۵۹ به ۵۹ رسید.

---

### ✅ P0-4 | مشکل N+1 در `getWarehouseDetails()` — **انجام شد و تأیید شد**

کوئری تکراری داخل حلقه حذف شد؛ یک کوئری واحد قبل از حلقه همه‌ی تاریخ‌های خروج انبار را می‌گیرد و در PHP بر اساس `loadingQuotaNumber` گروه‌بندی می‌شود.

**تأیید:** برای انباری با ۴ کوتاژ، تعداد کوئری از ۵ به ۲ کاهش یافت (برای ۴۰ کوتاژ یعنی ۴۱ → ۲).

---

### ✅ P0-5 | شرط‌های غیرقابل‌ایندکس روی تاریخ — **انجام شد و تأیید شد**
**محل:** `AppApiController::getFilteredSummary()`

`CONCAT(exitDate, ' ', exitTime) >= ?` به الگوی sargable تبدیل شد:
```sql
(exitDate > ? OR (exitDate = ? AND exitTime >= ?)) AND (exitDate < ? OR (exitDate = ? AND exitTime < ?))
```

**تأیید:** EXPLAIN نشان داد `type=ref`, `key=idx_cargo_quota_lookup`, `Using index condition` (قبلاً امکان استفاده از هیچ ایندکسی نبود). همچنین صحت مرز `>=`/`<` با تست عملی (کاهش صحیح voucherCount هنگام کوتاه‌کردن بازه) تأیید شد.

---

## 🟠 اولویت ۱ — مهم

### ✅ P1-1 | نبود فشرده‌سازی (gzip) — **انجام شد و تأیید شد**

gzip در همه‌ی ۷ کنترلر (`AppApiController`, `AnalyticsController`, `CargoController`, `ChatController`, `LicenseController`, `OnlineUsersController`, `UtilityController`) و ۲ مسیر پاسخ مستقیم اضافه شد.

> **کشف مهم حین کار:** سرور تنظیم `output_buffering=4096` در php.ini دارد که باعث می‌شد شرط محافظتی اولیه (`ob_get_level()===0`) همیشه false شود و gzip هرگز فعال نشود — این باگ (که در کد قدیمی هم وجود داشت، نه فقط کد جدید) با تغییر شرط به `!in_array('ob_gzhandler', ob_list_handlers(), true)` رفع شد.

**تأیید:** حجم `getShipsList` از ۸٬۶۰۱ بایت به ۱٬۵۵۹ بایت رسید (**~۸۲٪ کاهش**)، هم مستقیم و هم از طریق `protected_proxy.php`.

### ⏸️ P1-2 | `Cache-Control: no-store` + نبود کش سمت سرور — **باقی‌مانده**
`Core/Response.php:22` روی تمام پاسخ‌ها `no-store, no-cache` می‌گذارد. یک micro-cache ۵ تا ۱۰ ثانیه‌ای (APCu یا فایل) برای `getShipsList`, `getActiveShips`, `getRealTimeData` می‌تواند بار دیتابیس را چند برابر کم کند.

### ⏸️ P1-3 | ساخت اتصال‌های تکراری دیتابیس — **باقی‌مانده**
`Database.php` هم mysqli هم PDO نگه می‌دارد؛ `config.php::getDbConnection()` یک اتصال سوم مستقل می‌سازد.

### ⏸️ P1-4 | `new Request()` گران و در هر Controller ساخته می‌شود — **باقی‌مانده**
`file_get_contents('php://input')` حتی برای GET بدون بدنه اجرا می‌شود؛ باید lazy/singleton شود.

### ✅ P1-5 | `LIKE '%...'` با wildcard ابتدایی — **انجام شد و تأیید شد**
**محل:** `AppApiController::checkQuotaExistenceCargo()` (مسیر اسکن بارکد)

راه‌حل: ستون محاسبه‌شده (Generated Column) اضافه شد:
```sql
ALTER TABLE InitialInfo ADD COLUMN loadingQuotaNumberReversed VARCHAR(100)
GENERATED ALWAYS AS (REVERSE(loadingQuotaNumber)) STORED;
CREATE INDEX idx_quota_reversed ON InitialInfo (loadingQuotaNumberReversed);
```
کوئری از `LIKE '%x'` (غیرقابل‌ایندکس) به `LIKE 'reverse(x)%'` روی ستون معکوس تبدیل شد.

**تأیید:** ۳ سناریو (تطابق کامل، تطابق انتهایی، عدم تطابق) درست کار کردند؛ EXPLAIN نشان داد `type=range`, `key=idx_quota_reversed`.

> ⚠️ این تغییر ساختار دیتابیس هم فقط روی `cargo_test` اجرا شده — باید روی Production هم تکرار شود.

### ✅ P1-6 | کوئری بدون فیلتر کافی در `getInitialInfo()` — **انجام شد و تأیید شد**
**محل:** `CargoController.php` (مسیر باز صفحه ثبت حواله)

فیلتر `loadingQuotaNumber` به کوئری `allTrackingNumbers` اضافه شد (قبلاً فقط بر اساس شرکت+انبار+نوع‌کالا بود و کل تاریخچه را می‌گرفت).

**تأیید:** تعداد `allTrackingNumbers` از عدد بزرگ نامشخص به ۵۹ (دقیقاً برابر voucherCount واقعی همان کوتاژ) رسید. همچنین بررسی شد که این فیلد در اندروید اصلاً جایی خوانده نمی‌شود (داده‌ی مرده) — پس تغییر کاملاً بی‌خطر بود.

### ✅ P1-7 | الگوریتم‌های O(n²) در PHP — **انجام شد و تأیید شد**
**محل:** `getWarehouseDetails()` و `getFilteredSummary()` در `AppApiController.php`

همه‌ی `in_array()` داخل حلقه با کلید آرایه انجمنی (`$seen[$value]=true`) جایگزین شدند (O(n) به‌جای O(n²)).

**تأیید:** خروجی JSON (شامل ترتیب و محتوای `cargoTypes`, `shippingCompanies`, `operatorCount`) کاملاً با قبل یکسان ماند.

### ⏸️ P1-8 | `sanitizeInput()` روی مقادیر پارامتری SQL — **عمداً باقی‌مانده**
تصمیم گرفته شد چون می‌تواند رفتار را تغییر دهد (مقادیر حاوی `'` یا `&`)، نیاز به بررسی دقیق‌تر و تأیید صریح شما دارد.

### ⏸️ P1-9 | نبود Pagination — **عمداً باقی‌مانده**
تصمیم گرفته شد چون نیاز به تغییر هم‌زمان API سرور و UI اندروید دارد (تغییر بزرگ‌تر)، و با حجم داده‌ی فعلی (کل `InitialInfo` فقط ۳۰۸ ردیف) هنوز حاد نیست.

---

## 🟡 اولویت ۲ — بهبود

| # | مورد | وضعیت |
|---|------|------|
| P2-1 | `Logger` با `LOCK_EX` روی هر لاگ | ⏸️ باقی‌مانده |
| P2-2 | `bootstrap.php` — `file_exists`×۲ + بارگذاری همیشگی `jdf.php` | ⏸️ باقی‌مانده |
| P2-3 | autoloader سفارشی + Composer autoloader هر دو ثبت می‌شدند | ✅ **انجام شد** — autoloader سفارشی فقط به‌عنوان fallback (وقتی Composer نیست) اجرا می‌شود |
| P2-4 | `ensureTablesExist()` کد مرده | ✅ **انجام شد** — حذف شد؛ تست شد چت هنوز درست کار می‌کند |
| P2-5 | `getUnreadCount()` با `NOT EXISTS` روی کل جدول چت | ⏸️ باقی‌مانده |
| P2-6 | زیرکوئری همبسته `GROUP_CONCAT` در چت | ⏸️ باقی‌مانده |
| P2-7 | `getMessages` همیشه ۱۰۰ پیام کامل برمی‌گرداند | ⏸️ باقی‌مانده |
| P2-8 | `syncPermissions` هر بار `permissions.json` را می‌خواند | ⏸️ باقی‌مانده |
| P2-9 | بررسی فعال بودن OPcache روی سرور | ⏸️ باقی‌مانده (طی بررسی دیدیم OPcache فعال است با `validate_timestamps=1`، اما بررسی تنظیمات بهینه هنوز انجام نشده) |
| P2-10 | `check_password.php` — `session_start()` هر بار | ⏸️ باقی‌مانده |

---

# بخش دوم: اپلیکیشن اندروید

## 🔴 اولویت ۰ — بحرانی

### ✅ A0-1 | طوفان Polling — **انجام شد و تأیید شد (بخش عمده)**

| محل | وضعیت |
|-----|------|
| `ChatViewModel.kt` (۵ ثانیه) | ✅ با `repeatOnLifecycle(RESUMED)` — `startPolling()`/`pausePolling()` عمومی شدند |
| `SelectInfoScreen.kt` (۳۰ ثانیه، ۲ پولر) | ✅ یکی شدند + `repeatOnLifecycle` |
| `CargoCounterScreen.kt` (۳۰ ثانیه) | ✅ |
| `CargoDetailsScreen.kt` (۳۰ ثانیه) | ✅ |
| `PermissionPoller.kt` (۳ دقیقه) | ⏸️ بررسی/اصلاح نشد |
| `LoadingNotificationService.kt` | ⏸️ بررسی/اصلاح نشد (سرویس Foreground، ماهیتش فرق دارد) |

**تأیید:** با تست دستی روی گوشی، هر صفحه فقط ۱ درخواست به‌جای ۲ می‌زند، و پولینگ در پس‌زمینه اپ متوقف می‌شود.

### ✅ A0-2 | دو Poller همزمان در `SelectInfoScreen` — **انجام شد و تأیید شد**
در حین اصلاح A0-1 رفع شد (بالا را ببینید).

### ✅ A0-3 | `LazyColumn` بدون `key` — **بررسی و اصلاح شد**
بررسی دقیق نشان داد شمارش اولیه‌ی گزارش (۳۳ مورد) به‌خاطر الگوی جست‌وجوی تک‌خطی نادرست بود؛ اکثر لیست‌ها از قبل چندخطی و دارای key بودند. فقط **۳ مورد واقعی** پیدا و اصلاح شد:
- `SelectInfoScreen.kt` — لیست کشتی‌های گروه‌بندی‌شده
- `SelectInfoScreen.kt` — لیست کوتاژهای فیلترشده
- `WarehouseDetailsScreen.kt` — چیپ‌های کوتاژ

### ✅ A0-4 | محاسبات سنگین داخل Composition — **انجام شد و تأیید شد**
- `SelectInfoScreen.kt`: `filteredShips`/`groupedShips` اصلی + `shipRealTimeData` داخل `item{}` + ۵ محاسبه‌ی `ActiveQuotasDialog` همگی به `remember` منتقل شدند.
- `ReportsViewModel.updateFilteredQuotas()`: الگوی O(n²) (`groupBy→map→sorted→flatMap{filter}`) به O(n) اصلاح شد (استفاده از خروجی خودِ `groupBy` به‌جای فیلتر مجدد).

---

## 🟠 اولویت ۱ — مهم

### 🔶 A1-1 | نبود Baseline Profile — **زیرساخت آماده، تولید فایل ناتمام**

کارهای انجام‌شده:
- ماژول جدید `baselineprofile/` ساخته شد (پلاگین `androidx.baselineprofile`, وابستگی‌های macrobenchmark/uiautomator/profileinstaller)
- `app/build.gradle.kts`: buildType اختصاصی `benchmark` (بدون دست‌زدن به امضای release واقعی) + `implementation(libs.androidx.profileinstaller)`
- `settings.gradle.kts`, `gradle/libs.versions.toml`, `build.gradle.kts` (ریشه) به‌روزرسانی شدند

**🎁 یافته‌ی جانبی مهم (خارج از گزارش اصلی):** حین اجرای Baseline Profile روی امولاتور Pixel، این خطای واقعی و بحرانی کشف شد:
```
UnsatisfiedLinkError: dlopen failed: libsecrets.so program alignment (4096)
cannot be smaller than system page size (16384)
```
یعنی **اپ روی هر دستگاه با اندازه صفحه حافظه ۱۶ کیلوبایتی (استاندارد رو به رشد اندروید ۱۵+، شامل Pixel 8/9 به بعد) بلافاصله در استارتاپ کرش می‌کند.** با افزودن این خط در `app/src/main/cpp/CMakeLists.txt` رفع و تأیید شد:
```cmake
target_link_options(secrets PRIVATE "-Wl,-z,max-page-size=16384")
```

**ناتمام مانده:** تولید فعلی خودِ فایل `baseline-prof.txt` با خطای غیرقطعی (`Unable to confirm activity launch completion`) در محیط تست (امولاتور/گوشی از راه دور) متوقف ماند — این یک مشکل تایمینگ/منابع محیط اجرا به‌نظر می‌رسد، نه چیزی که با کد بیشتر حل شود. **باید روی ماشینی با Android Studio نصب‌شده (مدیریت بومی امولاتور) این دستور اجرا شود:**
```bash
./gradlew :app:generateReleaseBaselineProfile
```

### ⏸️ A1-2 | ضدالگوی `LaunchedEffect { coroutineScope.launch { } }` — **باقی‌مانده**
(تا حدی طی بازنویسی SelectInfoScreen برای A0-1/A0-4 اصلاح شد، اما بررسی سیستماتیک کل پروژه انجام نشده)

### ⏸️ A1-3 | تنظیمات OkHttp — **باقی‌مانده**
بدون Cache، بدون تنظیم ConnectionPool، timeout یکسان، `serializeNulls()` بی‌دلیل.

### ⏸️ A1-4 | دسترسی مستقیم به `RetrofitClient` از داخل Composable — **باقی‌مانده**
(حتی بعد از بازسازی A0-1، `fetchRealTimeData()` هنوز مستقیماً `RetrofitClient.apiService` صدا می‌زند، نه از طریق Repository)

### ⏸️ A1-5 | `targetSdk = 33` در حالی که `compileSdk = 36` — **باقی‌مانده**

### ⏸️ A1-6 | فایل‌های عظیم Composable — **باقی‌مانده** (بازسازی ساختاری بزرگ)

---

## 🟡 اولویت ۲ — بهبود (زمان build)

همه‌ی موارد A2-1 تا A2-9 **باقی‌مانده** (بدون تغییر نسبت به گزارش اولیه).

---

# ✅ خلاصه‌ی کارهای امروز (۱۴ مرحله تکمیل‌شده)

1. ایندکس‌های دیتابیس روی `CargoInfo` (P0-2)
2. gzip روی همه‌ی endpointها + رفع باگ `output_buffering` (P1-1)
3. اصلاح کوئری‌های Aggregate (P0-3، ۳ از ۵ متد)
4. رفع N+1 در `getWarehouseDetails` (P0-4)
5. اصلاح شرط تاریخ غیرقابل‌ایندکس (P0-5)
6. اصلاح الگوریتم‌های O(n²) در PHP (P1-7)
7. طوفان Polling در `SelectInfoScreen` (A0-1, A0-2)
8. key‌گذاری LazyColumnها (A0-3)
9. طوفان Polling در `ChatViewModel`, `CargoCounterScreen`, `CargoDetailsScreen` (A0-1)
10. انتقال محاسبات به `remember` + اصلاح O(n²) در ReportsViewModel (A0-4)
11. رفع LIKE با wildcard ابتدایی (P1-5)
12. رفع فیلتر ناقص در `getInitialInfo` (P1-6)
13. پاک‌سازی autoloader و کد مرده (P2-3, P2-4)
14. زیرساخت Baseline Profile + **کشف و رفع باگ کرش ۱۶KB page size** (A1-1، جزئی)

---

# 📋 برای فردا — اولویت‌بندی پیشنهادی

## کارهای فوری (قبل از هر چیز)
- [ ] **اجرای ایندکس‌های P0-2 و ستون/ایندکس P1-5 روی دیتابیس Production** (فقط روی `cargo_test` انجام شده بود!)
- [ ] تکمیل تولید Baseline Profile روی ماشینی با Android Studio (`./gradlew :app:generateReleaseBaselineProfile`)
- [ ] **مهم:** نسخه‌ی جدید اپ (با رفع باگ کرش ۱۶KB) باید هرچه زودتر منتشر شود — این یک باگ واقعی کاربران است

## ادامه‌ی کار طبق اولویت گزارش
- [ ] P0-1: بازنویسی `protected_proxy.php` (whitelist cache, rate limit APCu, log buffering)
- [ ] P1-2: micro-cache سمت سرور برای endpointهای پرتکرار
- [ ] P1-3/P1-4: یکپارچه‌سازی اتصال دیتابیس + lazy کردن Request
- [ ] A1-3/A1-4: تنظیمات OkHttp + عبور از RetrofitClient مستقیم در Composable
- [ ] A1-5: بالابردن targetSdk
- [ ] تصمیم‌گیری درباره P1-8 (sanitizeInput) و P1-9 (Pagination) — این دو عمداً معلق ماندند و نیاز به تصمیم شما دارند
- [ ] موارد P2 و A2 (پاک‌سازی و بهبود زمان build) — کم‌ریسک، هر وقت وقت بود

# گزارش بررسی جامع صفحه «ثبت و خروج حواله»

**تاریخ:** ۱۴۰۵/۰۵/۲۲ (2026-08-13)
**دامنه بررسی:** اندروید + سمت سرور (PHP)

## فایل‌های بررسی‌شده

**اندروید**
- `app/src/main/java/com/atk/atk_cargo/feature/cargo_registration/presentation/RegisterCargoScreen.kt` (۹۰۳ خط)
- `app/src/main/java/com/atk/atk_cargo/ui/viewmodel/CargoViewModel.kt` (۱۰۳۷ خط)
- `.../components/RegisterCargoForm.kt`، `RegisterCargoDialogs.kt`، `CargoInfoDetailsDialogSection.kt`
- `app/src/main/java/com/atk/atk_cargo/feature/cargo/domain/QuotaValidationUseCase.kt`
- `app/src/main/java/com/atk/atk_cargo/api/RetrofitClient.kt`، `ApiService.kt`، `data/model/CargoModels.kt`
- `app/src/main/java/com/atk/atk_cargo/data/repository/ReportsRepository.kt`

**سرور**
- `PHP/protected_proxy.php`، `PHP/src/bootstrap.php`
- `PHP/src/Controllers/CargoController.php`، `UtilityController.php`، `AnalyticsController.php`، `AppApiController.php`
- `PHP/src/Services/CargoService.php`، `PHP/src/Repositories/CargoRepository.php`
- `PHP/src/Core/Request.php`، `Database.php`

---

## خلاصه اجرایی

| اولویت | تعداد | ماهیت |
|---|---|---|
| **P0 — بحرانی** | ۵ | نبود کامل احراز هویت و مجوز روی مسیر ثبت/خروج/حذف، هویت جعل‌پذیر، شکست کامل یک جریان کاربری، رِیس‌کاندیشن تولید رکورد تکراری |
| **P1 — بالا** | ۷ | اجرای کنترل‌های کسب‌وکار فقط سمت کلاینت، بی‌اثر بودن محدودیت brute-force، پیام موفقیت دروغین، نشت coroutine |
| **P2 — متوسط** | ۱۴ | چهار برابر شدن بی‌دلیل ترافیک شبکه، محاسبات سنگین در بدنهٔ Composable، ~۱۲۰ خط کد مرده، ناسازگاری قواعد اعتبارسنجی |
| **P3 — کم** | ۸ | لاگ، کش، پاک‌سازی، جزئیات پیاده‌سازی |

**سه یافتهٔ مهم که باید فوراً دیده شوند:**

1. **هیچ‌کدام از endpointهای این صفحه احراز هویت ندارند.** هر کسی که آدرس سرور را داشته باشد می‌تواند بدون ورود به سیستم حواله ثبت کند، خروج بزند یا حواله حذف کند. الگوی درست در همین پروژه وجود دارد (`AppApiController.php:41`) اما در `CargoController` استفاده نشده است.
2. **دیالوگ «تأیید ثبت تکراری» به دلیل یک ناسازگاری نام‌گذاری JSON هرگز نمایش داده نمی‌شود.** سرور `requires_confirmation` می‌فرستد، مدل کلاینت `requiresConfirmation` انتظار دارد. کل جریان تأیید تکراری مرده است.
3. **هر بار بروزرسانی لیست، چهار درخواست شبکه می‌زند که دوتای آن دقیقاً یکسان است** — و سقف نرخ پروکسی ۶۰ درخواست در دقیقه به ازای هر IP است.

---

# P0 — بحرانی

## P0-1. تمام endpointهای ثبت، خروج، ویرایش و حذف حواله بدون احراز هویت هستند

**شواهد:**

`PHP/protected_proxy.php` تنها گِیت جلوی این APIهاست و فقط سه کار می‌کند: بررسی لیست IPهای مسدود (خط ۱۸۰)، محدودیت نرخ ۶۰ درخواست در دقیقه (خط ۱۸۷)، و بررسی اینکه `target` در whitelist باشد (خط ۲۰۵). **هیچ بررسی هویتی وجود ندارد.**

`PHP/src/bootstrap.php` هم فقط autoloader و timezone را تنظیم می‌کند.

`CargoController` — که `saveOrUpdate`، `updateCargoInfo`، `confirmCargo`، `deleteCargoInfo`، `getInitialInfo`، `checkScaleReceipt` را سرو می‌کند — هیچ‌جا نشست را بررسی نمی‌کند.

در مقابل، الگوی درست در همین پروژه پیاده شده است:

```php
// PHP/src/Controllers/AppApiController.php:41
private function requireAuthenticatedSession(): void {
    $username = (string)($this->request->getHeader('X-Username') ?? '');
    $deviceId = (string)($this->request->getHeader('X-Device-Id') ?? '');
    $token    = (string)($this->request->getHeader('X-Session-Token') ?? '');
    if (!$this->sessionService->isValidToken($username, $deviceId, $token)) {
        http_response_code(401); ... exit;
    }
}
```

کلاینت هم این هدرها را روی **همهٔ** درخواست‌ها می‌فرستد (`RetrofitClient.kt:92-94`) — یعنی زیرساخت آماده است و فقط سمت سرور مصرف نمی‌شود.

**تأثیر:** یک درخواست ساده مثل زیر بدون هیچ اعتبارنامه‌ای رکورد تجاری ثبت می‌کند:

```
POST /protected_proxy.php?target=saveOrUpdateCargoInfo.php
{"shipName":"...","trackingNumber":"...","username":"admin","userType":"admin", ...}
```

همین برای `deleteCargoInfo.php` هم صادق است و فقط `id` می‌خواهد.

**اصلاح:** یک trait یا middleware مشترک (مثلاً `App\Core\RequiresSession`) بسازید و در ابتدای **هر** action در `CargoController` صدا بزنید. `SessionService::isValidToken` از قبل موجود است (`PHP/src/Services/SessionService.php:150`).

---

## P0-2. هویت کاربر از بدنهٔ درخواست خوانده می‌شود و کاملاً جعل‌پذیر است

**شواهد:**

```php
// PHP/src/Controllers/CargoController.php:44
$requiredFields = [..., 'username', 'userType'];
```

این مقادیر مستقیماً از بدنهٔ JSON گرفته و بدون هیچ اعتبارسنجی در دیتابیس ذخیره می‌شوند:

- `CargoRepository::insertCargo` — `$params['username']`, `$params['userType']` (خط ۸۲-۸۳)
- `CargoRepository::updateCargoExit` — همان (خط ۱۲۲)
- `CargoRepository::confirmCargo` — `confirm_username`, `confirm_usertype` (خط ۲۰۱)

**تأثیر:** کل زنجیرهٔ ردیابی (audit trail) بی‌ارزش است. هر کاربری می‌تواند حواله را به نام کاربر دیگری ثبت یا تأیید کند. حتی بدون P0-1، یک کاربر معمولی می‌تواند در بدنهٔ درخواست `userType: "admin"` بفرستد.

**اصلاح:** بعد از احراز هویت، `username` و `userType` را از رکورد نشست سمت سرور استخراج کنید و مقادیر بدنه را نادیده بگیرید (نه اینکه با آن‌ها مقایسه کنید — کلاً دور بریزید).

---

## P0-3. هیچ کنترل سطح دسترسی روی حذف و ویرایش حواله وجود ندارد

**شواهد:**

سیستم مجوز کامل موجود است:
- `PHP/config/permissions.json` شامل `delete_cargo` و `edit_cargo` برای هر نقش
- `PHP/src/Services/PermissionService.php`
- `AppApiController::requirePermission()` (خط ۶۲)

اما `CargoController::deleteCargoInfo` (خط ۲۳۴) هیچ‌کدام را صدا نمی‌زند. تنها چیزی که مانع حذف است، این جریان **سمت کلاینت** است:

```kotlin
// CargoViewModel.kt:893-899
val passwordResponse = apiService.checkPassword(password, "delete_info")
if (passwordResponse.isSuccessful && passwordResponse.body()?.success == true) {
    apiService.deleteCargo(cargoInfoRequest)   // ← فراخوانی جداگانه
}
```

دو درخواست HTTP کاملاً مستقل. سرور در درخواست دوم هیچ اطلاعی از نتیجهٔ درخواست اول ندارد. کافی است مستقیماً `deleteCargoInfo.php` صدا زده شود.

**اصلاح:** بررسی رمز و بررسی مجوز باید **داخل خود** `deleteCargoInfo` انجام شود، در یک تراکنش، با هویت گرفته‌شده از نشست.

---

## P0-4. دیالوگ «تأیید ثبت تکراری» به دلیل ناسازگاری نام‌گذاری JSON هرگز باز نمی‌شود

**شواهد:**

سرور با کلید snake_case پاسخ می‌دهد:

```php
// PHP/src/Services/CargoService.php:70 و :106
"data" => [
    "warning" => true,
    "message" => $warningMessage,
    "existing_cargo" => $existingCargo,
    "requires_confirmation" => true          // ← snake_case
]
```

مدل کلاینت camelCase انتظار دارد و هیچ `@SerializedName` ندارد:

```kotlin
// data/model/CargoModels.kt:105
val requiresConfirmation: Boolean? = null    // ← camelCase، بدون annotation
```

و نمونهٔ Gson هیچ `FieldNamingPolicy` تنظیم نکرده — یعنی حالت پیش‌فرض `IDENTITY` (`RetrofitClient.kt:67-71`).

**نتیجه:** `requiresConfirmation` همیشه `null` است. بنابراین:

```kotlin
// CargoViewModel.kt:504-511
private fun handle24HourWarning(responseBody: SaveOrUpdateResponse) {
    if (responseBody.requiresConfirmation == true) {   // ← همیشه false
        _showDuplicateConfirmationDialog.value = true
    } else {
        showMessage(responseBody.message, MessageType.WARNING)   // ← همیشه این
    }
}
```

**زنجیرهٔ کد مردهٔ ناشی از این باگ:**
- `DuplicateConfirmationDialog` در `RegisterCargoScreen.kt:845` هرگز رندر نمی‌شود
- `_pendingCargoInfo` (`CargoViewModel.kt:91`) پر می‌شود ولی هرگز مصرف نمی‌شود
- `confirmDuplicateCargoRegistration()` (خط ۵۷۹) و `cancelDuplicateCargoRegistration()` (خط ۶۱۱) هرگز فراخوانی نمی‌شوند
- `duplicateConfirmation = "proceed"` هرگز به سرور نمی‌رسد، پس شاخهٔ `$params['duplicateConfirmation'] !== "proceed"` در `CargoService.php:42` و `:92` همیشه true است

**تأثیر کاربری:** کاربر پیام هشدار می‌بیند اما هیچ راهی برای ادامه دادن ندارد. ثبت حوالهٔ تکراری مجاز (که منطق سرور صریحاً برایش مسیر گذاشته) عملاً غیرممکن است.

**اصلاح:**

```kotlin
@SerializedName("requires_confirmation") val requiresConfirmation: Boolean? = null,
```

همین بررسی را برای `existing_cargo` و سایر کلیدهای snake_case در تمام مدل‌ها انجام دهید — احتمال دارد موارد مشابه دیگری هم باشد. راه‌حل سراسری: `GsonBuilder().setFieldNamingPolicy(LOWER_CASE_WITH_UNDERSCORES)` مناسب نیست چون بقیهٔ APIها camelCase هستند؛ پس `@SerializedName` صریح درست‌ترین کار است.

---

## P0-5. رِیس‌کاندیشن: خواندن بدون قفل → درج حوالهٔ تکراری در دیتابیس

**شواهد:**

```php
// PHP/src/Services/CargoService.php:77
$existingCargo = $this->repo->findCargoByKeys(...);   // SELECT بدون FOR UPDATE
$shouldInsertNew = !$existingCargo;
...
if ($shouldInsertNew) {
    $this->repo->insertCargo($params, ...);           // INSERT
}
```

`CargoRepository::findCargoByKeys` (خط ۳۶) یک `SELECT ... LIMIT 1` معمولی است. بین این SELECT و INSERT هیچ قفلی وجود ندارد و در schema هم unique key روی این ترکیب دیده نمی‌شود.

**اثبات اینکه این مشکل واقعاً رخ می‌دهد:** خود اپ یک زیرسیستم کامل برای مقابله با نتیجهٔ این باگ دارد:

```kotlin
// CargoViewModel.kt:242
private fun checkForDuplicateTrackingNumbers(cargoList: List<CargoInfo>): List<String>
// CargoViewModel.kt:735-742 → نمایش DuplicateTrackingNumbersDialog
```

یعنی رکوردهای تکراری در عمل تولید می‌شوند و اپ فقط آن‌ها را گزارش می‌دهد.

**مشکل مشابه در مسیر خروج:** دو درخواست هم‌زمان خروج برای یک حواله، هر دو `status = 'ورود'` می‌خوانند و هر دو `updateCargoExit` اجرا می‌کنند (`CargoService.php:142`, `CargoRepository.php:115`) — دومی روی اولی می‌نویسد و تناژ موقت **دو بار** کسر می‌شود (`CargoService.php:160-170`).

**اصلاح:**

1. UNIQUE INDEX روی ترکیب کلیدی:
```sql
ALTER TABLE CargoInfo
  ADD UNIQUE KEY uk_cargo_identity
  (shipName, loadingWarehouse, cargoType, shippingCompany, loadingQuotaNumber, trackingNumber);
```
(اگر ثبت تکراری عمدی مجاز است، ستون تفکیک‌کننده اضافه کنید تا unique همچنان معنا داشته باشد.)

2. برای مسیر خروج، از update شرطی استفاده کنید و `affected_rows` را چک کنید:
```sql
UPDATE CargoInfo SET status='خروج', ... WHERE id = ? AND status = 'ورود'
```
اگر `affected_rows == 0` یعنی کس دیگری زودتر خروج زده — خطا برگردانید.

3. یا `SELECT ... FOR UPDATE` در `findCargoByKeys` (تراکنش از قبل باز است).

---

# P1 — بالا

## P1-1. کنترل‌های کلیدی کسب‌وکار فقط سمت کلاینت اجرا می‌شوند

سه کنترل مهم فقط در اپ اندروید هستند و سرور آن‌ها را نمی‌شناسد:

| کنترل | محل کلاینت | وضعیت سرور |
|---|---|---|
| کوتاژ غیرفعال | `QuotaValidationUseCase.kt:22-30` | بررسی نمی‌شود |
| رسیدن به حد نصاب درصد | `QuotaValidationUseCase.kt:37-51` | بررسی نمی‌شود |
| اتمام تناژ موقت | `QuotaValidationUseCase.kt:75-85` | بررسی نمی‌شود |

در مورد تناژ موقت، سرور آن را **کسر** می‌کند اما جلوی ثبت را نمی‌گیرد:

```php
// PHP/src/Services/CargoService.php:161
$newTempTonnage = max(0, (float)$tempTonnageData['temp_tonnage_amount'] - $netWeightValue);
```

یعنی وقتی تناژ موقت صفر شد، همچنان می‌توان بی‌نهایت خروج ثبت کرد و مقدار صفر باقی می‌ماند.

**اصلاح:** این سه بررسی را به `CargoService::saveOrUpdateCargo` و داخل همان تراکنش منتقل کنید. بررسی سمت کلاینت را نگه دارید (برای UX سریع)، اما سرور باید حرف آخر را بزند.

## P1-2. تکراری بودن شماره قبض باسکول در مسیر اصلی خروج بررسی نمی‌شود

بررسی تکراری بودن یک فراخوانی **جداگانه و صرفاً مشورتی** از سمت کلاینت است (`CargoViewModel.kt:624-657` → `check_scale_receipt.php`).

اعتبارسنجی سمت سرور در مسیر واقعی خروج فقط این است:

```php
// PHP/src/Services/CargoService.php:320
private function validateExitData(string $netWeight, string $scaleReceiptNumber): void {
    // فقط: عدد مثبت بودن وزن، خالی نبودن قبض، رقمی بودن قبض
}
```

جالب اینکه متد `isScaleReceiptDuplicate` وجود دارد اما فقط در `updateCargoInfo` (مسیر ویرایش کامل) استفاده شده — `CargoController.php:112`.

**اصلاح:** `isScaleReceiptDuplicate` را داخل `saveOrUpdateCargo` و در همان تراکنش صدا بزنید + UNIQUE INDEX روی `scaleReceiptNumber` (با در نظر گرفتن NULL برای رکوردهای ورود).

## P1-3. محدودیت تلاش برای رمز حذف کاملاً بی‌اثر است

```php
// PHP/src/Controllers/UtilityController.php:110-122
if (session_status() === PHP_SESSION_NONE) { session_start(); }
if (!isset($_SESSION['attempt_count'])) { $_SESSION['attempt_count'] = 0; }
if ($_SESSION['attempt_count'] >= 5) { http_response_code(429); ... }
```

این مکانیزم بر پایهٔ session cookie است. اما کلاینت OkHttp **هیچ CookieJar تنظیم‌شده‌ای ندارد** (`RetrofitClient.kt:108-125`) — پیش‌فرض OkHttp کوکی‌ها را دور می‌ریزد.

**نتیجه:** هر درخواست یک نشست PHP تازه می‌سازد، `attempt_count` همیشه ۰ است، شمارنده هرگز به ۵ نمی‌رسد. تنها سد باقی‌مانده، سقف ۶۰ درخواست در دقیقهٔ پروکسی است — یعنی ۸۶۴۰۰ تلاش در روز روی رمز حذف.

**اصلاح:** شمارندهٔ تلاش را در دیتابیس نگه دارید با کلید `(username, deviceId)` یا `(ip, passwordType)`، نه در `$_SESSION`.

## P1-4. پیام «حواله با موفقیت حذف شد» بدون توجه به نتیجه نمایش داده می‌شود

```kotlin
// CargoInfoDetailsDialogSection.kt:408-417
onConfirm = {
    coroutineScope.launch {
        val request = CargoInfoRequest(id = info.id ?: 0)
        viewModel.deleteCargo(request, password)          // ← بدون انتظار برای نتیجه
        onUpdateTypeChange("cargo_delete")
        snackbarHostState.showSnackbar("حواله با موفقیت حذف شد")   // ← بی‌قید و شرط
        onDismiss()
    }
}
```

`deleteCargo` یک تابع غیر-suspend است که خودش `viewModelScope.launch` می‌کند — پس بلافاصله برمی‌گردد. رمز اشتباه، خطای شبکه، یا خطای سرور: کاربر در هر حالت پیام موفقیت می‌بیند.

ضمناً `showDeleteConfirmation` در مسیر confirm به `false` برنمی‌گردد.

**اصلاح:** `deleteCargo` را `suspend` کنید و نتیجه (`Result<Unit>`) برگردانید؛ پیام را بر اساس نتیجهٔ واقعی نمایش دهید.

## P1-5. نشت اطلاعات داخلی سرور در پیام‌های کاربر

**سمت سرور:**

```php
// PHP/src/Controllers/CargoController.php:614
private function sendErrorResponse(string $message): void {
    http_response_code(500);
    echo json_encode(['error' => true, 'message' => $message], ...);
}
```

و در `saveInitialInfo` مستقیماً خطای دیتابیس:

```php
// CargoController.php:516
throw new Exception("خطا در آماده‌سازی دستور SQL: " . $this->conn->error);
// CargoController.php:534
throw new Exception("خطا در اجرای دستور SQL: " . $stmt->error);
```

**سمت کلاینت:** بدنهٔ خام خطا مستقیم روی صفحه:

```kotlin
CargoViewModel.kt:650  showMessage("خطا در بررسی شماره قبض باسکول: $errorBody", ...)
CargoViewModel.kt:915  showErrorMessage("خطا در حذف حواله: ${deleteResponse.errorBody()?.string()}")
RegisterCargoScreen.kt:185  showSnackbar("خطا در بررسی کوتاژ: ${e.message}")
```

**اصلاح:** سرور پیام عمومی برگرداند و جزئیات را فقط در `Logger` بنویسد. کلاینت هم `errorBody` خام را نمایش ندهد.

## P1-6. نشت coroutine — سه CoroutineScope دستی خارج از viewModelScope

```kotlin
// CargoViewModel.kt:679, 765, 940
CoroutineScope(Dispatchers.Default + SupervisorJob()).launch { ... }
```

این scopeها به چرخهٔ حیات ViewModel وصل نیستند و هرگز cancel نمی‌شوند. وقتی کاربر صفحه را ترک می‌کند، درخواست شبکه ادامه پیدا می‌کند و در پایان روی `_loadableTonnage` و `_loadableTrucks18Wheeler` می‌نویسد — روی ViewModel از بین رفته.

**اصلاح:** `viewModelScope.launch(Dispatchers.Default)`. `SupervisorJob` هم لازم نیست چون `viewModelScope` خودش supervisor دارد.

## P1-7. کدهای وضعیت HTTP نادرست

- خطاهای کسب‌وکار (`"تعداد نفرات باید عددی بزرگتر از صفر باشد"` در `CargoService.php:119`) با `sendErrorResponse` به **HTTP 500** تبدیل می‌شوند. باید ۴۰۰ یا ۴۲۲ باشند.
- `confirmCargo` پیام خطای `"حواله قبلاً تأیید شده است"` را با **HTTP 200** برمی‌گرداند (`CargoController.php:220`).
- کلاینت روی همین‌ها منطق می‌سازد: `handleErrorHttpResponse` فقط کد ۴۰۹ را به‌عنوان warning می‌شناسد (`CargoViewModel.kt:491`) و بقیه را «خطای ناشناخته» می‌داند.

---

# P2 — متوسط (عملکرد، معماری، کیفیت کد)

## P2-1. هر بروزرسانی چهار درخواست شبکه می‌زند که دوتا از آن‌ها دقیقاً یکسان است

`ReportsRepository.getInitialInfo` و `ReportsRepository.getCargoInfo` **هر دو همان endpoint را صدا می‌زنند**:

```kotlin
// ReportsRepository.kt:39 و :65 — هر دو یکسان
val response = apiService.getCargoInfo(quotaNumber, shippingCompany, warehouse, cargoType)
```

و `loadCargoInfoList` هر دو را فراخوانی می‌کند:

| # | فراخوانی | محل |
|---|---|---|
| ۱ | `repository.getInitialInfo` → `getInitialInfo.php` | `CargoViewModel.kt:682` |
| ۲ | `apiService.getLoadableTonnage` | `CargoViewModel.kt:688` |
| ۳ | `repository.getCargoInfo` → `getInitialInfo.php` **(تکراری)** | `CargoViewModel.kt:732` |
| ۴ | `apiService.getLoadableTonnage` **(تکراری)** | `CargoViewModel.kt:768` |

هر فراخوانی `getInitialInfo.php` سمت سرور **چهار کوئری** اجرا می‌کند (`CargoController.php:413, 429, 457, 468`) که شامل دو `SELECT *` و یک `SELECT DISTINCT` روی کل کوتاژ است.

`refreshCargoInfo()` بعد از هر ثبت، هر بروزرسانی، هر حذف، و هر تغییر وضعیت کوتاژ صدا زده می‌شود — و در `handleSuccessResponse` (خط ۵۴۵) هم.

**تأثیر:** با سقف ۶۰ درخواست/دقیقه به ازای هر IP در پروکسی (`protected_proxy.php:22`)، چند کاربر پشت یک NAT دفتری به‌راحتی به خطای ۴۲۹ می‌خورند.

**اصلاح:** `getInitialInfo` را از repository حذف کنید (تکراری است) و اولین بلوک `loadCargoInfoList` (خط ۶۷۹-۷۲۷) را کاملاً بردارید — کارش را بلوک دوم انجام می‌دهد. این کار ترافیک را نصف و کوئری‌های سرور را نصف می‌کند.

## P2-2. `allTrackingNumbers` بی‌استفاده منتقل می‌شود

سرور برای هر درخواست تمام شماره حواله‌های کوتاژ را می‌فرستد:

```php
// CargoController.php:468
$trackStmt = $this->conn->prepare("SELECT DISTINCT trackingNumber FROM CargoInfo WHERE ...");
```

کلاینت آن را ذخیره می‌کند:

```kotlin
// CargoViewModel.kt:748-749
val allTrackingNumbers = result.allTrackingNumbers?.toSet() ?: emptySet()
_cachedTrackingNumbers.value = allTrackingNumbers
```

`_cachedTrackingNumbers` (خط ۱۰۴) `private` است و **هیچ‌جا خوانده نمی‌شود**.

**اصلاح:** حذف کوئری از سرور، حذف فیلد از مدل، حذف `_cachedTrackingNumbers`.

## P2-3. فیلتر و مرتب‌سازی سنگین در بدنهٔ Composable بدون `remember`

```kotlin
// RegisterCargoScreen.kt:270
val (nonExitedCargos, exitedCargos) = filteredCargoInfoList.partition { it.status == "ورود" }

// RegisterCargoScreen.kt:665-669
val currentItems = if (selectedTab == 0) {
    nonExitedCargos.sortedByDescending { it.entryTime }
} else {
    exitedCargos.sortedByDescending { "${it.exitDate} ${it.exitTime}" }
}
```

هیچ‌کدام در `remember` نیستند — با **هر بازترکیب** (هر کاراکتر جستجو، هر فریم انیمیشن FAB، هر تغییر `isPressed`) کل لیست دوباره partition و sort می‌شود. در حالت دوم برای هر آیتم یک String هم ساخته می‌شود.

**فیلتر دوباره‌کاری:**

```kotlin
// خط 258-268: فیلتر اول
val filteredCargoInfoList by remember(cargoInfoList, searchQuery) { derivedStateOf { ... } }
// خط 671-675: همان فیلتر، دوباره
val filteredItems = if (searchQuery.isNotEmpty()) {
    currentItems.filter { it.trackingNumber.contains(searchQuery, ignoreCase = true) }
} else currentItems
```

ضمناً `remember(keys) { derivedStateOf { } }` الگوی نادرستی است — وقتی کلید می‌دهید، `derivedStateOf` هیچ ارزش افزوده‌ای ندارد و فقط یک لایهٔ اضافی state می‌سازد.

**اصلاح:**

```kotlin
val visibleItems = remember(cargoInfoList, searchQuery, selectedTab) {
    val filtered = if (searchQuery.isEmpty()) cargoInfoList
                   else cargoInfoList.filter { it.trackingNumber.contains(searchQuery, true) }
    val (entered, exited) = filtered.partition { it.status == "ورود" }
    if (selectedTab == 0) entered.sortedByDescending { it.entryTime }
    else exited.sortedByDescending { "${it.exitDate} ${it.exitTime}" }
}
```

(شمارندهٔ تب‌ها هم باید از `entered.size`/`exited.size` همین `remember` بیاید.)

## P2-4. `LazyColumn` بدون `key` و مقایسه با `last()`

```kotlin
// RegisterCargoScreen.kt:706
items(filteredItems) { info ->
    ErrorHandlingCargoInfoRow(...)
    if (info != filteredItems.last()) { HorizontalDivider(...) }
}
```

دو مشکل:

1. **بدون `key`:** با هر تغییر لیست، Compose نمی‌تواند آیتم‌ها را تطبیق دهد و همه را بازسازی می‌کند؛ موقعیت اسکرول و انیمیشن‌ها می‌پرد.
2. **`info != filteredItems.last()`:** مقایسهٔ ساختاری `data class`. اگر دو رکورد با محتوای یکسان وجود داشته باشد — که دقیقاً همان مشکل P0-5 است و اپ برایش دیالوگ دارد — جداکنندهٔ اشتباهی حذف می‌شود.

**اصلاح:**

```kotlin
itemsIndexed(filteredItems, key = { _, it -> it.id ?: it.trackingNumber }) { index, info ->
    ErrorHandlingCargoInfoRow(...)
    if (index < filteredItems.lastIndex) HorizontalDivider(...)
}
```

## P2-5. لیست در ارتفاع ثابت ۴۵۰dp حبس شده است

```kotlin
// RegisterCargoScreen.kt:701-703
modifier = Modifier.fillMaxWidth().heightIn(max = 450.dp)
```

کل صفحه یک `Column` بدون `verticalScroll` است. روی تبلت فضای خالی زیاد می‌ماند، روی صفحهٔ کوچک لیست به شدت فشرده می‌شود. `BoxWithConstraints` که در خط ۲۸۳ هست و می‌توانست همین را حل کند، استفاده نمی‌شود (با `@SuppressLint("UnusedBoxWithConstraintsScope")` ساکت شده).

**اصلاح:** ساختار را به یک `LazyColumn` واحد تبدیل کنید که هدر (ShipInfo، فرم، تب‌ها) را به‌عنوان `item` و ردیف‌ها را با `items` رندر کند — با `Modifier.weight(1f)` روی لیست.

## P2-6. تغییر کوتاژ دو بار همان API را صدا می‌زند + مشکل N+1

`QuotaEntryDialog` قبل از تأیید بررسی می‌کند:

```kotlin
// RegisterCargoDialogs.kt:615
val response = viewModel.checkQuotaExistenceCargo(quotaEntry, shipName)
if (response.exists && ...) { onConfirm(quotaEntry) }
```

سپس `onConfirm` در صفحه، `handleQuotaEntry` را صدا می‌زند که **دقیقاً همان بررسی را دوباره انجام می‌دهد**:

```kotlin
// RegisterCargoScreen.kt:155
val response = viewModel.checkQuotaExistenceCargo(quotaCode, currentInitialInfo.shipName)
```

و خود `checkQuotaExistenceCargo` به ازای **هر** کوتاژ منطبق یک درخواست جدا می‌زند (N+1):

```kotlin
// CargoViewModel.kt:142-151
val updatedQuotas = quotaResponse.matchingQuotas.map { quota ->
    val quotaStatus = checkDetailedQuotaStatus(...)   // ← یک درخواست شبکه به ازای هر آیتم
    quota.copy(isActive = quotaStatus.isActive)
}
```

**اصلاح:** دیالوگ نتیجهٔ بررسی‌شده را در `onConfirm` پاس بدهد تا `handleQuotaEntry` نیاز به فراخوانی دوباره نداشته باشد. برای N+1، سرور باید `isActive` را در همان پاسخ `checkQuotaExistenceCargo` برگرداند.

## P2-7. `handleQuotaEntry` یک `InitialInfo` ناقص و نادرست می‌سازد

```kotlin
// RegisterCargoScreen.kt:161-172
val newInitialInfo = InitialInfo(
    shipName = selectedQuota.shipName,
    loadingWarehouse = selectedQuota.warehouse,
    cargoType = selectedQuota.cargoType,
    shippingCompany = selectedQuota.shippingCompany,
    cargoWeight = 0f,              // ← صفر
    loadingQuotaNumber = selectedQuota.quotaNumber.toIntOrNull() ?: 0,
    remainingWeight = 0f,          // ← صفر
    totalNetWeight = 0f,           // ← صفر
    averageNetWeight = 0f,         // ← صفر
    remainingServices = 0          // ← صفر
)
viewModel.setInitialInfo(newInitialInfo)
viewModel.refreshCargoInfo()       // ← fire-and-forget
```

سه مشکل مجزا:

1. **صفرها فوراً روی UI می‌نشینند.** `setInitialInfo` بلافاصله StateFlow را تغییر می‌دهد؛ `refreshCargoInfo` غیرهمگام است. تا رسیدن پاسخ، `ShipInfoSection` تناژ صفر نشان می‌دهد و پیام موفقیت هم همان لحظه نمایش داده می‌شود.

2. **`tempTonnageStatus` روی `false` پیش‌فرض می‌رود.** اگر کاربر در این پنجرهٔ زمانی ثبت بزند، `validateTempTonnage` (`QuotaValidationUseCase.kt:76`) بی‌سروصدا رد می‌شود چون شرط `initialInfo.tempTonnageStatus` برقرار نیست. کنترل تناژ موقت دور زده می‌شود.

3. **`quotaNumber.toIntOrNull()` صفرهای ابتدایی را نابود می‌کند.** دیالوگ دقیقاً ۴ رقم می‌خواهد (`RegisterCargoDialogs.kt:600`)، پس `"0123"` معتبر است — اما به `123` تبدیل می‌شود و در `loadCargoInfoList` به‌صورت `"123"` به سرور می‌رود که با رکورد `"0123"` مطابقت نمی‌کند. کوتاژ «یافت نشد».

همچنین `matchingQuotas.first()` بدون بررسی چندتایی بودن انتخاب می‌شود و برخلاف دیالوگ، `isActive` را چک نمی‌کند.

**اصلاح:** `setInitialInfo` را حذف کنید و فقط پارامترها را به یک متد `switchQuota(quotaNumber, ...)` در ViewModel بدهید که خودش `loadCargoInfoList` را صدا بزند و `initialInfo` را از پاسخ سرور بسازد. `loadingQuotaNumber` هم باید `String` باشد نه `Int`.

## P2-8. `htmlspecialchars` روی داده‌ای که در JSON مصرف می‌شود (خطر خرابی داده)

```php
// CargoController.php:580
private function sanitizeString(string $input): string {
    return htmlspecialchars(trim($input), ENT_QUOTES, 'UTF-8');
}
```

این روی `shipName`، `loadingWarehouse`، `cargoType`، `shippingCompany` اعمال و سپس در `WHERE` استفاده می‌شود.

اما `saveInitialInfo` (که رکورد اصلی را می‌سازد) **هیچ sanitize نمی‌کند** و مقدار خام را ذخیره می‌کند (`CargoController.php:519-531`).

**نتیجه:** اگر نام شرکت باربری شامل `&` یا `'` باشد، در `InitialInfo` خام ذخیره می‌شود ولی در `saveOrUpdateCargo` به `&amp;` / `&#039;` تبدیل شده و مقایسه‌های `findCargoByKeys` و `findTempTonnage` شکست می‌خورند → رکورد پیدا نمی‌شود → درج تکراری.

ضمناً مصرف‌کنندهٔ این API یک اپ اندرویدی است؛ HTML-escape در ورودی هیچ محافظتی ایجاد نمی‌کند. Escape باید در **خروجی** لایهٔ نمایش وب اعمال شود، نه در ورودی.

**اصلاح:** `sanitizeString` را به `trim()` ساده تغییر دهید (SQL Injection با prepared statement از قبل بسته است) و اگر پنل وبی هست، escape را آنجا انجام دهید.

## P2-9. حدود مجاز اعتبارسنجی در سه لایه متفاوت است

| مقدار | کلاینت (صفحه) | کلاینت (UseCase) | سرور |
|---|---|---|---|
| وزن خالص | `1000..60000` (`RegisterCargoScreen.kt:420`) | `1000..45000` (`QuotaValidationUseCase.kt:105`) | فقط `> 0` (`CargoService.php:321`) |
| طول قبض باسکول | — | `8..10` (`QuotaValidationUseCase.kt:113`) | دقیقاً `8` + پیشوند `44..55` (`CargoService.php:297-303`) در مسیر بررسی؛ فقط `ctype_digit` در مسیر خروج (`CargoService.php:327`) |
| تعداد نفرات | `1..5` (`RegisterCargoForm.kt:224,264`) | — | `>= 1` بدون سقف (`CargoService.php:118`) |

قواعد سرور باید مرجع باشند و کلاینت همان را بازتاب دهد.

## P2-10. حدود ۱۲۰ خط کد مرده در `RegisterCargoScreen.kt`

متغیرهای حالتی که **هرگز مقدار فعال نمی‌گیرند** و بلوک‌های UI وابسته به آن‌ها هیچ‌وقت رندر نمی‌شوند:

| متغیر | تعریف | بلوک مرده |
|---|---|---|
| `showConfirmationDialog` | خط ۲۱۸ | خطوط ۷۵۲-۷۶۵ (`DialogPassword`) |
| `confirmationMessage` | خط ۲۱۹ (`val` + `mutableStateOf` — هرگز تغییر نمی‌کند) | — |
| `cargoInfoToUpdate` | خط ۲۲۰ | — |
| `showExitStatusDialog` | خط ۲۲۷ | خطوط ۷۸۹-۷۹۶ (`ExitStatusDialog`) |
| `showStatisticsDialog` | خط ۲۲۸ | خطوط ۸۱۵-۸۳۳ (`SelectInfoScreenContent`) |
| `showQuotaWarning` | خط ۲۳۰ | خطوط ۸۳۵-۸۴۳ (`QuotaWarningDialog`) |

نکتهٔ خاص در بلوک `showStatisticsDialog`: `SelectInfoScreenContent(navController = rememberNavController(), ...)` — یک NavController تازه و منفصل از گراف ناوبری اپ ساخته می‌شود؛ اگر این بلوک روزی فعال شود، ناوبری داخل آن به هیچ‌جا نمی‌رود.

**`netWeight` هم عملاً مرده است:** در خط ۲۱۲ تعریف و در خط ۲۴۵ پاک می‌شود، اما هیچ `onNetWeightChange`ای وجود ندارد. پس:
- اعتبارسنجی خطوط ۴۱۹-۴۲۳ هرگز اجرا نمی‌شود (`netWeight.isNotBlank()` همیشه false)
- `viewModel.submitCargoInfo(trackingNumber, netWeight, ...)` در خط ۴۲۵ همیشه رشتهٔ خالی می‌فرستد (که برای مسیر «ثبت ورود» درست است، ولی گمراه‌کننده)
- `viewModel.updateCargoInfo(cargoInfo, netWeight)` در خط ۷۵۷ اگر روزی فعال شود، وزن خالی می‌فرستد

**`updateType` فقط نوشته می‌شود، هرگز اثری ندارد:**

```kotlin
// RegisterCargoScreen.kt:893-902
LaunchedEffect(initialInfo, loadableTonnage) {
    if (initialInfo != null && loadableTonnage.isNotEmpty()) {
        val excludedUpdateTypes = setOf("cargo_submit", "cargo_update", "cargo_delete")
        if (updateType == null || updateType !in excludedUpdateTypes) {
            if (updateType == null) { updateType = "initial" }
        }
    }
}
```

این بلوک هیچ کاری نمی‌کند جز مقداردهی متغیری که هیچ‌کس نمی‌خواند. کل `LaunchedEffect` و متغیر `updateType` (و پارامتر `onUpdateTypeChange` در `CargoInfoDetailsDialog`) قابل حذف است.

## P2-11. تشخیص فشرده شدن FAB با `pointerInput` دستی

```kotlin
// RegisterCargoScreen.kt:295-307
.clickable(interactionSource = remember { MutableInteractionSource() }, indication = null) { ... }
.pointerInput(Unit) {
    awaitPointerEventScope {
        while (true) {
            val event = awaitPointerEvent()
            isPressed = event.changes.firstOrNull()?.pressed == true
        }
    }
}
```

دو `interactionSource` موازی و یک حلقهٔ بی‌نهایت دستی. اگر انگشت از محدودهٔ دکمه خارج شود، رویداد up دریافت نمی‌شود و `isPressed` روی `true` گیر می‌کند — دکمه کوچک می‌ماند.

**اصلاح:**

```kotlin
val interactionSource = remember { MutableInteractionSource() }
val isPressed by interactionSource.collectIsPressedAsState()
// ... .clickable(interactionSource = interactionSource, indication = null) { ... }
```

و بلوک `pointerInput` را کامل حذف کنید.

## P2-12. سه سیستم موازی پیام‌رسانی در یک صفحه

1. `SnackbarHostState` محلی + `SnackbarHost` در Scaffold (خطوط ۲۰۲، ۲۸۹)
2. `StatusSnackbar` سراسری روی `Box(fillMaxSize)` با `zIndex(Float.MAX_VALUE)` از `viewModel.snackbarMessage` (خطوط ۸۰۰-۸۱۳)
3. `MessageDialog` از `snackbarQueue` (خطوط ۷۲۷-۷۳۴)

سه منبع حقیقت برای «پیام به کاربر». یک عملیات ممکن است هم‌زمان از دو کانال پیام بدهد (مثلاً `handleQuotaEntry` از کانال ۱ و `submitCargoInfo` از کانال ۳).

**اصلاح:** یک کانال واحد `UiEvent` در ViewModel (`Channel`/`SharedFlow`) و یک مصرف‌کننده در صفحه.

## P2-13. `handleQuotaEntry` منطق دامنه را در لایهٔ UI قرار داده

```kotlin
// RegisterCargoScreen.kt:142
suspend fun handleQuotaEntry(
    quotaCode: String,
    currentInitialInfo: InitialInfo?,
    viewModel: CargoViewModel,
    snackbarHostState: SnackbarHostState,   // ← وابستگی Compose در منطق کسب‌وکار
    onQuotaChanged: () -> Unit = {}
)
```

یک تابع سطح فایل، `public`، که هم ViewModel و هم `SnackbarHostState` می‌گیرد. غیرقابل تست واحد و خارج از هر لایهٔ معماری.

**اصلاح:** انتقال به `CargoViewModel` (یا یک UseCase) با خروجی `Result`.

## P2-14. `isSubmitting` مسیر تأیید تکراری را پوشش نمی‌دهد

`submitCargoInfo` با `_isSubmitting` محافظت می‌شود (`CargoViewModel.kt:312-316, 377`)، اما `confirmDuplicateCargoRegistration` (خط ۵۷۹) نه این پرچم را می‌گذارد و نه اعتبارسنجی‌ها را دوباره اجرا می‌کند — مستقیم `apiService.saveOrUpdateCargoInfo` صدا می‌زند. (فعلاً به‌خاطر P0-4 غیرقابل دسترس است، اما بعد از رفع آن فعال می‌شود.)

---

# P3 — کم

## P3-1. لاگ درون حلقهٔ فیلتر

```kotlin
// RegisterCargoScreen.kt:262-264
if (searchQuery.isNotEmpty()) {
    Log.d("RegisterCargoActivity_Log", "Checking ${cargoInfo.trackingNumber} against '$searchQuery': $matches")
}
```

در بیلد release توسط `-assumenosideeffects` حذف می‌شود (`proguard-rules.pro:215-223`)، پس ریسک امنیتی ندارد. اما در debug به ازای هر کاراکتر × هر رکورد یک خط لاگ تولید می‌شود.

## P3-2. لاگ دسترسی پروکسی عملاً هرگز نوشته نمی‌شود

```php
// protected_proxy.php:214-215
$this->logAccess('PROXY_ACCESS', $target);   // فقط در pendingLogLine ذخیره می‌کند
echo $this->execute($target);
// ...
// خط 237
$proxy->flushLog();                          // نوشتن واقعی
```

اما تمام کنترلرها با `exit;` پایان می‌یابند (`CargoController.php:611`, `620`؛ `UtilityController.php:149`). `exit` داخل فایل include شده کل اسکریپت را خاتمه می‌دهد — پس `echo`، `flushLog()` و `fastcgi_finish_request()` هیچ‌کدام اجرا نمی‌شوند. (خروجی به لطف flush خودکار بافرها در shutdown همچنان می‌رسد.)

**اصلاح:** `register_shutdown_function([$proxy, 'flushLog'])`.

## P3-3. `autocommit(FALSE)` بازگردانده نمی‌شود

```php
// CargoService.php:29
$conn->autocommit(FALSE);
```

در مسیرهای return زودهنگام (خط ۶۳-۷۲ و ۹۹-۱۰۸ که `rollback()` می‌کنند و برمی‌گردند) اتصال singleton تا پایان درخواست در حالت غیر-autocommit می‌ماند. فعلاً بی‌خطر است چون فرایند PHP بعد از پاسخ تمام می‌شود، ولی شکننده است.

**اصلاح:** در `finally` مقدار `autocommit(TRUE)` را برگردانید.

## P3-4. `Cache-Control: max-age=60, public` روی دادهٔ تجاری

```php
// AnalyticsController::handleQuotaRemaining
header('Cache-Control: max-age=60, public');
```

`public` به پروکسی‌های میانی اجازهٔ کش می‌دهد. برای دادهٔ تناژ و کوتاژ باید `private` باشد.

ضمناً `handleQuotaRemaining` — برخلاف `handleRealTimeLoadingData` در همان کلاس — `requireAuthenticatedSession()` را صدا نمی‌زند.

## P3-5. `SELECT *` در `getInitialInfo.php`

خطوط ۴۱۳ و ۴۵۷ در `CargoController.php`. ستون‌های اضافه به کلاینت می‌روند و در برابر تغییر schema شکننده است.

## P3-6. فایل‌های rate-limit بدون پاک‌سازی

```php
// protected_proxy.php:126
$rateFile = PROXY_LOG_DIR . "/rate_limit_$safeIp.txt";
```

مسیر fallback (نبود APCu) به ازای هر IP یکتا یک فایل می‌سازد بدون هیچ garbage collection.

## P3-7. `Regex` در هر اسکن بارکد کامپایل می‌شود

```kotlin
// RegisterCargoScreen.kt:205
val cleanedBarcode = barcode.replace(Regex("[^0-9]"), "")
```

به ثابت سطح فایل منتقل شود: `private val NON_DIGITS = Regex("[^0-9]")`.

## P3-8. تناقض `sql_mode` بین لایه‌ها

`Database::getMysqliConnection` مقدار `NO_ENGINE_SUBSTITUTION` (بدون STRICT) می‌گذارد (خط ۷۴)، سپس `CargoService::saveOrUpdateCargo` آن را به `STRICT_TRANS_TABLES` تغییر می‌دهد (خط ۳۰). یعنی رفتار درج در endpointهای مختلف متفاوت است. باید در یک نقطه (اتصال) تثبیت شود.

---

# نکات مثبت

برای انصاف، این‌ها درست انجام شده‌اند:

- **همهٔ کوئری‌ها prepared statement هستند** — در سراسر `CargoRepository` و `CargoController` هیچ الحاق رشته‌ای در SQL دیده نشد. SQL Injection بسته است.
- **TLS اجباری با CA اختصاصی** — `network_security_config.xml` با `cleartextTrafficPermitted="false"` و CAهای Certum باندل‌شده.
- **رمزها با `password_verify` بررسی می‌شوند** (`UtilityController.php:132`) — نه مقایسهٔ متن ساده.
- **Secrets در کتابخانهٔ native نگه‌داری می‌شوند** (`Secrets.kt` + `libsecrets.so`) نه در `BuildConfig` یا `strings.xml`.
- **ProGuard جدی پیکربندی شده** — obfuscation dictionary، repackaging، حذف `Log`.
- **`isSubmitting` جلوی دابل-کلیک روی دکمهٔ ثبت را می‌گیرد** (`RegisterCargoForm.kt:118`).
- **تراکنش دیتابیس در `saveOrUpdateCargo` با rollback صحیح در تمام مسیرهای خطا** — فقط قفل کم دارد.
- **`innodb_lock_wait_timeout = 5`** برای جلوگیری از قفل طولانی (`CargoService.php:31`).
- **`MicroCache::forget` بعد از هر نوشتن** برای جلوگیری از دادهٔ کهنه (`CargoService.php:282`).
- **الگوی احراز هویت درست در `AppApiController` و `AnalyticsController` وجود دارد** — فقط باید به `CargoController` هم گسترش یابد.

---

# نقشه راه پیشنهادی

### گام ۱ — امنیت (فوری، پیش از هر کار دیگر)
1. یک trait مشترک `RequiresSession` بسازید و در ابتدای همهٔ actionهای `CargoController` صدا بزنید *(P0-1)*
2. `username`/`userType` را از نشست بگیرید و مقادیر بدنه را حذف کنید *(P0-2)*
3. `requirePermission('delete_cargo')` و `requirePermission('edit_cargo')` را اضافه کنید؛ بررسی رمز حذف را داخل خود endpoint ببرید *(P0-3)*
4. شمارندهٔ تلاش رمز را از `$_SESSION` به دیتابیس منتقل کنید *(P1-3)*

### گام ۲ — یکپارچگی داده
5. UNIQUE INDEX روی ترکیب کلیدی حواله + `scaleReceiptNumber` *(P0-5, P1-2)*
6. مسیر خروج را به `UPDATE ... WHERE id=? AND status='ورود'` با بررسی `affected_rows` تبدیل کنید *(P0-5)*
7. بررسی کوتاژ غیرفعال، حد نصاب درصد و تناژ موقت را به سرور منتقل کنید *(P1-1)*
8. `sanitizeString` را از `htmlspecialchars` به `trim` تغییر دهید *(P2-8)*

### گام ۳ — رفع باگ‌های کاربری
9. `@SerializedName("requires_confirmation")` — و بازبینی کل مدل‌ها برای موارد مشابه *(P0-4)*
10. `deleteCargo` را `suspend` با خروجی `Result` کنید و پیام موفقیت را مشروط کنید *(P1-4)*
11. `handleQuotaEntry` را به ViewModel منتقل و `InitialInfo` ناقص را حذف کنید؛ `loadingQuotaNumber` را `String` کنید *(P2-7, P2-13)*
12. کدهای وضعیت HTTP را اصلاح کنید *(P1-7)*

### گام ۴ — عملکرد
13. `ReportsRepository.getInitialInfo` تکراری و بلوک اول `loadCargoInfoList` را حذف کنید — نصف شدن ترافیک *(P2-1)*
14. `allTrackingNumbers` را از سرور و کلاینت حذف کنید *(P2-2)*
15. فیلتر/مرتب‌سازی را در یک `remember` واحد جمع کنید *(P2-3)*
16. `key` به `LazyColumn` اضافه و `last()` را با `lastIndex` جایگزین کنید *(P2-4)*
17. سه `CoroutineScope` دستی را به `viewModelScope` تبدیل کنید *(P1-6)*
18. فراخوانی تکراری بررسی کوتاژ و N+1 وضعیت کوتاژ را رفع کنید *(P2-6)*

### گام ۵ — پاک‌سازی
19. ~۱۲۰ خط کد مرده را حذف کنید: `showConfirmationDialog`، `showStatisticsDialog`، `showExitStatusDialog`، `showQuotaWarning`، `cargoInfoToUpdate`، `confirmationMessage`، `netWeight`، `updateType` و بلوک‌های UI وابسته *(P2-10)*
20. `pointerInput` دستی FAB را با `collectIsPressedAsState` جایگزین کنید *(P2-11)*
21. سه کانال پیام‌رسانی را به یک `UiEvent` واحد تبدیل کنید *(P2-12)*
22. حدود اعتبارسنجی را در سه لایه یکسان کنید *(P2-9)*
23. ساختار صفحه را به `LazyColumn` واحد تبدیل کنید و `BoxWithConstraints` بلااستفاده را بردارید *(P2-5)*

**نکته:** گام‌های ۱ و ۲ عمدتاً سمت سرور هستند و بدون تغییر در اپ منتشرشده قابل اجرا هستند — جز مورد ۹ که نیاز به انتشار نسخهٔ جدید اپ دارد.

# گزارش بررسی جامع صفحه «بررسی و تأیید حواله‌ها»

**تاریخ:** ۱۴۰۵/۰۵/۲۲ (2026-08-13)
**دامنه بررسی:** اندروید + سمت سرور (PHP)
**نقطه شروع:** `app/src/main/java/com/atk/atk_cargo/feature/cargo_details/presentation/CargoDetailsScreen.kt`

## فایل‌های بررسی‌شده

**اندروید**
- `feature/cargo_details/presentation/CargoDetailsScreen.kt` (۵۴۵ خط)
- `feature/cargo_details/presentation/components/CargoDetailsComponents.kt` (۸۹۱ خط)
- `feature/cargo_details/presentation/components/CargoDetailsDialogSection.kt`
- `ui/viewmodel/CargoViewModel.kt`
- `data/repository/ReportsRepository.kt`، `api/ApiService.kt`، `api/RetrofitClient.kt`، `api/SessionValidator.kt`
- `feature/reports/navigation/ReportsNavigation.kt`، `feature/cargo_counter/presentation/CargoCounterOperationScreen.kt`
- `data/model/CargoModels.kt`، `feature/home/domain/HomeQuotaParser.kt`

**سرور**
- `PHP/confirm_cargo.php` → `PHP/src/Controllers/CargoController.php::confirmCargo()`
- `PHP/src/Controllers/CargoController.php::getInitialInfo()` (مسیر خواندن لیست)
- `PHP/src/Repositories/CargoRepository.php`، `PHP/src/Core/AuthenticatesRequests.php`
- `PHP/src/Services/PermissionService.php`، `PHP/src/Services/SessionService.php`
- `PHP/config/permissions.json`، `PHP/protected_proxy.php`

---

## خلاصه اجرایی

| اولویت | تعداد | ماهیت |
|---|---|---|
| **P0 — بحرانی** | ۴ | ارتقای سطح دسترسی روی عملیات تأیید، IDOR روی شناسه حواله، کرش قطعی لیست، پاک‌شدن بی‌صدای فیلتر جستجو |
| **P1 — بالا** | ۵ | بلعیده‌شدن پیام ۴۰۹ سرور، نبود قفل ضد دوبار-زدن، نبود مدیریت ۴۰۱، بازگشت ضدالگوی رفع‌شده، نشت پیام خطای داخلی |
| **P2 — متوسط** | ۱۳ | رمزگشایی سه‌باره URL و ناسازگاری پارامترها، منطق شبکه در فایل UI، هزینه polling، محاسبات در مسیر recomposition |
| **P3 — کم** | ۱۱ | لاگ، DI، دسترس‌پذیری، نام‌گذاری، کد مرده |

**چهار یافته‌ای که باید فوراً دیده شوند:**

1. **`confirmCargo` تنها عملیات نوشتنِ CargoController است که گیت مجوز ندارد.** `deleteCargoInfo` مجوز `delete_cargo` و `updateCargoInfo` مجوز `edit_cargo` می‌خواهد، اما تأیید حواله فقط «لاگین بودن» را چک می‌کند. طبق `permissions.json` نقش `operator` مجوز `cargo_counter` ندارد و فقط منوی کلاینت برایش مخفی می‌شود — سرور جلویش را نمی‌گیرد.
2. **کلید `LazyColumn` روی `trackingNumber` است، در حالی که خود اپ می‌داند شماره حواله تکراری در داده‌ی واقعی وجود دارد** (`checkForDuplicateTrackingNumbers` برایش دیالوگ هشدار دارد). این یعنی کرش قطعی صفحه در همان سناریویی که اپ برایش هشدار طراحی کرده است.
3. **هر ۳۰ ثانیه فیلتر جستجو بی‌صدا پاک می‌شود ولی متن داخل کادر جستجو باقی می‌ماند.** بارشمار در حال جستجوی یک حواله، ناگهان لیست کامل زیر انگشتش می‌آید — روی صفحه‌ای که کارش «تأیید» است، این ریسک عملیاتی مستقیم دارد.
4. **سرور عمداً برای تأیید تکراری کد ۴۰۹ با پیام گویا برمی‌گرداند (با کامنت مفصل درباره‌ی چرایی‌اش)، ولی کلاینت آن پیام را دور می‌ریزد** و به جایش «خطا در ارتباط با سرور: 409» نشان می‌دهد.

---

# P0 — بحرانی

## P0-1. عملیات «تأیید حواله» در سرور هیچ گیت مجوزی ندارد (ارتقای سطح دسترسی)

**شواهد:**

```php
// PHP/src/Controllers/CargoController.php:190
$this->requireAuthenticatedSession();   // ← فقط همین
```

مقایسه با سایر عملیات نوشتنِ همین کنترلر:

| متد | خط | گیت |
|---|---|---|
| `deleteCargoInfo` | ۲۶۶–۲۶۷ | `requireAuthenticatedSession` + `requirePermission('delete_cargo')` + گیت رمز عبور |
| `updateCargoInfo` | ۹۹–۱۰۰ | `requireAuthenticatedSession` + `requirePermission('edit_cargo')` |
| `saveInitialInfo` | ۵۳۷–۵۳۸ | `requireAuthenticatedSession` + `requirePermission('initial_info')` |
| **`confirmCargo`** | **۱۹۰** | **فقط `requireAuthenticatedSession`** |

در `PHP/config/permissions.json` نقشِ `operator` دارای `"cargo_counter": false` است و کلاینت هم فقط بر همین اساس منو را پنهان می‌کند:

```kotlin
// app/src/main/java/com/atk/atk_cargo/feature/home/domain/HomeQuotaParser.kt:12
if (userPermissions["cargo_counter"] == true) {
    items.add(MenuItem("نظارت بارشمار", ...))
}
```

**اثر:** هر کاربر لاگین‌شده — با هر نقشی، از جمله `operator` که اصلاً نباید صفحه بارشمار را ببیند — می‌تواند با یک درخواست POST مستقیم به `confirm_cargo.php` هر حواله‌ای را تأیید کند. مخفی‌کردن منو در کلاینت هیچ محافظتی نیست.

**راه‌حل:** در `CargoController::confirmCargo` بلافاصله بعد از خط ۱۹۰:

```php
$this->requirePermission('cargo_counter');
```

و ترجیحاً یک کلید اختصاصی `confirm_cargo` به `permissions.json` اضافه شود (برای هر سه نقش `admin`/`operator`/`verifier`) تا مجوز «دیدن صفحه» از مجوز «تأیید کردن» جدا شود.

---

## P0-2. IDOR: شناسه حواله بدون هیچ بررسی دامنه‌ای پذیرفته می‌شود

**شواهد:**

```php
// PHP/src/Repositories/CargoRepository.php:267
UPDATE CargoInfo SET confirm = 'تائید شده', ...
WHERE id = ? AND (confirm IS NULL OR confirm != 'تائید شده')
```

تنها اعتبارسنجی روی ورودی، `(int)$data['id'] > 0` است (`CargoController.php:201, 207`). هیچ بررسی‌ای وجود ندارد که این حواله متعلق به کوتاژ / کشتی / انبار / شرکتی باشد که کاربر واقعاً روی آن کار می‌کند.

**اثر:** یک بارشمار که مجاز به کار روی کشتی A است، با شماره‌گذاری متوالی `id` (که در `CargoInfo` کلید اصلی auto-increment است) می‌تواند حواله‌های همه‌ی کشتی‌ها، انبارها و شرکت‌های حمل را تأیید کند. با توجه به اینکه تأیید حواله یک عمل با اثر مالی/عملیاتی است، این باید بسته شود.

**راه‌حل:** بدنه‌ی درخواست علاوه بر `id` شامل `loadingQuotaNumber` (و ترجیحاً `shipName`) شود و شرط `WHERE` سرور به آن‌ها هم مقید گردد:

```php
WHERE id = ? AND loadingQuotaNumber = ? AND shipName = ?
      AND (confirm IS NULL OR confirm != 'تائید شده')
```

این کار عملاً هزینه‌ای ندارد (کلاینت هر دو مقدار را در `CargoInfo` دارد) ولی جلوی تأیید خارج از دامنه را می‌گیرد.

---

## P0-3. کرش قطعی `LazyColumn` روی داده‌ای که خود اپ برایش هشدار دارد

**شواهد:**

```kotlin
// components/CargoDetailsComponents.kt:757  و  770
items(
    items = unconfirmedCargos.sortedByDescending { it.entryTime },
    key = { it.trackingNumber }        // ← کلید غیریکتا
)
```

و در همان جریان داده:

```kotlin
// ui/viewmodel/CargoViewModel.kt:202
private fun checkForDuplicateTrackingNumbers(cargoList: List<CargoInfo>): List<String> { ... }

// خط ۶۵۱–۶۵۶ — در همین loadCargoInfoList که این صفحه استفاده می‌کند
if (duplicateTrackingNumbers.isNotEmpty()) {
    _showDuplicateDialog.value = true
    Log.w("CargoViewModel_Log", "حواله‌های تکراری شناسایی شدند: ...")
}
```

یعنی وجود `trackingNumber` تکراری در داده‌ی تولید یک واقعیت شناخته‌شده است که کد صریحاً برایش سازوکار هشدار دارد. Compose در برخورد با کلید تکراری در `LazyColumn` استثنای `IllegalArgumentException: Key "..." was already used` پرتاب می‌کند و کل صفحه کرش می‌کند.

بدتر اینکه این کرش با polling ۳۰ ثانیه‌ای هم می‌تواند رخ دهد: کافی است در فاصله‌ی دو بارگذاری، حواله‌ی تکراری در سرور ثبت شود.

**راه‌حل:** استفاده از کلید اصلی رکورد:

```kotlin
key = { it.id ?: it.trackingNumber.hashCode() }
```

یا برای اطمینان کامل `key = { "${it.id}-${it.trackingNumber}" }`.

---

## P0-4. فیلتر جستجو هر ۳۰ ثانیه (و بعد از هر تأیید) بی‌صدا پاک می‌شود

**شواهد:**

```kotlin
// ui/viewmodel/CargoViewModel.kt:658-660
_cargoInfoList.value = result.cargoInfoList
_initialInfo.value = result.initialInfo
_filteredCargoInfoList.value = result.cargoInfoList   // ← فیلتر فعال دور ریخته می‌شود
```

سازوکار جبرانی صفحه این است:

```kotlin
// CargoDetailsScreen.kt:295
LaunchedEffect(cargoInfoList) {
    viewModel.filterCargoInfoList(searchQuery)
    viewModel.updateInfoValues()
}
```

اما `cargoInfoList` یک `StateFlow` است و مقادیر برابر را conflate می‌کند. در یک تیک polling که هیچ حواله‌ای تغییر نکرده باشد (حالت غالب)، `_cargoInfoList` امیت نمی‌کند، پس `LaunchedEffect` اجرا نمی‌شود — ولی `_filteredCargoInfoList` همین حالا با لیست کامل بازنویسی شده است.

**نتیجه:** کاربر شماره حواله را جستجو کرده، لیست به یک ردیف رسیده؛ ۳۰ ثانیه بعد لیست کامل برمی‌گردد در حالی که متن جستجو هنوز در کادر است. هیچ نشانه‌ای هم به کاربر داده نمی‌شود.

همین باگ به شکل صریح‌تر بعد از هر تأیید هم رخ می‌دهد:

```kotlin
// ui/viewmodel/CargoViewModel.kt:182
filterCargoInfoList("")   // ← جستجوی جاری نادیده گرفته می‌شود
```

**ریسک عملیاتی:** روی صفحه‌ای که کار اصلی‌اش «تپ کردن روی یک حواله و تأیید آن» است، تغییر ناگهانی محتوای لیست زیر انگشت کاربر می‌تواند به تأیید حواله‌ی اشتباه منجر شود (و طبق P0-2 هیچ راه بازگشتی هم در سرور تعریف نشده — تأیید غیرقابل لغو است).

**راه‌حل:** نگه‌داشتن `searchQuery` به‌عنوان state در ViewModel و اعمال آن در انتهای `loadCargoInfoList`:

```kotlin
private val _searchQuery = MutableStateFlow("")
// در انتهای loadCargoInfoList:
_filteredCargoInfoList.value = applyFilter(result.cargoInfoList, _searchQuery.value)
```

و حذف `filterCargoInfoList("")` از `updateCargoConfirmation`.

---

# P1 — بالا

## P1-1. پیام معنادار ۴۰۹ سرور دور ریخته می‌شود

**شواهد سرور** (کامنت خودِ کد توضیح می‌دهد که این تغییر عمدی بوده):

```php
// PHP/src/Controllers/CargoController.php:240-245
// این یک خطای واقعی است (تأیید تکراری یا رقابت هم‌زمانی)، نه موفقیت؛
// کد ۲۰۰ باعث می‌شد کلاینت آن را success بداند ...
$this->sendJsonResponse(["status" => "error",
    "message" => "حواله قبلاً تأیید شده است یا تغییری اعمال نشد"], 409);
```

**شواهد کلاینت:**

```kotlin
// CargoDetailsScreen.kt:112-114
} else {
    Result.failure(Exception("خطا در ارتباط با سرور: ${response.code()}"))
}
```

`response.errorBody()` هرگز خوانده نمی‌شود. همین برای ۴۰۱ (نشست منقضی)، ۴۰۴ (حواله حذف شده) و ۴۰۳ صادق است — کاربر برای هر سه حالت متفاوت، یک پیام بی‌معنی با یک عدد می‌بیند.

**راه‌حل:**

```kotlin
val errorJson = response.errorBody()?.string()
val serverMessage = runCatching {
    JsonParser.parseString(errorJson).asJsonObject.get("message")?.asString
}.getOrNull()
Result.failure(Exception(serverMessage ?: "خطا در ارتباط با سرور: ${response.code()}"))
```

---

## P1-2. نبود قفل ضد دوبار-زدن روی دکمه «تأیید حواله»

**شواهد:**

```kotlin
// CargoDetailsScreen.kt:460-473
onConfirm = {
    coroutineScope.launch {
        handleCargoConfirmation(...)   // POST + سپس GET کامل لیست
        selectedCargoInfo = null       // ← دیالوگ فقط در انتهای همه‌چیز بسته می‌شود
    }
}
```

```kotlin
// components/CargoDetailsDialogSection.kt:640
Button(onClick = onConfirm, ...)   // نه enabled، نه نشانگر loading
```

بین تپ کاربر و بسته‌شدن دیالوگ، یک POST به `confirm_cargo.php` و یک GET کامل به `getInitialInfo.php` (دو کوئری سنگین) فاصله است — روی شبکه‌ی موبایل به‌راحتی چند ثانیه. در این بازه دکمه فعال و بدون هیچ بازخوردی است.

**اثر:** هر تپ اضافه یک coroutine و یک POST جدید می‌سازد. درخواست دوم به‌خاطر شرط `WHERE ... confirm != 'تائید شده'` با `affected = 0` و کد ۴۰۹ برمی‌گردد، پس کاربری که عملیاتش **موفق** بوده پیام **خطا** می‌بیند. ضمناً دو بار بارگذاری کامل لیست به سرور تحمیل می‌شود.

**راه‌حل:** یک `var isConfirming by remember { mutableStateOf(false) }`، پاس‌دادن آن به `CargoDetailsDialog` برای `enabled = !isConfirming` و نمایش `CircularProgressIndicator` داخل دکمه.

---

## P1-3. اعتبارسنجی نشست فقط یک بار انجام می‌شود و ۴۰۱ در ادامه مدیریت نمی‌شود

**شواهد:**

```kotlin
// CargoDetailsScreen.kt:257
LaunchedEffect(Unit) {
    val result = validateServerSession(userPreferencesManager)
    ...
}
```

این فقط در اولین composition اجرا می‌شود. بعد از آن حلقه‌ی ۳۰ ثانیه‌ای تا هر زمان که صفحه باز باشد ادامه می‌یابد. اگر ادمین کاربر را force-logout کند یا نشست منقضی شود، سرور به‌درستی ۴۰۱ برمی‌گرداند (`AuthenticatesRequests.php:34`) اما کلاینت:

- در مسیر لیست: `ReportsRepository.getCargoInfo` استثنا پرتاب می‌کند → `loadCargoInfoList` فقط یک اسنک‌بار خطا نشان می‌دهد و هر ۳۰ ثانیه تکرار می‌کند
- در مسیر تأیید: پیام «خطا در ارتباط با سرور: 401»

کاربر نه خارج می‌شود، نه به صفحه ورود هدایت می‌شود، فقط با یک صفحه‌ی از کار افتاده می‌ماند.

**راه‌حل:** یک `Interceptor` (یا `Authenticator`) سراسری در `RetrofitClient` که روی هر پاسخ ۴۰۱، `clearUserCredentials` را صدا بزند و رویداد خروج را به لایه‌ی ناوبری بدهد. این مشکل مختص این صفحه نیست ولی اینجا به‌خاطر polling دائمی بیشترین نمود را دارد.

---

## P1-4. `handleQuotaChangeInDetails` دقیقاً همان ضدالگویی است که در ViewModel رفع شده

**شواهد:**

```kotlin
// CargoDetailsScreen.kt:174-188
val fullQuotaInt = fullQuotaNumber.toIntOrNull() ?: 0
val newInitialInfo = InitialInfo(
    ...
    cargoWeight = 0f,
    loadingQuotaNumber = fullQuotaInt,
    remainingWeight = 0f,
    totalNetWeight = 0f,
    averageNetWeight = 0f,
    remainingServices = 0
)
viewModel.setInitialInfo(newInitialInfo)
```

و در ViewModel، تابعی که **مخصوصاً برای رفع همین** نوشته شده:

```kotlin
// ui/viewmodel/CargoViewModel.kt:140-160
// برخلاف پیاده‌سازی قبلی که یک InitialInfo ناقص با صفرهای دستی می‌ساخت و
// آن را بلافاصله روی state می‌نشاند (تناژ/تعداد سرویس صفر لحظه‌ای روی UI،
// و tempTonnageStatus=false که کنترل تناژ موقت را در همان بازه دور می‌زد)،
// ... quota.quotaNumber به‌صورت String به loadCargoInfoList می‌رود، پس
// صفرهای ابتدایی (مثل «0123») برخلاف toIntOrNull() قبلی از بین نمی‌روند.
fun switchQuota(quota: MatchingQuota) { ... }
```

سه اثر مشخص، همان‌ها که کامنت بالا نام برده است:

1. **پرش تناژ به صفر** روی `InitialInfoSection` تا زمانی که `loadCargoInfoList` برگردد
2. **`tempTonnageStatus = false`** — کنترل تناژ موقت در همین بازه دور زده می‌شود
3. **`toIntOrNull()`** صفرهای ابتدایی کوتاژ را می‌خورد؛ کوتاژ `0123` تبدیل به `123` می‌شود و `currentQuota` در `QuotaEntryDialog` (خط ۵۰۳) هم اشتباه پر می‌شود

**راه‌حل:** کل تابع `handleQuotaChangeInDetails` (خطوط ۱۵۶–۲۱۱) حذف و به‌جایش `viewModel.switchQuota(selectedQuota)` صدا زده شود. اعتبارسنجی «وجود، تعلق به همین کشتی، فعال بودن» را `QuotaEntryDialog` از قبل انجام داده و در `handleQuotaChangeInDetails` بی‌دلیل تکرار شده است.

---

## P1-5. نشت پیام خطای داخلی سرور به کلاینت

**شواهد:**

```php
// PHP/src/Controllers/CargoController.php:250-252
} catch (Exception $e) {
    $this->logger->error("Error in confirmCargo: " . $e->getMessage());
    $this->sendJsonResponse(["status" => "error", "message" => $e->getMessage()], 500);
}
```

در مقابل، الگوی درست در همین فایل رعایت شده:

```php
// خط ۱۷۵ (updateCargoInfo)
$this->sendJsonResponse(['error' => true, 'message' => 'خطای داخلی سرور رخ داده است.'], 500);

// خط ۵۲۶ (getInitialInfo)
"message" => "خطایی در سیستم رخ داده است. لطفاً بعداً تلاش کنید."
```

**اثر:** خطای mysqli (شامل نام جدول/ستون و بخشی از کوئری) مستقیم به کلاینت می‌رود. برای مهاجم، نقشه‌ی اسکیمای دیتابیس.

**راه‌حل:** پیام عمومی به کلاینت، جزئیات فقط در `logger`.

---

# P2 — متوسط

## P2-1. رمزگشایی سه‌باره URL و ناسازگاری پارامترها بین بارگذاری اول و بروزرسانی‌ها

**شواهد:**

ناوبری type-safe کامپوز (`toRoute()`) خودش آرگومان‌ها را decode می‌کند. با این حال دو لایه decode دستی هم اضافه شده:

```kotlin
// feature/reports/navigation/ReportsNavigation.kt:57-59  — decode دوم
shippingCompany = route.shippingCompany?.let { URLDecoder.decode(it, "UTF-8") } ?: "",

// CargoDetailsScreen.kt:276-278  — decode سوم
val decodedShippingCompany = URLDecoder.decode(shippingCompany, "UTF-8")
```

و بدتر از آن، مقادیر decode‌شده **فقط** در بارگذاری اول استفاده می‌شوند. سه مسیر دیگر مقدار خام را می‌فرستند:

| مسیر | خط | مقدار ارسالی |
|---|---|---|
| بارگذاری اول | ۲۸۰–۲۸۴ | `decodedShippingCompany` |
| polling ۳۰ ثانیه‌ای | ۳۰۶–۳۱۱ | `shippingCompany` خام |
| refresh دستی | ۳۴۱–۳۴۶ | `shippingCompany` خام |
| reload بعد از تأیید | ۴۶۶–۴۷۰ | `shippingCompany` خام |

**اثر:** برای رشته‌های فارسی معمولی این تفاوت خنثی است، ولی:
- نام شرکتی حاوی `%` → `URLDecoder.decode` استثنای `IllegalArgumentException` پرتاب می‌کند، آن هم داخل `LaunchedEffect` → کرش صفحه
- نام حاوی `+` → تبدیل به فاصله؛ بارگذاری اول و بروزرسانی‌ها دو رشته‌ی متفاوت به سرور می‌فرستند و بروزرسانی با ۴۰۴ «اطلاعات وارد شده مطابقت ندارد» شکست می‌خورد

**راه‌حل:** حذف هر دو لایه‌ی decode دستی (هم در `ReportsNavigation.kt` و هم در صفحه) و اتکا به decode خودکار ناوبری. `URLDecoder.decode(String, String)` هم در Android API 33+ به نفع `decode(String, Charset)` منسوخ شده است.

---

## P2-2. منطق شبکه و کسب‌وکار به‌صورت تابع top-level داخل فایل UI

**شواهد:** `CargoDetailsScreen.kt` خطوط ۹۸–۲۱۱ سه تابع `suspend` سطح فایل دارد:

```kotlin
suspend fun confirmCargo(info: CargoInfo, username: String, userType: String): Result<String> {
    val response = RetrofitClient.apiService.confirmCargo(requestBody)   // ← دسترسی مستقیم به singleton
```

این‌ها نه در `ReportsRepository` هستند، نه در `CargoViewModel`، نه از Koin عبور می‌کنند. `RetrofitClient` یک `object` سراسری است، پس این توابع در تست واحد اصلاً قابل mock کردن نیستند. `handleCargoConfirmation` هم `viewModel` را به‌عنوان پارامتر می‌گیرد که وارونگی کامل جهت وابستگی است (UI → تابع سطح فایل → ViewModel).

**راه‌حل:**
- `ReportsRepository.confirmCargo(cargoId: Int): Result<String>`
- `CargoViewModel.confirmCargo(cargoId: Int)` که خودش refresh و پیام را مدیریت کند
- صفحه فقط `viewModel.confirmCargo(info.id)` را صدا بزند

---

## P2-3. ارسال `username`/`userType` در بدنه — کد مرده و گمراه‌کننده

سرور از نسخه‌ی فعلی این مقادیر را کاملاً نادیده می‌گیرد:

```php
// PHP/src/Controllers/CargoController.php:202-205
// username/userType از نشست معتبرشده گرفته می‌شود، نه از بدنه‌ی درخواست
$username = (string)$this->authenticatedUsername;
```

اما کلاینت هنوز:
- `username`/`userType` را با `collectAsState` می‌خواند (خطوط ۲۳۱–۲۳۲)
- در بدنه‌ی درخواست می‌گذارد (خطوط ۱۰۰–۱۰۴)
- از چهار لایه تابع عبورشان می‌دهد (خطوط ۱۲۱–۱۳۲، ۴۶۵–۴۶۶)

**راه‌حل:** حذف کامل هر دو پارامتر از امضای توابع و بدنه‌ی درخواست. این کد این توهم را ایجاد می‌کند که هویت از کلاینت می‌آید — یعنی دقیقاً همان چیزی که سرور عمداً بسته است.

---

## P2-4. بروزرسانی خوش‌بینانه‌ی نادرست و بی‌فایده

```kotlin
// ui/viewmodel/CargoViewModel.kt:170-177
fun updateCargoConfirmation(cargoId: Int?) {
    ... cargoInfo.copy(
        confirm = "تائید شده",
        exitTime = cargoInfo.exitTime?.takeIf { it.isNotBlank() } ?: getCurrentTime(),
        exitDate = cargoInfo.exitDate?.takeIf { it.isNotBlank() } ?: getCurrentDate()
    )
```

اما سرور در تأیید حواله اصلاً `exitTime`/`exitDate` را تغییر نمی‌دهد:

```php
// PHP/src/Repositories/CargoRepository.php:268-273
UPDATE CargoInfo SET confirm = 'تائید شده', confirm_username = ?,
                     confirm_usertype = ?, updated_at = NOW() WHERE id = ?
```

پس کلاینت برای لحظاتی زمان خروجی نشان می‌دهد که در دیتابیس وجود ندارد. ضمناً بلافاصله بعد از آن `loadCargoInfoList` صدا زده می‌شود (`CargoDetailsScreen.kt:136`) که همه چیز را از سرور بازنویسی می‌کند — یعنی این بروزرسانی خوش‌بینانه هم **نادرست** است هم **بی‌اثر**.

**راه‌حل:** یا فقط `confirm` محلی بروزرسانی شود، یا کلاً حذف شود و به reload سرور تکیه شود.

---

## P2-5. `JSON_NUMERIC_CHECK` در پاسخ تأیید

```php
// PHP/src/Controllers/CargoController.php:235
echo json_encode($response, JSON_UNESCAPED_UNICODE | JSON_NUMERIC_CHECK);
```

`JSON_NUMERIC_CHECK` هر رشته‌ی عددی را به عدد تبدیل می‌کند. `trackingNumber` و `loadingQuotaNumber` در دیتابیس رشته‌اند و می‌توانند صفر ابتدایی داشته باشند: `"0123"` در پاسخ به `123` تبدیل می‌شود.

ضمناً این تنها مسیری در کل کنترلر است که به‌جای `sendJsonResponse` مستقیم `echo` می‌کند — یعنی فشرده‌سازی gzip و `JSON_THROW_ON_ERROR` را هم از دست می‌دهد.

**راه‌حل:** استفاده از `$this->sendJsonResponse($response, 200)` مثل بقیه‌ی مسیرها.

---

## P2-6. ثبت نشدن زمان تأیید در دیتابیس (نقص در زنجیره ردیابی)

سرور `confirmTime` و `confirmDate` را در پاسخ می‌سازد:

```php
// PHP/src/Controllers/CargoController.php:216-217
$confirmTime = date('H:i:s');
$confirmDate = date('Y/m/d');
```

اما در دیتابیس چیزی جز `updated_at = NOW()` ذخیره نمی‌شود، و `updated_at` با هر ویرایش بعدی همان رکورد بازنویسی می‌شود. یعنی برای عملیاتی که «چه کسی» را ذخیره می‌کند (`confirm_username`)، «چه زمانی» قابل بازیابی نیست.

ضمناً `date('Y/m/d')` تاریخ **میلادی** برمی‌گرداند در حالی که کل سیستم شمسی است و `jdf.php` / `jdate()` در همین کنترلر (خط ۴۵۹) استفاده می‌شود — پس این مقدار حتی به‌عنوان نمایش هم با بقیه‌ی اپ ناسازگار است.

**راه‌حل:** ستون `confirm_at DATETIME` به `CargoInfo` اضافه و در همان UPDATE پر شود.

---

## P2-7. هزینه‌ی واقعی polling سی‌ثانیه‌ای

هر تیک، به ازای هر کاربر باز روی این صفحه:

| درخواست | هزینه سرور |
|---|---|
| `getInitialInfo.php` | ۳ کوئری، دوتای آن‌ها `SELECT *` روی `InitialInfo` و `CargoInfo` (خطوط ۴۶۲، ۵۰۶) |
| `getLoadableTonnage` | یک درخواست جدا از `loadCargoInfoList` (خط ۶۸۰) |
| هر دو درخواست | `isValidToken` + **`updateLastActivity` (یک نوشتن)** + `getByUsername` در `requireAuthenticatedSession` |

یعنی حداقل ۴ درخواست در دقیقه به ازای هر کاربر، که هرکدام یک نوشتن روی جدول نشست دارد. سقف نرخ پروکسی ۶۰ درخواست در دقیقه **به ازای IP** است (`protected_proxy.php:22`) و کل کاربران یک دفتر معمولاً پشت یک NAT هستند — با ۱۰ کاربر همزمان روی این صفحه، بودجه‌ی نرخ صرفاً با polling پر می‌شود.

**راه‌حل‌های به ترتیب اثر:**
1. `SELECT *` را به فهرست صریح ستون‌ها تبدیل کنید (به‌ویژه `CargoInfo` که ستون‌های زیادی دارد و کلاینت همه را نمی‌خواهد)
2. `ETag`/`If-None-Match` روی `getInitialInfo` (بر اساس `MAX(updated_at)` کوئری) تا در حالت بدون‌تغییر پاسخ ۳۰۴ خالی برگردد؛ `OkHttp` کش دیسک را از قبل دارد (`RetrofitClient.kt:120`)
3. افزایش بازه به ۶۰ ثانیه یا backoff تدریجی وقتی چند تیک متوالی بدون تغییر بوده

---

## P2-8. نمایش اسنک‌بار «موفقیت» در هر تیک polling

```kotlin
// CargoDetailsScreen.kt:312-315
) {
    viewModel.updateInfoValues()
    showUpdateMessage("اطلاعات با موفقیت بروزرسانی شد", MessageType.SUCCESS)
}
```

هر ۳۰ ثانیه یک پیام موفقیت که کاربر درخواستش نکرده. این پیام فقط برای refresh **دستی** (خط ۳۵۰) معنا دارد. اثر جانبی‌اش این است که کاربر یاد می‌گیرد اسنک‌بارها را نادیده بگیرد — و همان‌جا پیام خطای واقعی هم گم می‌شود.

**راه‌حل:** یک پارامتر `showMessage: Boolean` به `refreshData` و `false` برای مسیر polling.

---

## P2-9. محاسبات تکراری در مسیر recomposition

**الف) `derivedStateOf` داخل `remember(key)`:**

```kotlin
// CargoDetailsScreen.kt:250-255
val groupedCargoList by remember(filteredCargoInfoList) {
    derivedStateOf {
        filteredCargoInfoList.groupBy { it.confirm == "تائید شده" }.toSortedMap(compareBy { it })
    }
}
```

با دادن `filteredCargoInfoList` به‌عنوان کلیدِ `remember`، هر تغییر لیست یک شیء `derivedStateOf` **جدید** می‌سازد — یعنی دقیقاً همان چیزی که `derivedStateOf` قرار بود از آن جلوگیری کند. شکل درست: `remember { derivedStateOf { ... } }` (بدون کلید؛ خودِ `derivedStateOf` تغییرات state را دنبال می‌کند).

**ب) مرتب‌سازی داخل بدنه‌ی `LazyColumn`:**

```kotlin
// components/CargoDetailsComponents.kt:758 و 771
items = unconfirmedCargos.sortedByDescending { it.entryTime }
items = confirmedCargos.sortedByDescending { "${it.exitDate} ${it.exitTime}" }
```

این‌ها در هر recomposition دوباره اجرا می‌شوند (و مورد دوم برای هر آیتم یک `String` جدید هم می‌سازد). باید در `remember(groupedCargoList)` نگه داشته شوند.

**ج)** ضمناً سرور از قبل مرتب‌سازی را انجام داده (`ORDER BY ... entryTime DESC` در `CargoController.php:506`)، پس مرتب‌سازی اول احتمالاً کاملاً زائد است.

---

## P2-10. دکمه «تغییر کشتی» در مسیر ناوبری کاملاً بی‌اثر است

```kotlin
// CargoDetailsScreen.kt:421-424
onClick = {
    isFabExpanded = false
    onChangeSelectionClick?.invoke()   // ← اگر null باشد، فقط منو بسته می‌شود
}
```

`onChangeSelectionClick` از `CargoCounterOperationScreen.kt:41` پاس داده می‌شود، اما در `ReportsNavigation.kt:54-61` اصلاً پاس داده نمی‌شود و `null` می‌ماند. کاربری که از مسیر گزارش‌ها وارد شده، دکمه‌ای می‌بیند که هیچ کاری نمی‌کند.

**راه‌حل:** آیتم FAB فقط وقتی `onChangeSelectionClick != null` است رندر شود.

---

## P2-11. کنترل کسب‌وکار فقط سمت کلاینت اعمال می‌شود

```kotlin
// CargoDetailsScreen.kt:475
showConfirmButton = info.status == "ورود" && info.confirm != "تائید شده"
```

سرور فقط شرط `confirm != 'تائید شده'` را در `WHERE` دارد و **هیچ بررسی‌ای روی `status` نمی‌کند**. یعنی حواله‌ای با وضعیت «خروج» از طریق API قابل تأیید است، هرچند UI دکمه‌اش را نشان نمی‌دهد.

**راه‌حل:** افزودن شرط به کوئری `CargoRepository::confirmCargo`:

```php
WHERE id = ? AND status = 'ورود' AND (confirm IS NULL OR confirm != 'تائید شده')
```

---

## P2-12. رشته‌های جادویی و دو املای متفاوت از یک کلمه

`"تائید شده"` در سه لایه‌ی مستقل تکرار شده: `CargoViewModel.kt:174`، `CargoDetailsScreen.kt:252`، `CargoDetailsDialogSection.kt:106`، و `CargoRepository.php:269, 273`. همچنین `"ورود"` و `"خروج"` در Kotlin، PHP و SQL.

بدتر: در UI دو املای متفاوت کنار هم استفاده می‌شود — `"تائید شده"` برای مقدار داده و `"تأیید شده"` برای نمایش (`CargoDetailsDialogSection.kt:191`). هر مقایسه‌ای که به اشتباه از املای نمایشی استفاده کند بی‌صدا `false` می‌شود.

**راه‌حل:** یک `object CargoStatus { const val CONFIRMED = "تائید شده"; ... }` در لایه‌ی مدل و یک ثابت متناظر در PHP.

---

## P2-13. دو سیستم موازی پیام‌رسانی در یک صفحه

- `snackbarMessage` + `StatusSnackbar` (محلی، خطوط ۲۴۲، ۳۷۸–۳۸۴) — برای refresh و تغییر کوتاژ
- `resultMessage`/`showAnimatedMessage` + `MessageDialog` (از ViewModel، خطوط ۲۳۶–۲۳۸، ۴۴۷–۴۵۴) — برای تأیید حواله

نتیجه: تأیید موفق یک **دیالوگ مودال** نشان می‌دهد ولی بروزرسانی موفق یک **اسنک‌بار** — بدون منطق روشنی برای این تفاوت. این دو می‌توانند همزمان روی صفحه باشند.

---

# P3 — کم

| # | یافته | مکان |
|---|---|---|
| P3-1 | `e.printStackTrace()` به‌جای `Log.e` — در بیلد ریلیز به stderr می‌رود و در Crashlytics دیده نمی‌شود | `CargoDetailsScreen.kt:116` |
| P3-2 | فراخوانی شرطی composable: `passedViewModel ?: viewModel(factory = ...)` — اگر مقدار پارامتر بین recompositionها از non-null به null برود، جدول slot خراب می‌شود | `CargoDetailsScreen.kt:229` |
| P3-3 | ساخت دستی `ReportsRepository` در لایه UI در حالی که `UserPreferencesManager` از Koin می‌آید — DI ناسازگار | `CargoDetailsScreen.kt:226-228`، `ReportsNavigation.kt:52` |
| P3-4 | `var confirm: String` در `data class CargoInfo` — فیلد قابل تغییر در مدلی که به‌عنوان state کامپوز استفاده می‌شود؛ تغییر مستقیم recomposition ایجاد نمی‌کند | `CargoModels.kt:28` |
| P3-5 | تابع `refreshData` صرفاً یک wrapper بدون ارزش افزوده روی `viewModel.loadCargoInfoList` است | `CargoDetailsScreen.kt:81-96` |
| P3-6 | نام‌گذاری `decodedcargoType` (بدون camelCase) | `CargoDetailsScreen.kt:278` |
| P3-7 | `confirmCargo` سرور هدرهای `X-Content-Type-Options`/`X-Frame-Options` را که `updateCargoInfo` و `searchByScaleReceipt` می‌گذارند، ندارد | `CargoController.php:183` |
| P3-8 | fallback محدودیت نرخ پروکسی (وقتی APCu نصب نباشد) بدون قفل روی فایل می‌نویسد — رِیس در شرایط همزمانی | `protected_proxy.php:140` |
| P3-9 | `Modifier.clickable` روی `Surface` بدون `role = Role.Button` — TalkBack آن را به‌عنوان دکمه اعلام نمی‌کند | `CargoDetailsScreen.kt:518` |
| P3-10 | ارتفاع مؤثر `FloatingActionButtonItem` حدود ۴۲dp است، کمتر از حداقل ۴۸dp هدف لمسی متریال | `CargoDetailsScreen.kt:526` |
| P3-11 | `matchingQuotas.first()` بدون منطق انتخاب وقتی چند کوتاژ منطبق برگردد | `CargoDetailsScreen.kt:171` |

---

# ترتیب پیشنهادی اقدام

**گام اول — امنیت سرور (چند خط، بدون تغییر کلاینت):**
1. `requirePermission('cargo_counter')` در `confirmCargo` — P0-1
2. مقید کردن `WHERE` به `loadingQuotaNumber`/`shipName` و `status = 'ورود'` — P0-2، P2-11
3. پیام عمومی در بلوک `catch` — P1-5
4. `sendJsonResponse` به‌جای `echo json_encode(..., JSON_NUMERIC_CHECK)` — P2-5

**گام دوم — کرش و صحت داده در کلاینت:**
5. اصلاح کلید `LazyColumn` — P0-3
6. نگه‌داشتن `searchQuery` در ViewModel و اعمالش بعد از هر بارگذاری — P0-4
7. خواندن `errorBody` و نمایش پیام واقعی سرور — P1-1
8. قفل ضد دوبار-زدن روی دکمه تأیید — P1-2

**گام سوم — بازسازی معماری:**
9. انتقال `confirmCargo`/`handleCargoConfirmation` به Repository و ViewModel — P2-2، P2-3
10. جایگزینی `handleQuotaChangeInDetails` با `viewModel.switchQuota` — P1-4
11. حذف لایه‌های اضافی `URLDecoder` — P2-1
12. مدیریت سراسری ۴۰۱ — P1-3

**گام چهارم — عملکرد:**
13. حذف `SELECT *`، افزودن ETag، حذف اسنک‌بار polling — P2-7، P2-8
14. اصلاح `derivedStateOf` و انتقال مرتب‌سازی‌ها به `remember` — P2-9

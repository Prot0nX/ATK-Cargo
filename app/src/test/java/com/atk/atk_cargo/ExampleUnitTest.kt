package com.atk.atk_cargo

import org.junit.Assert.assertEquals
import org.junit.Test

// تست واحد ساده که روی ماشین توسعه اجرا میشود.
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
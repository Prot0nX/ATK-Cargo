# گزارش جامع بررسی دیالوگ «تحلیل جامع عملیات» (Comprehensive Analytics)

تاریخ بررسی: ۱۴۰۵/۰۵/۲۲ (۲۰۲۶-۰۸-۱۳) · شاخه: `main` · کامیت پایه: `806eaa4`

---

## ۱. دامنه بررسی و نقشه جریان داده

**کلاینت (Android/Compose)**

| لایه | فایل |
|---|---|
| پوسته دیالوگ، هدر، ناوبری تاریخ | `app/src/main/java/com/atk/atk_cargo/feature/reports/presentation/dialogs/ComprehensiveAnalyticsDialog.kt` |
| بدنه اصلی (گروه‌بندی، کارت‌ها، اشتراک‌گذاری) | `.../dialogs/QuotaAnalysisSection.kt` |
| میزبان دیالوگ | `app/src/main/java/com/atk/atk_cargo/ui/screens/ManageReportsScreen.kt:261-265, 274-276` |
| ViewModel | `app/src/main/java/com/atk/atk_cargo/ui/viewmodel/ReportsViewModel.kt:104-117, 129-136, 166-231, 872-909` |
| Repository | `app/src/main/java/com/atk/atk_cargo/data/repository/ReportsRepository.kt:497-504` |
| Retrofit | `app/src/main/java/com/atk/atk_cargo/api/ApiService.kt:258-263` |
| مدل‌ها | `app/src/main/java/com/atk/atk_cargo/data/model/ReportModels.kt:206-252` |
| هدرهای نشست | `app/src/main/java/com/atk/atk_cargo/api/RetrofitClient.kt:83-100` |

**سرور (PHP)**

| لایه | فایل |
|---|---|
| Entry point | `PHP/realTimeLoadingData.php` |
| Proxy | `PHP/protected_proxy.php` |
| Controller | `PHP/src/Controllers/AnalyticsController.php:36-47, 52-85, 293-356` |
| مجوزها | `PHP/config/permissions.json` · `PHP/src/Services/PermissionService.php` |
| اسکیمای دیتابیس | `PHP/schema.sql:20-73` |

**جریان:**

```
FAB «آمار جامع» → showAnalyticsDialog = true
  → ComprehensiveAnalyticsDialog (LaunchedEffect)
  → ReportsViewModel.loadComprehensiveAnalytics()
  → ReportsRepository.getComprehensiveAnalysis(offset)
  → GET protected_proxy.php?target=realTimeLoadingData.php&action=getComprehensiveAnalysis&offset=N
  → AnalyticsController::handleComprehensiveAnalysisRequest()
  → JSON { success, data: { dateInfo, quotaCompletionAnalysis[] } }
  → _comprehensiveAnalytics  →  QuotaAnalysis(completionData)
  → viewModel.updateInitialQuotas(...)  →  _filteredQuotas  →  UI
```

نکته معماری مهم همین سطر آخر است: داده از ViewModel به UI می‌رود و **دوباره از UI به ViewModel برمی‌گردد** تا فیلتر شود (بخش C-1).

---

## ۲. خلاصه مدیریتی

| حوزه | وضعیت | مهم‌ترین مورد |
|---|---|---|
| امنیت | 🔴 بحرانی | مجوز `view_reports` («مشاهده آمار تحلیلی») در **هیچ‌جای سرور** بررسی نمی‌شود؛ هر کاربر با نشست معتبر — حتی `operator` و `verifier` که این مجوز برایشان `false` است — کل داده تجاری ۸ روز اخیر را می‌گیرد |
| باگ عملکردی | 🔴 جدی | برچسب «امروز» روی پنجره‌ای است که **دیروز ۰۷:۰۰ تا امروز ۰۷:۰۰** را می‌شمارد؛ عملیات امروز از ۰۷:۰۰ تا لحظه حاضر با هیچ offsetی قابل مشاهده نیست |
| پرفورمنس | 🔴 جدی | هیچ ایندکسی روی `CargoInfo.exitDate` وجود ندارد ⇒ هر باز شدن دیالوگ یک **full table scan** روی بزرگ‌ترین جدول است؛ برخلاف endpoint خواهر، نه MicroCache دارد نه ETag |
| باگ همزمانی | 🟠 متوسط | اولین باز شدن دیالوگ **دو درخواست همسان** می‌زند؛ تعویض سریع تاریخ race می‌سازد و درخواست قبلی لغو نمی‌شود |
| کیفیت کد | 🟠 متوسط | رفت‌وبرگشت داده UI↔ViewModel، منطق گروه‌بندی سه‌بار تکرار شده، کلید رشته‌ای `"a|b|c"` با split دستی، خط لوله مرتب‌سازی ViewModel که خروجی‌اش دور ریخته می‌شود |
| UX | 🟠 متوسط | حالت خطا دکمه «تلاش مجدد» ندارد؛ بعد از هر بارگذاری یک فریم داده تاریخ قبلی نمایش داده می‌شود |

---

## ۳. امنیت

### 🔴 A-1 | مجوز `view_reports` هرگز اعمال نمی‌شود (Broken Access Control)

پروژه دقیقاً برای همین قابلیت یک مجوز تعریف کرده است:

```php
// PHP/PermissionManager.php:152
'view_reports' => ['label' => 'مشاهده آمار تحلیلی', ...]
```

```json
// PHP/config/permissions.json
"admin":    { "view_reports": true  },
"operator": { "view_reports": false },
"verifier": { "view_reports": false }
```

اما جست‌وجوی کامل نشان می‌دهد این کلید **در هیچ کجای `PHP/src/` بررسی نمی‌شود** و در کلاینت هم استفاده‌ای ندارد:

```bash
$ grep -rn "view_reports" PHP/src/      # هیچ نتیجه‌ای
$ grep -rn "view_reports" app/src/      # هیچ نتیجه‌ای
```

`AnalyticsController::requireAuthenticatedSession()` (`AnalyticsController.php:36-47`) فقط **احراز هویت** (authentication) می‌کند، نه **تعیین سطح دسترسی** (authorization). این در حالی است که خود پروژه الگوی درست را در `AppApiController` دارد:

```php
// PHP/src/Controllers/AppApiController.php:61-72
private function requirePermission(string $feature): void {
    ...
    if (!$this->permissionService->hasPermission($username, $userType, $feature)) {
        http_response_code(403);
        echo json_encode(['error' => 'شما مجوز انجام این عملیات را ندارید.'], JSON_UNESCAPED_UNICODE);
        exit;
    }
}
```

و در ۹ نقطه (`AppApiController:221-292`، `CargoController:100,194,284`) از آن استفاده می‌کند.

**نتیجه عملی:** یک کاربر `operator` (که در UI حتی منوی گزارشات را نمی‌بیند، چون `HomeQuotaParser.kt:15` روی `manage_ships` گیت می‌کند) با نشست معتبر خودش می‌تواند اجرا کند:

```bash
curl -H "X-Username: op1" -H "X-Device-Id: ..." -H "X-Session-Token: ..." \
  "https://<host>/protected_proxy.php?target=realTimeLoadingData.php&action=getComprehensiveAnalysis&offset=0"
```

و کل کوتاژها، صاحبان کالا، شرکت‌های باربری، انبارها و تناژ خالص را دریافت کند. کنترل دسترسی این قابلیت **فقط سمت کلاینت** است و کلاینت قابل اعتماد نیست.

نکته دوم: گیت کلاینتی هم روی مجوز اشتباه است — صفحه گزارشات با `manage_ships` باز می‌شود نه `view_reports`، و دکمه FAB «آمار جامع» (`ReportsCommonWidgets.kt:183-189`) هیچ شرطی ندارد.

**اصلاح پیشنهادی:**

```php
// AnalyticsController.php — افزودن به کنار requireAuthenticatedSession
private ?string $authenticatedUsername = null;

private function requirePermission(string $feature): void {
    $user = (new \App\Repositories\UserRepository())
        ->getByUsername($this->authenticatedUsername ?? '');
    $userType = (string)($user['userType'] ?? '');

    if (!(new \App\Services\PermissionService())
            ->hasPermission($this->authenticatedUsername ?? '', $userType, $feature)) {
        header('Content-Type: application/json; charset=UTF-8');
        http_response_code(403);
        echo json_encode(['error' => 'شما مجوز مشاهده آمار تحلیلی را ندارید.'], JSON_UNESCAPED_UNICODE);
        exit;
    }
}
```

```php
// در switch مربوط به handleRealTimeLoadingData
case 'getComprehensiveAnalysis':
    $this->requirePermission('view_reports');
    $this->handleComprehensiveAnalysisRequest();
    break;
```

و سمت کلاینت، گیت‌کردن آیتم FAB بر اساس همان مجوز (تا کاربر با ۴۰۳ روبه‌رو نشود).

> این الگو باید به `getRealTimeData` و `getKotazhInfo` روی همین کنترلر هم اعمال شود.

---

### 🟠 A-2 | `offset` سمت سرور کلمپ نمی‌شود

```php
// AnalyticsController.php:294
$offset = (int)($this->request->get('offset', 0));
```

هیچ محدودیتی روی بازه نیست. این دقیقاً همان جایی است که در تابع خواهر **درست** انجام شده:

```php
// AnalyticsController.php:173
$shiftOffset = max(-14, min(0, (int)$this->request->get('shiftOffset', 0)));
```

محدودیت ۸ روزه (`-7..0`) فقط سمت کلاینت است، در دو نقطه:

```kotlin
// ReportsViewModel.kt:113
if (offset in -7..0) { ... }
// ComprehensiveAnalyticsDialog.kt:225
enabled = offset > -7
```

پیامدها:

1. **دسترسی به هر تاریخی از تاریخچه** — `offset=-3650` کاملاً کار می‌کند و داده ۱۰ سال پیش را برمی‌گرداند. محدوده محصولی «۸ روز» عملاً وجود ندارد.
2. **تاریخ‌های آینده** — `offset=+30` پذیرفته می‌شود (تابع خواهر با `min(0, ...)` جلوی این را می‌گیرد).
3. **ریسک خطای مرگبار** — با مقدار بسیار بزرگ، `$currentTime + ($offset * 86400)` در PHP سرریز عدد صحیح می‌کند و به `float` تبدیل می‌شود؛ ارسال آن به `jdate()`/`date()` می‌تواند `TypeError` بدهد. توجه کنید که `TypeError` از `Error` ارث می‌برد نه `Exception`، پس `catch (Exception $e)` در `handleRealTimeLoadingData` (خط ۸۱) آن را نمی‌گیرد و به یک خطای مرگبار مدیریت‌نشده تبدیل می‌شود.

**اصلاح:**

```php
$offset = max(-7, min(0, (int)$this->request->get('offset', 0)));
```

بهتر است این عدد `7` به یک ثابت مشترک (`private const MAX_ANALYTICS_DAYS_BACK = 7;`) تبدیل شود و کلاینت هم از همان مقدار — نه دو عدد hardcode شده در دو فایل — تبعیت کند.

---

### 🟠 A-3 | نشت پیام خام سرور در UI و عدم تشخیص ۴۰۱

```kotlin
// ReportsRepository.kt:497-504
suspend fun getComprehensiveAnalysis(offset: Int = 0): ComprehensiveAnalysisResponse {
    val response = apiService.getComprehensiveAnalysis(offset = offset)
    if (response.isSuccessful) {
        return response.body() ?: throw Exception("Empty response body")
    } else {
        throw Exception("Error ${response.code()}: ${response.errorBody()?.string()}")
    }
}
```

این تابع دقیقاً بالای `getRealTimeLoadingData` قرار دارد که کامنت صریحی دارد چرا نباید این کار را کرد (`ReportsRepository.kt:393-407`):

```kotlin
// بدون try/catch عمومی (مطابق الگوی getShipDetails)؛ ... کلاینت
// نمی‌توانست بین ۴۰۱ (نشست نامعتبر)، ۴۲۹ (rate limit) و خطای شبکه تشخیص دهد.
throw HttpStatusException(response.code(), "خطا در دریافت ...")
```

مشکلات نسخه Analytics:

- `HttpStatusException` استفاده نمی‌شود ⇒ کلاینت **۴۰۱ را از خطای شبکه تشخیص نمی‌دهد**؛ وقتی نشست منقضی شود کاربر به‌جای هدایت به صفحه ورود، یک کارت خطا می‌بیند.
- بدنه خام خطا مستقیم به پیام کاربر می‌رود. `ReportsViewModel.kt:899` آن را در `LoadingState.Error(e.message)` می‌گذارد و `ErrorStateCard` عیناً نمایش می‌دهد. کاربر چیزی مثل این می‌بیند:

  ```
  Error 401: {"error":"نشست معتبر نیست. لطفاً دوباره وارد شوید."}
  ```

  یعنی JSON خام + پیشوند انگلیسی داخل یک کارت خطای فارسی.
- برخلاف تمام توابع همسایه، `withContext(Dispatchers.IO)` ندارد (تناقض سبکی؛ اثر عملکردی‌اش جدی نیست چون Retrofit خودش dispatch می‌کند).

**اصلاح:**

```kotlin
suspend fun getComprehensiveAnalysis(offset: Int = 0): ComprehensiveAnalysisResponse =
    withContext(Dispatchers.IO) {
        val response = apiService.getComprehensiveAnalysis(offset = offset)
        if (response.isSuccessful) {
            response.body() ?: throw Exception("داده‌های دریافتی خالی است")
        } else {
            throw HttpStatusException(
                response.code(),
                "خطا در دریافت اطلاعات تحلیلی (کد ${response.code()})"
            )
        }
    }
```

و در ViewModel، تفکیک ۴۰۱/۴۲۹ از بقیه (همان کاری که برای real-time انجام شده).

---

### 🟡 A-4 | Rate-limit مشترک روی سنگین‌ترین کوئری

`protected_proxy.php:22-23` سقف ۶۰ درخواست در دقیقه **به ازای هر IP** می‌گذارد. در یک شبکه سازمانی پشت NAT، همه کاربران یک IP دارند؛ یعنی هم سقف زودتر پر می‌شود (کاربر بی‌گناه ۴۲۹ می‌گیرد) و هم مهاجم داخلی می‌تواند تا ۶۰ بار در دقیقه full-scan توصیف‌شده در P-1 را تریگر کند. این endpoint به دلیل هزینه‌اش نیازمند سقف اختصاصی سخت‌گیرانه‌تر (مثلاً ۱۰/دقیقه به ازای هر `username`) است.

---

### 🟡 A-5 | اشتراک‌گذاری داده تجاری بدون تأیید و بدون رد ممیزی

دو نقطه (`QuotaAnalysisSection.kt:202-273` و `481-523`) کل خلاصه عملیاتی — نام کشتی، صاحب کالا، انبار، تعداد حواله و تناژ — را با `Intent.ACTION_SEND` به هر اپلیکیشن نصب‌شده‌ای می‌فرستند:

- هیچ دیالوگ تأییدی وجود ندارد؛ **یک long-press ناخواسته** روی کارت گروه، chooser اشتراک‌گذاری را باز می‌کند.
- هیچ لاگ/ممیزی سمت سرور ثبت نمی‌شود که چه کسی چه داده‌ای را خارج کرده است.
- `context.startActivity(shareIntent)` بدون `try/catch` است (`ActivityNotFoundException` مدیریت نشده)، برخلاف الگوی موجود در `ReportsViewModel.kt:866-868` برای اشتراک‌گذاری PDF.

پیشنهاد: حداقل یک تأیید برای long-press، و ثبت رویداد صادرات در `ActivityLog` سرور.

---

### 🟢 A-6 | مواردی که درست انجام شده‌اند

- تمام کوئری‌ها **prepared statement** با `bind_param` هستند؛ هیچ الحاق رشته‌ای ورودی کاربر در SQL وجود ندارد (`AnalyticsController.php:329-330`).
- هویت از **هدر** خوانده می‌شود نه از Query String، پس در لاگ دسترسی وب‌سرور و پروکسی ذخیره نمی‌شود (کامنت خطوط ۳۰-۳۵ درست و آگاهانه است).
- `Cache-Control: no-store` روی این مسیر ست می‌شود، پس داده تجاری در کش دیسک OkHttp باقی نمی‌ماند.
- پروکسی هدرهای `X-Content-Type-Options`, `X-Frame-Options` را ست می‌کند و `target` را برابر یک whitelist اعتبارسنجی می‌کند (بدون path traversal).

---

## ۴. باگ‌ها

### 🔴 B-1 | پنجره «۲۴ ساعته» با برچسب «امروز» نمی‌خواند — عملیات امروز نامرئی است

کوئری سرور (`AnalyticsController.php:325`):

```sql
WHERE c.status = 'خروج'
  AND ( (c.exitDate = ? AND c.exitTime >= '07:00:00')     -- ← دیروز
     OR (c.exitDate = ? AND c.exitTime <  '07:00:00') )    -- ← امروز
```

```php
$stmt->bind_param("ss", $yesterdayJalaliDate, $todayJalaliDate);
```

یعنی بازه واقعی: **دیروز ساعت ۰۷:۰۰ تا امروز ساعت ۰۷:۰۰**.

اما آن‌چه به کلاینت به‌عنوان تاریخ فرستاده می‌شود، **امروز** است:

```php
$dateInfo = ['jalaliDate' => $todayJalaliDate, 'dayName' => ...];
```

و کلاینت آن را زیر عنوان‌های زیر نمایش می‌دهد:

| محل | متن |
|---|---|
| `ComprehensiveAnalyticsDialog.kt:287` | «گزارشات ۲۴ ساعته» |
| `ComprehensiveAnalyticsDialog.kt:201` | «امروز» (برای `offset == 0`) |
| `QuotaAnalysisSection.kt:313` | «کوتاژ فعالی در **24 ساعت گذشته** وجود ندارد» |

**سناریوی شکست:** ساعت ۱۵:۰۰ روز جاری، کاربر دیالوگ را باز می‌کند و برچسب «امروز» را می‌بیند. داده‌ای که می‌بیند ساعت ۰۷:۰۰ همان روز تمام شده است — **۸ ساعت عملیات امروز اصلاً در آن نیست**. و چون کلاینت `offset` مثبت را نمی‌پذیرد (`offset in -7..0`)، **هیچ راهی برای دیدن عملیات جاری امروز وجود ندارد**. کاربر عملاً همیشه گزارش «روز کاری کامل قبلی» را با برچسب «امروز» می‌بیند.

بین ۰۰:۰۰ تا ۰۷:۰۰ یک شیفت اضافی هم عقب می‌رود:

```php
// AnalyticsController.php:296-299
$currentTime = time();
if (date('H') < 7) {
    $currentTime = strtotime('-1 day');
}
```

پس ساعت ۰۳:۰۰ بامداد، «امروز» به معنای **پریروز ۰۷:۰۰ تا دیروز ۰۷:۰۰** است.

**اصلاح — یکی از دو گزینه، بسته به نیت محصول:**

الف) اگر منظور واقعاً «۲۴ ساعت گذشته» است، پنجره باید تا لحظه حاضر کشیده شود:

```sql
WHERE c.status = 'خروج'
  AND ( (c.exitDate = ? AND c.exitTime >= ?)   -- دیروز از ساعت جاری
     OR (c.exitDate = ? AND c.exitTime <= ?) )  -- امروز تا ساعت جاری
```

ب) اگر منظور «روز کاری (۰۷:۰۰ تا ۰۷:۰۰)» است — که با ساختار کد سازگارتر است — کد درست است ولی **برچسب‌ها اشتباه‌اند**. باید:
- `dateInfo` بازه را صریح برگرداند (`از 1405/05/21 ساعت 07:00 تا 1405/05/22 ساعت 07:00`)،
- برچسب «امروز» به «روز کاری اخیر» تغییر کند،
- و `offset = +0` بازه در جریان (امروز ۰۷:۰۰ تا الان) را هم به‌عنوان یک گزینه اضافه کند.

---

### 🔴 B-2 | اولین باز شدن دیالوگ دو درخواست همسان می‌زند

```kotlin
// ComprehensiveAnalyticsDialog.kt:80-84
LaunchedEffect(isVisible) {
    if (isVisible) {
        viewModel.loadComprehensiveAnalytics()      // ← درخواست اول
    }
}
...
// ComprehensiveAnalyticsDialog.kt:143-147
ReportsViewModel.LoadingState.Idle -> {
    LaunchedEffect(Unit) {
        viewModel.loadComprehensiveAnalytics()      // ← درخواست دوم
    }
}
```

مقدار اولیه `_analyticsLoadingState` برابر `LoadingState.Idle` است (`ReportsViewModel.kt:106`). در اولین باز شدن دیالوگ، هر دو `LaunchedEffect` **در یک پاس composition ساخته می‌شوند** و هر دو اجرا می‌شوند — پیش از آن‌که هیچ‌کدام بتوانند وضعیت را به `Loading` تغییر دهند و شاخه `Idle` را از composition خارج کنند.

نتیجه: دو فراخوانی همزمان به سنگین‌ترین کوئری سرور. چون پاسخ `Cache-Control: no-store` دارد، OkHttp هیچ‌کدام را کش یا ادغام نمی‌کند و **هر دو full-scan را اجرا می‌کنند** (P-1).

ضمناً شاخه `Idle` **هیچ UIای رندر نمی‌کند** — کاربر یک فریم صفحه سفید می‌بیند.

**اصلاح:** شاخه `Idle` را از فراخوانی شبکه خالی کن و فقط همان indicator را نشان بده. تنها یک نقطه ورود بماند:

```kotlin
ReportsViewModel.LoadingState.Idle -> {
    Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
        CircularProgressIndicator(color = AnalyticsAccent)
    }
}
```

---

### 🟠 B-3 | Race بین پاسخ‌ها هنگام تعویض سریع تاریخ

```kotlin
// ReportsViewModel.kt:872-874
fun loadComprehensiveAnalytics() {
    viewModelScope.launch {              // ← بدون نگه‌داشتن Job، بدون cancel
        ...
    }
}
```

هر بار `setAnalyticsDateOffset` صدا زده شود یک کوروتین جدید ساخته می‌شود و **درخواست قبلی لغو نمی‌شود**. اگر کاربر سریع روی فلش‌ها بزند، N درخواست همزمان در پرواز است و آخرین پاسخِ **رسیده** (نه آخرینِ **درخواست‌شده**) در `_comprehensiveAnalytics` می‌نشیند.

نتیجه: هدر ناوبری تاریخ `offset = -3` را نشان می‌دهد در حالی که جدول داده `offset = -1` است — و هیچ نشانه‌ای از این ناسازگاری وجود ندارد.

راه‌حل درست دقیقاً در همین ViewModel برای real-time پیاده شده است (`ReportsViewModel.kt:298-303`):

```kotlin
private var realTimeFetchJob: Job? = null
private suspend fun fetchRealTimeDataCoordinated(isDarkTheme: Boolean) {
    realTimeFetchJob?.cancel()
    val job = viewModelScope.launch { fetchRealTimeData(isDarkTheme) }
    realTimeFetchJob = job
    job.join()
}
```

**اصلاح:** همان الگو:

```kotlin
private var analyticsFetchJob: Job? = null

fun loadComprehensiveAnalytics() {
    analyticsFetchJob?.cancel()
    analyticsFetchJob = viewModelScope.launch { ... }
}
```

---

### 🟠 B-4 | بعد از هر بارگذاری، یک فریم داده تاریخ قبلی نمایش داده می‌شود

مسیر داده دو مرحله‌ای است و مرحله دوم یک composition عقب‌تر است:

```kotlin
// QuotaAnalysisSection.kt:168-178
LaunchedEffect(completionData) {
    viewModel.updateInitialQuotas(completionData)   // ← _filteredQuotas را می‌سازد
}
...
val filteredQuotas by viewModel.filteredQuotas.collectAsState()   // ← هنوز مقدار قبلی
val activeQuotas = filteredQuotas.filter { ... }
```

وقتی پاسخ جدید می‌رسد، `_analyticsLoadingState` به `Success` و `_comprehensiveAnalytics` به داده جدید تغییر می‌کند و `QuotaAnalysis` بلافاصله recompose می‌شود — اما `filteredQuotas` هنوز مقدار **قبلی** را دارد، چون `LaunchedEffect` بعد از اعمال composition اجرا می‌شود.

نتیجه:
- **اولین باز شدن:** یک فریم پیام «کوتاژ فعالی در 24 ساعت گذشته وجود ندارد» نمایش داده می‌شود، بعد داده ظاهر می‌شود.
- **تعویض تاریخ:** یک فریم داده **روز قبلی** زیر تاریخ **جدید** نمایش داده می‌شود.

اصلاح ریشه‌ای همان C-1 است (حذف رفت‌وبرگشت). راه‌حل حداقلی: مشتق کردن `filteredQuotas` از `comprehensiveAnalytics` با `combine` در ViewModel به‌جای push از UI.

---

### 🟠 B-5 | کلید تکراری در `LazyColumn` ⇒ کرش احتمالی

```kotlin
// QuotaAnalysisSection.kt:1062-1065
items(
    items = quotas,
    key = { quota -> quota.loadingQuotaNumber + "_" + quota.shipName }
)
```

`OwnerQuotasDialog` روی زیرمجموعه‌ای از یک گروه کار می‌کند که در آن `shipName`، `cargoType`، `warehouse` و `cargoOwner` **همگی یکسان‌اند**. تنها ستون‌های متمایزکننده باقی‌مانده `loadingQuotaNumber` و `shippingCompany` هستند — و `shippingCompany` در کلید نیست.

سرور در `GROUP BY` صریحاً `c.shippingCompany` را دارد (`AnalyticsController.php:326`)، پس دو ردیف با کوتاژ یکسان و شرکت باربری متفاوت کاملاً ممکن است. جدول `InitialInfo` هم هیچ محدودیت یکتایی ندارد (`schema.sql:71-72` فقط `PRIMARY KEY (id)` و یک ایندکس **غیریکتا**).

با دو ردیف هم‌کلید، Compose خطای زیر را پرتاب می‌کند و دیالوگ کرش می‌کند:

```
IllegalArgumentException: Key "12345678_کشتی الف" was already used.
If you are using LazyColumn/Row please make sure you provide a unique key for each item.
```

**اصلاح:**

```kotlin
key = { q -> "${q.loadingQuotaNumber}_${q.shipName}_${q.shippingCompany}_${q.warehouse}_${q.cargoType}" }
```

---

### 🟠 B-6 | `JOIN` بدون فیلتر `isActive` و بدون تضمین یکتایی ⇒ شمارش مضاعف

```sql
FROM CargoInfo c
JOIN InitialInfo i ON c.loadingQuotaNumber = i.loadingQuotaNumber
    AND c.loadingWarehouse = i.loadingWarehouse
    AND c.shippingCompany  = i.shippingCompany
```

دو مشکل:

1. **`i.isActive = 1` اعمال نشده.** این دقیقاً همان اشکالی است که در تابع خواهر شناسایی و رفع شده — کامنت B-12 در `AnalyticsController.php:257-258`:
   > فیلتر `i.isActive = 1` (هم‌راستا با `getActiveQuotasRemaining`) اضافه شد تا کوتاژهای غیرفعال‌شده در «بارگیری لحظه‌ای» ظاهر نشوند.

   اما همین فیلتر در `handleComprehensiveAnalysisRequest` وجود ندارد ⇒ کوتاژهای غیرفعال‌شده در تحلیل جامع ظاهر می‌شوند و آمار دو صفحه با هم نمی‌خواند.

2. **کلید JOIN یکتا نیست.** سه‌تایی `(loadingQuotaNumber, loadingWarehouse, shippingCompany)` در `InitialInfo` هیچ `UNIQUE` ندارد. اگر دو ردیف با همین سه‌تایی ولی `cargoOwner` یا `cargoType` متفاوت وجود داشته باشد، **هر ردیف `CargoInfo` با هر دو تطبیق می‌خورد** و `SUM(c.netWeight)` عملاً دو برابر گزارش می‌شود.

**اصلاح:**

```sql
WHERE i.isActive = 1
  AND c.status = 'خروج'
  AND ( ... )
```

و در سطح دیتابیس، افزودن `UNIQUE KEY uq_initial_quota (loadingQuotaNumber, loadingWarehouse, shippingCompany)` — یا اگر تکرار قانونی است، اضافه‌کردن `cargoType` به شرط JOIN تا تطبیق قطعی شود.

---

### 🟠 B-7 | `COUNT(*)` به‌جای `COUNT(DISTINCT trackingNumber)`

```sql
-- AnalyticsController.php:320
SUM(c.netWeight) AS last_24h_weight, COUNT(*) AS last_24h_vouchers
```

در همان فایل، شمارش حواله در جای دیگر با DISTINCT انجام می‌شود:

```sql
-- AnalyticsController.php:382 (getActiveQuotasRemaining)
COUNT(DISTINCT trackingNumber) as voucherCount
```

`COUNT(*)` سطرهای `CargoInfo` را می‌شمارد، نه حواله‌های یکتا. اگر یک `trackingNumber` بیش از یک سطر داشته باشد (یا به دلیل تکثیر ناشی از B-6)، عدد «تعداد حواله» بیش‌برآورد می‌شود. برای سازگاری بین دو صفحه باید یکسان باشد:

```sql
COUNT(DISTINCT c.trackingNumber) AS last_24h_vouchers
```

---

### 🟡 B-8 | هدر تاریخ در حال بارگذاری، تاریخ قبلی را نشان می‌دهد و برای `offset ≤ -2` خالی می‌شود

```kotlin
// ComprehensiveAnalyticsDialog.kt:160-165, 200-205
val dateInfo = analytics?.dateInfo
val formattedDate = dateInfo?.jalaliDate ?: ""
val dayName = dateInfo?.dayName ?: ""
...
text = if (offset == 0) "امروز" else if (offset == -1) "دیروز" else dayName
```

`offset` بلافاصله تغییر می‌کند، ولی `analytics` تا رسیدن پاسخ همان مقدار قبلی است. پس:

- در بازه بارگذاری، شماره تاریخ **روز قبلی** زیر برچسب **روز جدید** نمایش داده می‌شود.
- برای `offset ≤ -2` قبل از رسیدن اولین پاسخ، `dayName` خالی است و **وسط نوار ناوبری کاملاً بی‌متن** می‌شود.
- در حالت `Error`، `analytics` ممکن است `null` بماند و هدر تاریخ خالی باشد بدون هیچ توضیحی.

**اصلاح:** تاریخ را سمت کلاینت از روی `offset` محاسبه کن (پروژه `JalaliDateUtils` دارد و در `ReportsViewModel.kt:38` import شده است) و `dateInfo` سرور را فقط برای تأیید استفاده کن — یا هنگام تغییر offset، مقدار قبلی را پاک کن.

---

### 🟡 B-9 | حالت خطا دکمه «تلاش مجدد» ندارد

```kotlin
// ComprehensiveAnalyticsDialog.kt:131-134
is ReportsViewModel.LoadingState.Error -> {
    val error = (loadingState as ReportsViewModel.LoadingState.Error).message
    ErrorStateCard(errorMessage = error)
}
```

`ErrorStateCard` (خطوط ۲۹۵-۳۳۶) فقط آیکون و متن است. تنها راه تلاش مجدد، **بستن و باز کردن دوباره دیالوگ** یا تغییر تاریخ است. با توجه به این‌که خطای شبکه در محیط بندری رایج است، این یک شکاف واقعی UX است.

```kotlin
ErrorStateCard(
    errorMessage = error,
    onRetry = { viewModel.loadComprehensiveAnalytics() }
)
```

---

### 🟡 B-10 | `EmptyStateCard` عملاً کد مرده است و پیام خطای واقعی سرور بلعیده می‌شود

```kotlin
// ComprehensiveAnalyticsDialog.kt:136-141
analyticsData?.quotaCompletionAnalysis?.let { quotaData ->
    QuotaAnalysis(...)
} ?: EmptyStateCard("داده‌ای برای کوتاژها یافت نشد")
```

`ComprehensiveAnalytics.quotaCompletionAnalysis` نوعش `List<QuotaCompletionData>` است — **non-null با مقدار پیش‌فرض `emptyList()`** (`ReportModels.kt:227`). پس `?.let` هرگز به شاخه `?:` نمی‌رسد مگر خود `analyticsData` تهی باشد. لیست خالی هم وارد `QuotaAnalysis` می‌شود و پیام متفاوتی («کوتاژ فعالی در 24 ساعت گذشته وجود ندارد») نشان می‌دهد. دو پیام خالی متفاوت برای عملاً یک وضعیت.

مورد مرتبط — پیام خطای سرور دور ریخته می‌شود:

```kotlin
// ReportsViewModel.kt:895-897
} else {
    _analyticsLoadingState.value = LoadingState.Error("خطا در دریافت اطلاعات تحلیلی")
}
```

سرور در `catch` خود پیام دقیق‌تری می‌فرستد (`{'error': ...}`) اما چون کلید `success` در آن پاسخ وجود ندارد، Gson مقدار `false` می‌گذارد و اینجا با یک متن عمومی جایگزین می‌شود. پیام سرور هرگز به کاربر یا لاگ نمی‌رسد.

---

### 🟡 B-11 | مرز روز ۰۷:۰۰ در برابر مرز شیفت ۰۷:۳۰

- تحلیل جامع: مرز روز `07:00:00` (`AnalyticsController.php:325`).
- بارگیری لحظه‌ای: مرز شیفت `07:30:00` (`AnalyticsController.php:211, 236`) — با کامنت صریحی که چرا باید دقیقاً ۰۷:۳۰ باشد.

حواله‌های خروج‌شده بین **۰۷:۰۰ تا ۰۷:۳۰** در تحلیل جامع به روز جدید و در بارگیری لحظه‌ای به شیفت شب قبلی نسبت داده می‌شوند. اینجا داده‌ای گم نمی‌شود، اما دو صفحه برای یک بازه دو عدد متفاوت می‌دهند و کاربر راهی برای فهمیدن دلیل ندارد. این دو ثابت باید در یک نقطه (`config`) تعریف و مستند شوند.

---

## ۵. پرفورمنس

### 🔴 P-1 | هیچ ایندکسی روی `CargoInfo.exitDate` وجود ندارد ⇒ Full Table Scan

ایندکس‌های موجود (`schema.sql:45-46`):

```sql
KEY `idx_cargo_status_group` (loadingQuotaNumber, shipName, loadingWarehouse, shippingCompany, cargoType, status, netWeight),
KEY `idx_cargo_ship_lookup`  (shipName, loadingWarehouse, status, loadingQuotaNumber, shippingCompany, cargoType, netWeight)
```

اما شرط `WHERE` این کوئری روی `status`, `exitDate`, `exitTime` است:

```sql
WHERE c.status = 'خروج'
  AND ( (c.exitDate = ? AND c.exitTime >= '07:00:00')
     OR (c.exitDate = ? AND c.exitTime <  '07:00:00') )
```

`exitDate` و `exitTime` **در هیچ ایندکسی نیستند** (جست‌وجوی کل `schema.sql` فقط تعریف ستون را برمی‌گرداند)، و `status` در هر دو ایندکس موجود ستون سوم/ششم است — بدون پیشوند قابل استفاده. نتیجه: MySQL کل جدول `CargoInfo` را اسکن می‌کند، سپس `JOIN` و `GROUP BY` روی شش ستون از دو جدول را با temp table + filesort انجام می‌دهد.

این جدول اصلی‌ترین جدول تراکنشی سیستم است و مدام رشد می‌کند. هزینه با اندازه کل تاریخچه رشد می‌کند، نه با اندازه بازه ۲۴ ساعته — یعنی این کوئری با گذشت زمان **به‌طور خطی کندتر می‌شود** در حالی که حجم داده برگشتی ثابت است. و طبق B-2 هر باز شدن دیالوگ آن را دو بار اجرا می‌کند.

**اصلاح (بالاترین نسبت اثر به هزینه در کل این گزارش):**

```sql
ALTER TABLE `CargoInfo`
  ADD KEY `idx_cargo_exit_window` (`exitDate`, `status`, `exitTime`, `netWeight`);
```

با این ایندکس، هر دو شاخه `OR` به دو `ref` lookup روی `exitDate` تبدیل می‌شوند (index merge union) و `netWeight` انتهای ایندکس آن را برای `SUM` پوششی می‌کند.

> نکته جانبی: `exitDate` از نوع `varchar(100)` برای نگه‌داری تاریخ شمسی «۱۴۰۵/۰۵/۲۲» است. برای مقایسه تساوی مشکلی ندارد، ولی ۱۰۰ کاراکتر برای یک رشته ۱۰ کاراکتری، ایندکس را بی‌دلیل بزرگ می‌کند. `varchar(10)` کافی است.

---

### 🟠 P-2 | نه MicroCache، نه ETag — برخلاف endpoint خواهر در همان فایل

تابع `getRealTimeData` در همان کلاس هر دو را دارد:

```php
// AnalyticsController.php:253
return MicroCache::remember($cacheKey, 5, function () use ($shiftInfo) { ... });
```

```php
// AnalyticsController.php:194-206
$etag = '"' . md5(json_encode($data, JSON_UNESCAPED_UNICODE)) . '"';
header('Cache-Control: private, max-age=5');
header("ETag: $etag");
if ($ifNoneMatch !== null && trim($ifNoneMatch) === $etag) {
    http_response_code(304);
    exit;
}
```

`handleComprehensiveAnalysisRequest` **هیچ‌کدام را ندارد** و `Cache-Control: no-store` سراسری (خط ۵۴) را هم override نمی‌کند.

این در حالی است که این endpoint **کاندیدای به‌مراتب بهتری برای کش است**:
- برای `offset < 0` داده مربوط به یک روز تمام‌شده است و دیگر تغییر نمی‌کند ⇒ TTL می‌تواند ساعت‌ها باشد.
- برای `offset = 0` هم — با توجه به B-1 که پنجره ساعت ۰۷:۰۰ بسته می‌شود — داده تا ۰۷:۰۰ بعدی ثابت است.
- کوئری‌اش بسیار سنگین‌تر از real-time است (P-1).

**اصلاح:**

```php
$cacheKey = 'analytics_comprehensive_' . md5($todayJalaliDate . '|' . $yesterdayJalaliDate);
$ttl = ($offset < 0) ? 3600 : 60;

$completionData = MicroCache::remember($cacheKey, $ttl, function () use (...) {
    // اجرای کوئری
});
```

به‌همراه همان الگوی ETag/304 که برای real-time نوشته شده است.

---

### 🟠 P-3 | فیلتر و گروه‌بندی کامل در هر ضربه کلید، بدون debounce، دو بار

با هر کاراکتر در فیلد جستجو:

**مسیر ۱ — داخل ViewModel** (`ReportsViewModel.kt:172-229`): `updateSearchQuery` → `updateFilteredQuotas` → `filter` + `groupBy` + `sortedWith` + `flatMap` روی کل لیست.

**مسیر ۲ — داخل Composable** (`QuotaAnalysisSection.kt:178-180, 319-329`): همان لیست دوباره `filter` + `sortedByDescending` + `groupBy` می‌شود:

```kotlin
val activeQuotas = filteredQuotas               // ← بدون remember
    .filter { it.last_24h_vouchers > 0 }
    .sortedByDescending { it.last_24h_vouchers }
...
val grouped = when (groupingMode) { ... groupBy { ... } }   // ← بدون remember
```

فقط مرحله آخر (`sortedGroups`) داخل `remember` است (خط ۳۳۱) — و آن هم چون کلیدش `activeQuotas` است، در هر recomposition یک مقایسه ساختاری `O(n)` روی کل لیست انجام می‌دهد.

هیچ debounce یا `snapshotFlow` وجود ندارد و همه این کار **روی رشته اصلی** است.

**اصلاح:** حذف مسیر ۲ با انتقال کامل مشتق‌سازی به ViewModel (بخش C-1) و افزودن debounce:

```kotlin
val filteredQuotas: StateFlow<List<QuotaCompletionData>> =
    combine(
        _comprehensiveAnalytics,
        _searchQuery.debounce(250),
        _groupingMode
    ) { analytics, query, mode -> buildGroups(analytics, query, mode) }
        .flowOn(Dispatchers.Default)
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), emptyList())
```

---

### 🟠 P-4 | خروجی مرتب‌سازی ViewModel کاملاً دور ریخته می‌شود

`updateFilteredQuotas` (`ReportsViewModel.kt:202-227`) لیست را گروه‌بندی، مرتب و `flatMap` می‌کند تا یک **ترتیب** خاص بسازد. اما UI بلافاصله آن را دوباره `groupBy` و `sortedWith` می‌کند (`QuotaAnalysisSection.kt:319-356`) و ترتیب ورودی را کاملاً نادیده می‌گیرد.

بدتر از آن، دو طرف **کلیدهای گروه‌بندی متفاوتی** دارند:

| محل | کلید `BY_CARGO_OWNER` |
|---|---|
| `ReportsViewModel.kt:222` | `"${shipName}\|${warehouse}\|${cargoType}"` |
| `QuotaAnalysisSection.kt:321` | `"${shipName}\|${cargoType}\|${warehouse}"` |

ترتیب دو جزء وسط برعکس است. چون خروجی ViewModel استفاده نمی‌شود این تناقض امروز اثر مرئی ندارد — و همین آن را به یک **تله برای توسعه‌دهنده بعدی** تبدیل می‌کند: هر کسی که به منطق مرتب‌سازی ViewModel دست بزند، هیچ تغییری در UI نخواهد دید.

از `updateFilteredQuotas` فقط بخش `filter` (خطوط ۱۹۱-۱۹۹) واقعاً استفاده می‌شود. باقی باید حذف یا به تنها منبع حقیقت تبدیل شود.

---

### 🟡 P-5 | `Column(verticalScroll)` داخل `LazyColumn` — همه ردیف‌های گروه eager ساخته می‌شوند

```kotlin
// QuotaAnalysisSection.kt:650-688
Column(
    modifier = Modifier
        .heightIn(max = 400.dp)
        .verticalScroll(rememberScrollState())
) {
    quotas.forEach { quota -> AnalyticsQuotaCard(...) }   // ← بدون lazy
}
```

دو پیامد:

1. تمام کارت‌های یک گروه — حتی آن‌هایی که خارج از ۴۰۰dp قابل مشاهده‌اند — composed و measure می‌شوند.
2. اسکرول عمودی تودرتو داخل `LazyColumn` عمودی: ژست اسکرول داخلی و بیرونی با هم رقابت می‌کنند و کاربر هنگام رسیدن به انتهای لیست داخلی، «گیر کردن» اسکرول را حس می‌کند.

برای گروه‌های بزرگ (یک کشتی با ده‌ها کوتاژ) این محسوس است. جایگزین بهتر: به‌جای پنل قابل باز شدن با اسکرول داخلی، از `stickyHeader` + آیتم‌های مسطح در همان `LazyColumn` بیرونی استفاده شود تا بازیافت (recycling) واقعی اتفاق بیفتد.

---

### 🟡 P-6 | رفت‌وبرگشت اضافی تاریخ در سرور

```php
// AnalyticsController.php:301-316
$todayJalaliDate = jdate('Y/m/d', $targetTime);
$dateParts = explode('/', $todayJalaliDate);
$gy = (int)$dateParts[0]; $gm = (int)$dateParts[1]; $gd = (int)$dateParts[2];
if (function_exists('jalali_to_gregorian')) {
    $gDate = jalali_to_gregorian($gy, $gm, $gd);
    $timestamp = mktime(12, 0, 0, $gDate[1], $gDate[2], $gDate[0]);
} else {
    $timestamp = time();
}
$dateInfo = ['jalaliDate' => $todayJalaliDate, 'dayName' => jdate('l', $timestamp)];
```

`$targetTime` **از قبل یک timestamp معتبر است**. کل این بلوک (تبدیل به رشته شمسی → split → تبدیل به میلادی → `mktime` → تبدیل دوباره به شمسی) با یک خط معادل است:

```php
$dateInfo = [
    'jalaliDate' => jdate('Y/m/d', $targetTime),
    'dayName'    => jdate('l', $targetTime),
];
```

هزینه CPU ناچیز است اما دو مشکل واقعی دارد (بخش C-8): نام‌گذاری گمراه‌کننده متغیرها و یک fallback خطرناک.

---

### 🟢 P-7 | مواردی که درست انجام شده‌اند

- `AnalyticsQuotaGroupExpansionPanel` وضعیت `isExpanded` را داخل lambda آیتم می‌خواند، پس باز/بسته کردن یک گروه فقط همان آیتم را recompose می‌کند، نه کل لیست.
- `items(key = ...)` روی `LazyColumn` بیرونی ست شده و کلیدش (`"group_$groupName"`) به‌درستی یکتاست.
- `@Immutable` روی مدل‌ها (`ReportModels.kt:206, 242`) اعلام شده که به Compose اجازه skip می‌دهد.
- `FloatTypeAdapter` سفارشی (`RetrofitClient.kt:41-64`) هم `null`، هم عدد و هم رشته را امن پارس می‌کند — بنابراین `last_24h_weight` در برابر تغییر نوع سرور شکننده نیست.
- پاسخ سرور با `ob_gzhandler` فشرده می‌شود.

---

## ۶. کیفیت کد و معماری

### C-1 | داده از ViewModel به UI می‌رود و دوباره به ViewModel برمی‌گردد

هسته اصلی بیشتر مشکلات بالا (B-4، P-3، P-4) همین است:

```kotlin
// QuotaAnalysisSection.kt:168-170
LaunchedEffect(completionData) {
    viewModel.updateInitialQuotas(completionData)
}
```

Composable داده‌ای را که خودش از ViewModel گرفته، دوباره **به ViewModel push می‌کند** تا ViewModel آن را در `_initialQuotas` ذخیره و فیلتر کند. این یعنی:

- منبع حقیقت دو تکه است (`_comprehensiveAnalytics` و `_initialQuotas`) و می‌توانند از هم جدا بیفتند.
- UI به یک عضو چرخه داده تبدیل شده و بدون رندر شدن، فیلتر کار نمی‌کند.
- تست‌پذیری از بین می‌رود: نمی‌توان منطق فیلتر را بدون composition آزمود.

**شکل درست:**

```kotlin
val filteredQuotas: StateFlow<List<QuotaCompletionData>> =
    combine(_comprehensiveAnalytics, _searchQuery, _groupingMode) { data, q, mode ->
        deriveGroups(data?.quotaCompletionAnalysis.orEmpty(), q, mode)
    }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), emptyList())
```

و حذف کامل `updateInitialQuotas`، `_initialQuotas` و `updateFilteredQuotas`.

---

### C-2 | منطق گروه‌بندی و ساخت عنوان، سه بار کپی شده است

بلوک زیر عیناً (با تفاوت‌های جزئی) در سه جا تکرار شده است:

| محل | کاربرد |
|---|---|
| `QuotaAnalysisSection.kt:204-234` | اشتراک‌گذاری کل |
| `QuotaAnalysisSection.kt:319-355` | نمایش لیست |
| `QuotaAnalysisSection.kt:486-493` | اشتراک‌گذاری یک گروه |

و منطق «تبدیل کلید به عنوان خوانا» چهار بار (خطوط ۲۴۴-۲۵۱، ۴۸۶-۴۹۳، ۵۴۰-۵۴۶، ۵۵۳-۵۷۷). هر تغییری در قالب گروه‌بندی باید در چهار نقطه اعمال شود — و همین حالا هم بین آن‌ها ناسازگاری هست (P-4).

پیشنهاد: استخراج به `feature/reports/domain/AnalyticsGrouping.kt` با یک تابع خالص:

```kotlin
data class QuotaGroup(
    val ship: String,
    val cargoType: String?,
    val warehouse: String?,
    val carrier: String?,
    val quotas: List<QuotaCompletionData>
) {
    val totalWeight: Float get() = quotas.sumOf { it.last_24h_weight.toDouble() }.toFloat()
    val totalVouchers: Int get() = quotas.sumOf { it.last_24h_vouchers }
    fun title(mode: QuotaGroupingMode): String = ...
}

fun buildGroups(data: List<QuotaCompletionData>, mode: QuotaGroupingMode): List<QuotaGroup>
```

این کار هم قابل تست واحد است، هم C-3 را حذف می‌کند.

---

### C-3 | کلید ترکیبی به‌صورت رشته `"a|b|c"` با `split` دستی

```kotlin
activeQuotas.groupBy { "${it.shipName}|${it.cargoType ?: "نامشخص"}|${it.warehouse ?: "نامشخص"}" }
...
val parts = it.first.split("|")
val warehouse = parts.getOrNull(2)?.trim() ?: "نامشخص"
```

الگویی شکننده: اگر نام کشتی یا انبار شامل کاراکتر `|` باشد (هیچ اعتبارسنجی‌ای مانع آن نیست، هر دو `varchar(100)` آزادند)، `split` نتیجه اشتباه می‌دهد و شماره اندیس‌ها جابه‌جا می‌شود — بی‌سروصدا، بدون کرش. کد در پنج نقطه با `parts.size >= 3`، `parts.size >= 2` و `getOrNull` این شکنندگی را مدیریت می‌کند، که خودش نشانه است.

جایگزین: یک `data class` به‌عنوان کلید `groupBy` (Kotlin به‌طور خودکار `equals`/`hashCode` می‌دهد).

---

### C-4 | عدد جادویی `-7` در دو فایل مستقل

```kotlin
ReportsViewModel.kt:113             if (offset in -7..0)
ComprehensiveAnalyticsDialog.kt:225 enabled = offset > -7
```

و سمت سرور اصلاً وجود ندارد (A-2). سه نقطه که باید هماهنگ بمانند و هیچ چیزی این هماهنگی را تضمین نمی‌کند. یک `const val ANALYTICS_MAX_DAYS_BACK = 7` مشترک لازم است.

---

### C-5 | وضعیت مخصوص این دیالوگ در ViewModel عمومی و بدون reset

`_searchQuery`، `_groupingMode`، `_filteredQuotas` و `_initialQuotas` نام‌های کاملاً عمومی دارند اما بررسی نشان می‌دهد **فقط** توسط همین دیالوگ استفاده می‌شوند. آن‌ها در یک `ReportsViewModel` نزدیک به ۱۰۰۰ خطی نشسته‌اند و:

- با نام‌های عمومی، خطر استفاده تصادفی توسط صفحه دیگری وجود دارد (پروژه از قبل `_warehouseQuotaGroupingMode` جدا دارد — نشانه‌ای از همین سردرگمی).
- هنگام بستن دیالوگ **هیچ‌کدام reset نمی‌شوند**: کاربر جستجو می‌کند، دیالوگ را می‌بندد، دوباره باز می‌کند و همان فیلتر قبلی هنوز اعمال است در حالی که فیلد جستجو... هم پر است. رفتار قابل دفاعی است اما عمدی به نظر نمی‌رسد.
- درخواست در حال پرواز هنگام بستن دیالوگ لغو نمی‌شود.

پیشنهاد: پیشوند `analytics` روی نام‌ها، و `fun onAnalyticsDialogDismissed()` برای پاک‌سازی.

---

### C-6 | `viewModelScope.launch` بی‌اثر

```kotlin
// ReportsViewModel.kt:178-183, 186-188
fun updateInitialQuotas(quotas: List<QuotaCompletionData>) {
    viewModelScope.launch {
        _initialQuotas.value = quotas
        updateFilteredQuotas()
    }
}
```

`viewModelScope` روی `Dispatchers.Main.immediate` است و بدنه هیچ نقطه تعلیقی ندارد؛ پس این بلوک **همان‌جا و به‌صورت همزمان روی رشته اصلی** اجرا می‌شود. `launch` اینجا هیچ کاری نمی‌کند جز پنهان کردن این واقعیت که کار سنگین (فیلتر + گروه‌بندی + مرتب‌سازی کل لیست) روی رشته اصلی است. یا باید حذف شود، یا با `withContext(Dispatchers.Default)` واقعاً از رشته اصلی خارج شود.

---

### C-7 | نام‌گذاری `snake_case` در مدل Kotlin

```kotlin
// ReportModels.kt:235-236, 247-248
val last_24h_weight: Float,
val last_24h_vouchers: Int,
```

خلاف قرارداد Kotlin و ناسازگار با بقیه فیلدهای همان کلاس (`loadingQuotaNumber`, `shipName`). این نام‌ها در ۱۴ نقطه از UI تکرار شده‌اند. راه‌حل تمیز بدون تغییر قرارداد سرور:

```kotlin
@SerializedName("last_24h_weight") val last24hWeight: Float,
@SerializedName("last_24h_vouchers") val last24hVouchers: Int,
```

---

### C-8 | نام متغیرهای گمراه‌کننده و fallback خطرناک در سرور

```php
// AnalyticsController.php:304-311
$gy = (int)$dateParts[0]; $gm = (int)$dateParts[1]; $gd = (int)$dateParts[2];
if (function_exists('jalali_to_gregorian')) {
    $gDate = jalali_to_gregorian($gy, $gm, $gd);
    ...
} else {
    $timestamp = time();   // ← بی‌صدا تاریخ اشتباه برمی‌گرداند
}
```

- پیشوند `g` (gregorian) روی متغیرهایی که مقدار **شمسی** نگه می‌دارند. کد درست کار می‌کند ولی خواننده بعدی را گمراه می‌کند؛ باید `$jy, $jm, $jd` باشد.
- شاخه `else`: اگر `jalali_to_gregorian` موجود نباشد، `dayName` بی‌سروصدا نام روز **امروز** را برمی‌گرداند برای تاریخی که ممکن است هفته پیش باشد — یک خطای داده بی‌صدا. ضمناً این بررسی زائد است: `jdf.php` هم `jdate` و هم `jalali_to_gregorian` را تعریف می‌کند (`jdf.php:12, 626`) و اگر بارگذاری نشده بود، خط ۳۰۱ (`jdate`) پیش‌تر شکست می‌خورد.

با اصلاح P-6 هر دو مشکل همزمان حل می‌شوند.

---

### C-9 | موارد کوچک‌تر

| # | مورد | محل |
|---|---|---|
| ۱ | `Modifier.fillMaxSize().weight(1f)` — `fillMaxSize` در کنار `weight` در یک `Column` زائد و متناقض است | `ComprehensiveAnalyticsDialog.kt:111` |
| ۲ | `formatNumber` دو پیاده‌سازی یکسان دارد: `ReportsViewModel.kt:236` و `ReportsDomain.kt:125`. دیالوگ از دومی استفاده می‌کند؛ اولی برای این مسیر مرده است | هر دو فایل |
| ۳ | `AnalyticsOwnerSummaryCard` مقدار وزن را با `.toInt()` (کوتاه‌سازی) نمایش می‌دهد در حالی که همه جای دیگر `.roundToInt()` است ⇒ اختلاف ۱ تنی بین کارت خلاصه و مجموع گروه | `QuotaAnalysisSection.kt:947` در برابر `621, 849` |
| ۴ | `AnalyticsGroupingModeButton` از `Surface + clickable` بدون `role = Role.Button` و بدون semantics `selected` استفاده می‌کند ⇒ TalkBack وضعیت انتخاب را اعلام نمی‌کند | `QuotaAnalysisSection.kt:407-414` |
| ۵ | placeholder پیش‌فرض `SearchField` می‌گوید «نام کشتی، شماره...» اما فراخوانی واقعی `keyboardType = Number` می‌دهد، پس عملاً جستجو با نام کشتی ممکن نیست — هرچند فیلتر ViewModel آن را پشتیبانی می‌کند | `QuotaAnalysisSection.kt:95, 198-199` در برابر `ReportsViewModel.kt:195` |
| ۶ | تمام رشته‌های فارسی hardcode شده‌اند (بدون `strings.xml`) — همسو با بقیه پروژه، ولی محلی‌سازی و اصلاح متن را پرهزینه می‌کند | هر دو فایل UI |
| ۷ | `QuotaAnalysis` کل `ReportsViewModel` را به‌عنوان پارامتر می‌گیرد ⇒ غیرقابل `@Preview` و غیرقابل تست جدا | `QuotaAnalysisSection.kt:162-165` |

---

## ۷. اولویت‌بندی اقدامات

### فوری (این هفته)

| # | اقدام | فایل | اثر |
|---|---|---|---|
| ۱ | افزودن `requirePermission('view_reports')` به `getComprehensiveAnalysis` (و `getRealTimeData`, `getKotazhInfo`) | `AnalyticsController.php` | بستن broken access control (A-1) |
| ۲ | افزودن ایندکس `idx_cargo_exit_window` روی `CargoInfo` | `schema.sql` + مهاجرت | حذف full scan؛ بیشترین سود پرفورمنسی (P-1) |
| ۳ | تصمیم‌گیری درباره معنای پنجره زمانی و اصلاح کوئری **یا** برچسب‌ها | `AnalyticsController.php:293-330` + UI | رفع نامرئی بودن عملیات امروز (B-1) |
| ۴ | حذف `LaunchedEffect(Unit)` از شاخه `Idle` | `ComprehensiveAnalyticsDialog.kt:143-147` | نصف شدن بار سرور در باز شدن دیالوگ (B-2) |
| ۵ | کلمپ `offset` سمت سرور به `max(-7, min(0, ...))` | `AnalyticsController.php:294` | A-2 |

### کوتاه‌مدت (این اسپرینت)

| # | اقدام | اثر |
|---|---|---|
| ۶ | `analyticsFetchJob` با `cancel()` مطابق الگوی real-time | B-3 |
| ۷ | افزودن `i.isActive = 1` و `COUNT(DISTINCT c.trackingNumber)` به کوئری | B-6، B-7 — درستی آمار |
| ۸ | اصلاح کلید `LazyColumn` در `OwnerQuotasDialog` | B-5 — رفع کرش احتمالی |
| ۹ | `HttpStatusException` در `getComprehensiveAnalysis` + تفکیک ۴۰۱ | A-3 |
| ۱۰ | افزودن `MicroCache::remember` + ETag/304 | P-2 |
| ۱۱ | دکمه «تلاش مجدد» در `ErrorStateCard` | B-9 |

### میان‌مدت (بدهی فنی)

| # | اقدام | اثر |
|---|---|---|
| ۱۲ | حذف رفت‌وبرگشت UI↔ViewModel و مشتق‌سازی `filteredQuotas` با `combine` + `debounce` + `flowOn(Default)` | C-1، P-3، P-4، B-4 یکجا |
| ۱۳ | استخراج `AnalyticsGrouping.kt` با `data class QuotaGroup` و تابع خالص `buildGroups` | C-2، C-3 |
| ۱۴ | ثابت مشترک برای بازه روز و مرزهای زمانی (۰۷:۰۰ / ۰۷:۳۰) | C-4، B-11 |
| ۱۵ | ساده‌سازی محاسبه `dateInfo` به دو فراخوانی `jdate` | P-6، C-8 |
| ۱۶ | `UNIQUE KEY` روی سه‌تایی کلید JOIN در `InitialInfo` | ریشه B-5 و B-6 |
| ۱۷ | تأیید و ثبت ممیزی برای اشتراک‌گذاری داده | A-5 |

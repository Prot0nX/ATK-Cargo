# استانداردسازی کامنت‌های چندخطی به تک‌خطی (کلاینت + سرور)

## Context

در حال حاضر کدبیس ATK-Cargo (هم اپ اندروید Kotlin و هم بک‌اند PHP) پر از بلاک‌کامنت‌های چندخطی با سبک‌های ناهماهنگ است: KDoc چندخطی، PHPDoc چندخطی با/بدون تگ، و رشته‌های پیاپی `//`. کاربر می‌خواهد همه‌ی این‌ها به یک کامنت تک‌خطی استاندارد و مختصر تبدیل شوند تا توسعه‌دهندگان دیگر سریع‌تر منظور هر بخش را بفهمند. این یک تغییر سراسری و مکانیکی (اما نیازمند بازنویسی معنایی هر کامنت، نه صرفاً truncate) در حدود ۱۸۰-۲۰۰ فایل است.

بررسی اولیه (۳ ایجنت Explore) نتایج زیر را برگرداند:

- **Kotlin (اپ اندروید):** ۳۶۵ فایل .kt در app/، core/{common,database,designsystem,domain,network}، feature/{admin,auth,cargo,cargo-workflow,chat,home,reports,startup,update}، baselineprofile/. حدود ۱۲۰-۱۴۰ فایل دارای کامنت چندخطی (۳۰ بلاک KDoc چندخطی تأیید شده + ~۱۱۸ فایل با رشته‌ی ۲+ خط `//`). بیشترین تراکم: `core/designsystem/.../ui/theme/*` (۱۱ فایل)، `feature/cargo-workflow/`، `core/network/.../api/` (Token/Auth)، `app/.../api/` (نوتیفیکیشن/سرویس‌ها).
- **PHP (بک‌اند سرور):** ۱۵۱ فایل .php غیر-vendor در PHP/src, PHP/api, PHP/User, PHP/config, PHP/migrations, PHP/deploy, PHP/tests + اسکریپت‌های ریشه. حدود ۵۵-۶۰ فایل دارای کامنت چندخطی (۵۲ فایل PHPDoc + ~۴۶ فایل رشته‌ی `//`). بیشترین تراکم: `PHP/src/Services/*`, `PHP/src/Controllers/*`, `PHP/src/Core/Router.php`.
- **بدون Dokka/Javadoc generation** پیکربندی نشده (فقط graphify و phpstan.neon پیدا شد) — یعنی حذف سینتکس KDoc/PHPDoc بی‌خطر است از نظر ابزار مستندسازی.
- **PHPStan (`PHP/phpstan.neon`) واقعاً فعال است** و به تگ‌های نوع در PHPDoc (مثل `@return array{total:int,...}` در `LicenseAdminService.php`) برای type-checking متکی است.
- **یک هدر لایسنس واقعی شخص‌ثالث** در `PHP/jdf.php:1-9` (GNU/LGPL، Reza Gholampanahi) — این نباید دست‌خورده یا کوتاه شود.
- هیچ TODOهای بلاکی یا کامنت کپی‌رایت دیگری پیدا نشد.

کاربر در پاسخ به سؤال شفاف‌سازی، گزینه‌ی «نگه‌داشتن تگ‌های نوع، فشرده‌سازی فقط بخش توضیحی» را انتخاب کرد.

## قوانین استاندارد تبدیل

1. **KDoc چندخطی (Kotlin):** بلاک `/** ... */` که فقط توضیح متنی است (بدون @param/@return) به یک خط `// <خلاصه‌ی مختصر>` بلافاصله بالای همان تعریف تبدیل می‌شود؛ سینتکس KDoc حذف می‌شود (چون Dokka پیکربندی نشده).
2. **رشته‌های `//` چندخطی (Kotlin و PHP):** چند خط `//` پیاپی که یک توضیح واحد را تشکیل می‌دهند، به یک خط `//` با همان مفهوم چکیده می‌شوند.
3. **PHPDoc چندخطی با تگ (@param/@return/@throws):** فقط بخش توضیح متنی/prose (خط یا خطوط اول قبل از تگ‌ها) به یک خط چکیده می‌شود؛ **تمام خطوط تگ (@param, @return, @throws و انواعشان از جمله array shape) دقیقاً دست‌نخورده باقی می‌مانند** تا phpstan خراب نشود.
4. **[به‌روزرسانی] همه‌ی کامنت‌ها به فارسی ترجمه/خلاصه می‌شوند** — کامنت‌های انگلیسی هم هنگام تک‌خطی‌سازی به فارسی برگردانده می‌شوند (تصمیم اصلاح‌شده در تاریخ ۱۴۰۵/۰۵/۳۰؛ rule 4 اصلی مبنی بر حفظ زبان اصلی لغو شد).
5. **`PHP/jdf.php` سطرهای ۱ تا ۹ دست‌نخورده می‌ماند** (هدر لایسنس شخص‌ثالث).
6. **بلاک‌های چندخطی که در واقع کد غیرفعال‌شده (commented-out code) هستند، نه توضیح متنی، از این تغییر مستثنی می‌شوند** — چون موضوع تسک، توضیحات است نه کد.
7. کامنت‌های تک‌خطی موجود دست‌نخورده می‌مانند (فقط چندخطی‌ها هدف هستند).

## دایرکتوری‌های مستثنی (اصلاً لمس نشوند)

`build/` (همه‌جا)، `.gradle/`، `.kotlin/`، `.idea/`، `.cxx/`، `app/release/`، `mapping-archive/`، `graphify-out/`، `.git/`، `PHP/vendor/`، کش `PHP/build/phpstan`.

## نحوه‌ی اجرا

با توجه به حجم کار (~۲۰۰ فایل، تخمین ۴۵۰-۶۰۰ بلاک کامنت)، اجرا به‌صورت دسته‌ای و موازی با چند subagent از نوع general-purpose (چون نیاز به Edit دارند) انجام می‌شود، هر کدام مسئول یک دایرکتوری/ماژول مشخص، مثلاً:

- Kotlin: به تفکیک `core/designsystem/theme`, `core/network`, `core/database`, `core/domain`, `feature/cargo-workflow`, `feature/cargo`, `feature/auth+admin`, `feature/chat+home+reports+startup+update`, `app/src/main/java/.../api` + بقیه‌ی app/
- PHP: به تفکیک `PHP/src/Controllers`, `PHP/src/Services`, `PHP/src/Core`+`PHP/src/Validators`+بقیه‌ی src, `PHP/tests`, اسکریپت‌های ریشه (با احتیاط ویژه برای jdf.php)

هر subagent باید: فایل‌های دایرکتوری خودش را Grep/Read کند، هر بلاک کامنت چندخطی را طبق قوانین بالا بازنویسی کند، و در پایان فهرست فایل‌های تغییریافته را گزارش دهد. دسته‌ها به‌صورت موازی (۳-۴ agent هم‌زمان) اجرا می‌شوند تا مرور نتایج قابل‌کنترل بماند.

## Verification

- **Kotlin:** اجرای `./gradlew :app:compileDebugKotlin` (یا build ماژول‌های دست‌خورده) برای اطمینان از این‌که هیچ سینتکس کامنتی به‌اشتباه بسته/باز نمانده.
- **PHP:** اجرای `vendor/bin/phpstan analyse` برای تأیید این‌که تگ‌های نوع دست‌نخورده مانده‌اند و آنالیز نوع همچنان پاس می‌شود؛ اجرای `vendor/bin/phpunit` برای اطمینان از عدم شکستن تست‌ها.
- بررسی نمونه‌ای (spot-check) چند فایل از هر دسته برای کیفیت خلاصه‌سازی.
- طبق `CLAUDE.md` پروژه، در پایان `graphify update .` اجرا شود تا گراف به‌روز شود.
